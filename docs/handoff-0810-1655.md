# Taptolk handoff — SOLAPI SMS provider boundary

## Checkpoint

- Branch: `codex/phase-1-foundation`
- HEAD: `e5bb97e`
- Existing WIP was preserved; no unrelated files were reverted or cleaned.
- No Production Supabase, Cron, Kakao, or Vercel setting was changed.

## Completed

- Added the official `solapi@5.5.1` Node SDK as a server-only web dependency.
- Added `SolapiSmsNotificationProvider` behind the existing `OwnerNotificationProvider` port.
- Added server-only `SOLAPI_API_KEY`, `SOLAPI_API_SECRET`, and digits-only `SOLAPI_SMS_FROM` configuration.
- SMS text contains only typed locale copy, the selected reason, and the one-time Taptolk response URL.
- Owner phone decryption remains inside the provider boundary; no phone value is logged or persisted.
- SOLAPI receipt IDs are persisted through the existing notification delivery service.
- Existing retry, lease, idempotency, and PWA response-token flow remain in use.
- Added a new migration that makes SMS the current Owner-contact queue channel while retaining the Kakao enum and history compatibility.
- Added provider/config tests and updated the database contract test for the current SMS-first decision.

## Validation evidence

- `corepack pnpm verify` PASS
- `corepack pnpm validate:design-system` PASS
- `corepack pnpm validate:wcj` PASS
- `corepack pnpm db:check` PASS
- 72 test files / 475 tests PASS

## External gate

Before any real send, the operator must create or identify a SOLAPI API Key and API Secret, register and approve the SOLAPI sender number, and inject the values through the approved server environment. Secrets and phone numbers must not be sent through chat, Git, docs, logs, or browser payloads.

## Exact next step

Configure the three server-only SOLAPI values in a non-Production staging environment, run one explicitly approved test SMS to an operator-controlled number, then open the received one-time response URL and complete the owner reply in the Taptolk PWA. Record only count/status/message-id-safe evidence and remove or rotate test credentials before any Production discussion.
