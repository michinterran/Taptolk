# Taptolk handoff — 2026-08-15 11:03 KST

## Current repository state

- Branch: `codex/phase-1-foundation`
- Starting HEAD: `8e72ff1`
- Existing dirty WIP was preserved. Do not reset, clean, stash, or broadly stage it.
- Stage only explicitly owned paths; never use `git add -A` or `git add .`.

## Completed scope

- Added redacted SOLAPI delivery-result ingestion for `SINGLE-REPORT` webhooks.
- Added webhook SHA-1 secret verification, bounded batch handling, and database-before-200 acknowledgement.
- Added delivery result persistence and idempotent reconciliation with notification deliveries.
- Added SOLAPI account balance snapshots and configurable warning/staleness policies.
- Added tenant-scoped delivery health and platform-only balance health to the operations dashboard.
- Added an authenticated internal balance-health endpoint and event-driven balance checks after dispatch/webhook handling.
- Added a Pro-ready production cron template for daily balance polling. The active Hobby deployment remains cron-free because the project already needs more jobs than Hobby permits.
- Added database, application, provider, webhook, configuration, and dashboard tests.
- Applied migration `20260815101207_solapi_delivery_operations_monitoring.sql` to staging Supabase.

## Validation evidence

- Local Supabase reset applied all 99 migrations.
- Existing pgTAP suite: 768 passed.
- New SOLAPI monitoring pgTAP suite: 30 passed.
- Targeted Vitest: 13 passed.
- `corepack pnpm validate:design-system`: passed.
- `corepack pnpm validate:wcj`: passed, 100/100/100 across 179 files.
- `CI=1 corepack pnpm verify`: all lint, typecheck, 557 tests, migration checks, secret/logo/stage/cron gates, design-system, WCJ, and 11 build tasks passed. Turbo printed completion and then remained resident; it was stopped after successful completion output.
- Staging metadata check confirmed both SOLAPI monitoring tables use enabled and forced RLS, and all four RPCs exist as security-definer functions.

## Unresolved risks and external gates

- Preview has the existing SOLAPI API key, API secret, and sender variables present, but `SOLAPI_WEBHOOK_SECRET` is missing.
- SOLAPI Console must register the deployed callback URL `/api/webhooks/solapi`, event `SINGLE-REPORT`, with the same secret as Vercel Preview/Production.
- Current local repository contains unrelated WIP, so do not deploy the entire dirty working tree without isolating or explicitly reviewing the intended paths.
- The authenticated operations dashboard has not yet been visually checked on a deployment.
- No real failed delivery webhook or low-balance condition has been induced; production/provider evidence remains pending.

## Exact next development step

1. Review and isolate only the SOLAPI monitoring implementation paths from the existing dirty WIP.
2. Add `SOLAPI_WEBHOOK_SECRET` to the intended Vercel environment without exposing its value.
3. Deploy a new Preview from the isolated changes.
4. Register the Preview callback in SOLAPI Console with `SINGLE-REPORT` and the matching secret.
5. Send one authorized test SMS, then verify: webhook HTTP success, redacted report row, notification status reconciliation, balance snapshot, and the authenticated operations dashboard.
6. After Preview acceptance, add the same configuration to Production and use the Pro cron template only if the Vercel plan supports all required schedules.

## Security boundary

Do not place phone numbers, OTPs, message bodies, provider credentials, sender values, QR tokens, activation material, or webhook secret values in chat, screenshots, Git, logs, documentation, or browser payloads beyond the provider's required configuration form.

## Session protocol

Follow the session handoff protocol in `AGENTS.md`: each later handoff must record branch and commit, completed scope, validation evidence, unresolved risks, exact next step, and user-owned external setup without credentials.
