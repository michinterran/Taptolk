# Taptolk handoff — 2026-08-15 16:58 KST

## Branch and commits

- Working branch: `codex/solapi-webhook-retry`
- Starting commit: `25fafad`
- Implementation commit: `b329328` (`fix: accept SOLAPI webhook retry secret`)
- The original worktree at `/Users/benjaminsong/Documents/Taptolk` remains on
  `codex/phase-1-foundation` at `8e72ff1`; its existing 57-path WIP was not
  reset, cleaned, stashed, or edited.

## Completed scope

- Diagnosed the repeated SOLAPI delivery-result webhook failure against the
  current Vercel Preview deployment.
- Confirmed the webhook request reached Vercel at the provider retry time and
  failed authentication, rather than failing DNS, routing, or deployment
  availability.
- Identified the rotation boundary: retries already queued by SOLAPI retained
  the signature created with the prior 30-character webhook secret, while the
  application had moved to the current secret.
- Added optional server-only `SOLAPI_WEBHOOK_SECRET_PREVIOUS` support so the
  bounded provider retry window accepts both the current and immediately prior
  secret. Invalid signatures remain rejected.
- Added tests for current-secret acceptance, prior-secret compatibility, and
  invalid-signature rejection.
- Added the previous secret to Vercel Preview as a Sensitive variable without
  printing or persisting its value in Git or this document.
- Deployed implementation commit `b329328` to Preview and moved the stable
  staging alias to the new Ready deployment.

## Deployment evidence

- Stable alias:
  `https://taptolk-staging-michinterrans-projects.vercel.app`
- Deployment:
  `https://taptolk-f481zlsza-michinterrans-projects.vercel.app`
- Vercel deployment ID: `dpl_M8grPQG5akzStJoHV3yud3Yza7MB`
- Preview environment contains both `SOLAPI_WEBHOOK_SECRET` and
  `SOLAPI_WEBHOOK_SECRET_PREVIOUS` as hidden Sensitive variables.
- `/api/health` returned HTTP 200.
- A current-secret signed empty-payload probe reached payload validation and
  returned HTTP 400.
- A prior-secret signed empty-payload probe reached payload validation and
  returned HTTP 400.
- An invalid-signature probe returned HTTP 401.

## Automated validation

- Focused Vitest: 2 files, 32 tests passed.
- `corepack pnpm validate:design-system`: passed; no new drift.
- `corepack pnpm validate:wcj`: passed; total/C/J/W all 100 across 179 files.
- `corepack pnpm verify`: passed, including lint, typecheck, 92 test files,
  558 tests, 99 migration checks, 38 database tests, secret/logo/stage/cron
  checks, design-system validation, WCJ validation, and build.
- No database migration was changed or applied for this fix.

## Unresolved risk and external gate

- The fifth provider attempt occurred at 2026-08-15 16:43:58 KST, before the
  compatibility deployment, and was rejected with the old retry signature.
- Based on SOLAPI's documented retry cadence, the sixth attempt is expected at
  approximately 2026-08-15 20:43:58 KST.
- Local signed probes prove both rotation secrets work, but actual provider
  delivery-result ingestion is not complete until a real retry returns success
  and creates the expected delivery-report data.
- Do not send another SMS merely to test this fix; the queued provider retry is
  sufficient.
- Do not remove `SOLAPI_WEBHOOK_SECRET_PREVIOUS` yet. Remove it only after all
  legacy retries have drained and provider success has been confirmed, then
  redeploy Preview.

## Exact next development step

At or just after 2026-08-15 20:43:58 KST, inspect the stable staging alias logs
for the SOLAPI retry. Confirm `solapi.webhook.accepted` rather than
`solapi.webhook.unauthorized`, then query Supabase read-only to verify that
`solapi_delivery_reports` gained the delivery result and that notification and
webhook-health reconciliation updated. If the provider still rejects the
delivery, capture only the redacted status/error category and do not expose any
secret, phone number, OTP, message body, authorization header, or webhook
signature.

## Session handoff protocol

Create `docs/handoff-MMDD-HHmm.md` when a major feature unit is complete or when
the same error begins repeating and a fresh session would reduce context and
token waste. Every handoff records the current branch and commit, completed
scope, validation evidence, unresolved risks, exact next step, and any required
user-owned external setup. Never include credentials or secret values. Carry
this protocol forward verbatim or by explicit reference in every later handoff
document. After writing the handoff, notify the user that this is a safe
checkpoint and recommend opening a new Codex session with the handoff path.
