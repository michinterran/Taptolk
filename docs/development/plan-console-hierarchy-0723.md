# 콘솔 계층 기획 — 관리회사 › 사이트 › QR (2026-07-23)

이 문서는 **기획 정리본**이다. 착수 전에 사용자 확정이 필요한 항목을 §7에 모았다.
화면 형태는 `DESIGN_SYSTEM.md` + `docs/design-canon/`이 정하고, 이 문서는 **무엇이
어떤 순서로 누구에게 가능한가**만 정한다.

---

## 1. 계층

```
Tenant (플랫폼)
└── 관리회사 (management_companies)
    └── 사이트 (sites)
        └── QR 배치 → QR 자산 → 차량 바인딩
```

**사이트는 관리회사 없이 존재할 수 없다.** 스키마가 이미 그렇다:
`sites.management_company_id`가 notNull이고 `(tenant_id, management_company_id)`
복합 FK다. Taptolk가 직접 관리하는 사이트도 `Taptolk 직영` 관리회사 레코드 아래에 붙는다
(`is_platform_direct` 플래그, 이름으로 판정하지 않는다).

## 2. 등록 순서 (이것이 콘솔의 척추)

| 순서 | 무엇 | 누가 | 현재 상태 |
|---|---|---|---|
| 1 | 관리회사 등록 | SUPER_ADMIN | 화면 있음. **주소검색 없음, 팝업이 좁음** |
| 2 | 사이트 등록 | SUPER_ADMIN + 해당 관리회사의 MANAGEMENT_ADMIN | `site:create` / `site:create-request` 권한은 있음 |
| 3 | QR 제작 | 위 둘이 끝난 뒤에만 | 위자드 있음. **관리회사 선택 단계가 없음** |

**QR 제작 위자드의 첫 단계는 두 단계로 갈라진다:**
`① 관리회사 선택 → ② 사이트 선택 → ③ 디자인 → ④ 수량 → ⑤ 검토·승인 → ⑥ 제작·입고`

지금 위자드는 사이트를 평면 목록으로 한 번에 보여준다. 관리회사가 늘어나면 못 쓴다.
**5단계가 아니라 6단계로 바뀐다.** (§7-A 확정 필요)

## 3. 동적 QR이 담는 것

QR 이미지에 구워지는 것은 **URL 하나**뿐이다. 관리회사·사이트 정보는 URL이 가리키는
레코드에서 나온다. 인쇄 후에는 바꿀 수 없으므로:

- canonical 호스트가 확정되어야 인쇄용 생성이 가능하다 (`PUBLIC_QR_BASE_URL`)
- QR 안에 사이트명·회사명을 **문자열로 넣지 않는다.** 사이트가 개명하면 인쇄물이 거짓이 된다
- 스캔 → 차주 정보 입력 → 서버 기록. 이 경로는 이미 `contact_sessions`로 구현되어 있다

## 4. 권한 — 관리회사별 기능 토글

**요구:** 슈퍼어드민이 관리회사마다 어떤 기능을 켤지 토글로 정한다.
특히 QR 생성 권한을 관리회사 관리자·사이트 관리자에게 줄지 여부.

현재 RBAC는 **역할 고정**이다. `roleHasPermission(role, permission)`이 정적 표를 본다.
관리회사별로 다르게 하려면 계층이 하나 더 필요하다:

```
유효 권한 = 역할 기본 권한 ∩ 그 관리회사에 켜진 기능
```

- 새 테이블 `management_company_features` (관리회사당 기능 플래그 + 감사 기록)
- `roleHasPermission`은 그대로 두고, 그 위에 `isFeatureEnabled(companyId, feature)`를 곱한다
- **토글은 권한을 넓히지 못하고 좁히기만 한다.** 역할이 못 하는 걸 토글로 켜줄 수 없다.
  넓히는 방향을 허용하면 RBAC이 두 곳에서 결정되어 감사 불가능해진다

이건 스키마 + RLS + pgTAP이 붙는 슬라이스다. (§7-B 확정 필요)

## 5. 화면 계층

| 화면 | 범위 | 접근 |
|---|---|---|
| 플랫폼 대시보드 | 전체 | SUPER_ADMIN, PLATFORM_OPERATOR |
| **관리회사 목록** | 전체 | 위 + 자기 회사만 보는 MANAGEMENT_ADMIN |
| **관리회사 상세** | 회사 1개 | 위 |
| **사이트 목록** (회사 안) | 그 회사의 사이트 | 위 + SITE_ADMIN(자기 사이트) |
| **사이트 운영 페이지** | 사이트 1개 | 위 |
| QR 제작 | 회사 › 사이트 | 기능 토글 ∩ 역할 |
| 운영 모니터링 · 리포트 | 현재 스코프 | 스코프에 따름 |
| 계정·권한 | 3계층 (§6) | SUPER_ADMIN 전체 / MANAGEMENT_ADMIN 자기 회사 / SITE_ADMIN 자기 사이트 |

**상단 "플랫폼 운영 콘솔"은 스코프 스위처다.** 전체 → 관리회사 → 사이트로 좁히면
그 아래 모든 화면(모니터링·리포트·계정)이 그 스코프를 따른다. 어드민 계정으로 특정
관리회사를 골라 그 회사 관점으로 운영·모니터링할 수 있어야 한다는 요구가 이것이다.

`ScopeSwitcher` 프리미티브는 이미 `packages/ui`에 있고, 정본 상단바에도 있다.
**현재는 표시만 하고 실제로 스코프를 좁히지 않는다.**

## 6. 계정·권한

세 계층에서 같은 화면 구조를 쓴다:

1. **전체 서비스 계정** — SUPER_ADMIN
2. **관리회사 계정** — 그 회사의 MANAGEMENT_ADMIN + 상위
3. **사이트 계정** — 그 사이트의 SITE_ADMIN + 상위

### 6.1 내 계정 (우측 상단 → 내 정보)

이름 · 연락처 · 소속 관리회사 · 소속 사이트를 보고 고칠 수 있어야 한다.

**✅ 확정 (2026-07-23): 주민등록번호는 넣지 않는다.** 개인정보보호법상 고유식별정보라
법령에 구체적 근거가 없으면 처리 자체가 금지된다(§24조의2). 관리자 계정 식별에는
필요하지 않다 — 이메일과 소속으로 충분하다. 급여·계약 등 법정 목적이 실제로 있다면
그건 별도 시스템의 문제이고, 근거 법령과 보관 기간을 먼저 확정한 뒤에 논의할 사안이다.
사용자가 제외를 확정했다.

## 7. 착수 전 사용자 확정 필요

| 표시 | 항목 | 왜 물어야 하나 |
|---|---|---|
| **A** | QR 위자드를 5단계 → **6단계**(관리회사 선택 추가)로 바꾼다 | 어제 5단계로 확정했다. 관리회사 선택을 넣으면 6단계가 된다 |
| **B** | `management_company_features` 신설 | 스키마 + RLS 슬라이스. §10 "승인 워크플로 신설"과 인접하다 |
| **C** | 고객관리 데이터 **전부 리셋** | 지금 staging에 **E2E 픽스처 테넌트 3개**가 산다. 이걸 지우면 E2E가 깨진다. 데모만 남기려면 픽스처와 데모를 분리하는 방법을 정해야 한다 (§8) |
| ~~D~~ | ~~주민등록번호 제외~~ | **확정됨 — 제외** |
| **E** | 사이트 등록을 MANAGEMENT_ADMIN이 **직접** 하나, **요청 후 승인**인가 | 권한 카탈로그에 `site:create`와 `site:create-request`가 **둘 다** 있다. 어느 쪽인지 정해야 한다 |

## 8. "고객관리 전부 리셋"에 대한 사실 확인

staging에는 지금 이런 게 섞여 있다:

- **E2E 픽스처**: `e2e-*` 테넌트 3개, 사이트 10개, 계정 12개. 테스트가 이걸 쓴다
- **데모 시드**: Taptolk 테넌트 아래 사이트 4개 (`7a9704b1-0d00-…`)
- 실계정 2개 (`s@i.co.kr`, `i@i.co.kr`)

**QR 위자드 현장 목록에 E2E 사이트가 나오는 이유가 이것이다.** 플랫폼 SUPER_ADMIN은
전 테넌트를 보므로 픽스처까지 다 보인다.

두 가지 방법이 있다:

1. **화면에서 거른다** — `e2e-` 테넌트를 콘솔 목록에서 제외. E2E는 그대로 산다.
   빠르고 되돌리기 쉽다
2. **staging에서 지운다** — 픽스처 데이터를 삭제. E2E는 매 실행마다 자기 픽스처를
   만들므로 원칙적으로는 가능하나, 남은 참조가 있으면 삭제가 막힌다

**권장: 1번.** E2E를 깨뜨리지 않고 화면만 깨끗해진다. (§7-C)

## 9. QR 위자드 현재 결함 (기획과 별개로 고칠 것)

스크린샷에서 확인됨:

1. 현장 목록에 E2E 픽스처가 섞여 나온다 → §8
2. 사이트를 골라도 **수량 단계에서 선택이 풀린다** — 수량 단계가 `?site=`를 안 읽는다
3. 브랜드 로고 섹션이 **또 다른 현장 드롭다운**을 띄운다 — 이미 고른 현장을 무시한다
4. 브랜드 로고 폼의 입력 글자가 디자인 시스템보다 크다
5. "Sticker Design Version" 목록은 **디자인 단계에 있을 것이 아니다** — 저장된 디자인
   버전 이력이고, 관리 화면에 속한다
6. "디자인 DRAFT 생성" 버튼 크기·톤이 정본 버튼 계약과 다르다 → §10
7. 선택된 관리 현장의 가독성이 낮다 (초록 노트 한 줄로는 부족)

## 10. 버튼 체계 재정의

정본과 `DESIGN_SYSTEM.md` §3.8이 이미 정한다: 높이 `2.5rem`, 좌우 `1.15rem`,
글자 `0.85rem`/`600`, pill, 변형 `primary`·`secondary`·`ghost`·`danger`,
크기 `default`·`compact`.

**문제는 정의가 아니라 적용이다.** 화면들이 `.tt-button`에 임의 클래스를 덧붙이거나
(`admin-approval-primary-action`, `admin-danger-action`) 폼 제출 버튼을 직접 그린다.

할 일: 콘솔의 모든 버튼을 `Button` 프리미티브 또는 `.tt-button` + 정의된 변형으로
통일하고, 화면별 버튼 클래스를 삭제한다. 어떤 버튼이 primary인지도 규칙으로 정한다 —
**한 패널에 primary는 하나.**

---

## 11. 제안 순서

1. §7 A~E 확정
2. **§9 위자드 결함 수정** + §10 버튼 통일 (확정 없이도 가능)
3. 관리회사 등록 화면 재구성 — 팝업 제거, 정본 상세 폼, 주소검색 연결
4. 관리회사 상세 → 사이트 목록 → 사이트 운영 페이지 계층 구현
5. 스코프 스위처를 실제로 동작시키기
6. `management_company_features` 슬라이스 (§7-B 확정 시)
7. 계정·권한 3계층 + 내 계정
8. 운영 모니터링·리포트를 위 데이터에 연결

---

## 12. 차주 여정과 PWA (2026-07-23 추가)

관리자 콘솔은 스티커를 **만드는** 쪽이고, PWA는 스티커가 **쓰이는** 쪽이다. 콘솔만
만들어서는 서비스가 성립하지 않는다.

### 12.1 전체 여정

```
관리회사 › 사이트에서 QR 스티커 발행(인쇄)
        ↓
차주에게 지급
        ↓
[A] 차주가 자기 폰으로 스캔 → 활성화
      차량번호 · 사이트 인증(아파트/빌딩/빌라) · 전화번호 인증 → 서버 등록
        ↓
[B] 제3자가 같은 스티커를 스캔 → 연락 요청
      상황 선택 또는 직접 입력 → 전송
        ↓
카카오 알림톡으로 링크 + Taptolk 메시지 전달
        ↓
차주가 링크 클릭 → PWA에서 응답
        ↓
호출자에게 응답 도착 → 호출 종료
```

### 12.2 같은 스티커, 두 가지 동작

**같은 URL이 상태에 따라 다르게 동작해야 한다.** 이것이 이 설계의 핵심 분기다.

| QR 자산 상태 | 스캔한 사람 | 보이는 화면 |
|---|---|---|
| 미활성 | 차주(최초) | **활성화 플로우** [A] |
| 활성 | 제3자 | **연락 요청** [B] |
| 활성 | 차주 본인 | 내 스티커 관리 / 수신함 |

스키마에는 이미 `qr_activation_codes`, `qr_bindings`, `contact_sessions`가 있다.
분기 판단은 **서버가 자산 상태로** 한다. 화면이 추측하지 않는다.

### 12.3 PWA 화면 (정본 4장 = `docs/design-canon/pwa/`)

| 순서 | 정본 | 화면 |
|---|---|---|
| B-1 | `01-caller-compose.png` | 차량번호 · "비밀 메세지를 전달합니다" · 상황 3개(가장 많이 쓰는 멘트 강조) · 멘트 더 보기 · 직접 입력 · **메시지 보내기** · 평균 응답 예상 시간 |
| B-2 | `02-caller-sent-waiting.png` | 전달 완료 · 보낸 메시지 · **현재 상태**(차주 메시지 확인 대기중, 확인 평균시간) · 응답 새로 고침 · 호출 내역 보기 |
| B-3 | `03-owner-reply.png` | 받은 메시지 · 응답 3개(바로 이동 / 10분만 / 연락 부탁) · 직접 입력 · **응답 보내기** |
| B-4 | `04-caller-response-received.png` | 차주 응답 · 보낸/받은 메시지 · 호출 시각 · 응답 시각 · **확인했습니다** |

하단 탭: `MESSAGES · ALERT · HISTORY · SETTINGS`.

**⚠️ 정본에 없는 화면 — 만들기 전에 시안이 필요하다:**

- **[A] 차주 활성화 플로우 전체** — 차량번호 입력, 사이트 인증, 전화번호 인증(OTP), 완료
- `ALERT` · `HISTORY` · `SETTINGS` 탭 내용
- 오류 상태 — 만료된 스티커, 폐기된 스티커, 활성화 안 된 스티커를 제3자가 스캔

이 화면들을 §3 패턴 조합으로 만들 수는 있으나, PWA는 콘솔과 시각 언어가 다르므로
**임의로 만들지 않는다.** (§13-F)

### 12.4 정직성 — 이 화면들이 주장하는 수치

정본에 "평균 응답 예상 시간 : 3분", "확인 평균시간 2분"이 있다. **실측 없이는 쓰지 않는다.**
데이터가 쌓이기 전에는 그 줄을 비운다. 목업의 숫자를 그대로 굽는 것은 §4 위반이다.

### 12.5 알림 채널 — 보고서를 읽고 정리 (2026-07-23)

`Taptolk_User_Communication_and_Kakao_Risk_Report_2026.pdf` v1.0 (2026-07-18, 20쪽)을
읽었다. 보고서는 §14~15에서 **카카오를 핵심 경로에 두지 말라**고 결론짓는다.

**보고서의 최종 기술 선택:**

| 방식 | 권고 |
|---|---|
| Taptolk 웹 + **SMS** | **핵심 표준** |
| Taptolk 웹 + Web Push | SMS 보조·비용 절감 |
| **카카오 알림 채널** + Taptolk 웹 | **2차 선택 기능** — 계약·정책 검토 후 |
| 카카오 오픈채팅 중심 | **비권장** |
| 안심번호 통화 중계 | 고도화 |

**사용자 확정 (2026-07-23):** 알림톡은 **링크 + 기본 메시지**("Taptolk에서 연락이
왔습니다" 수준)만 전달한다. 호출자가 쓴 문구는 알림톡 본문에 싣지 않는다.
→ §13-H 해결. 보고서의 "알림 채널로만 선택 적용"과 일치한다.

#### ✅ 확정 (2026-07-23): 알림 채널은 카카오 알림톡. SMS는 쓰지 않는다

보고서는 SMS를 MVP 기본으로 권고했으나, 운영자가 **SMS를 쓰지 않고 알림톡으로 간다**고
확정했다. 오픈채팅이 아니라 알림톡이며, 이는 보고서가 "비권장"한 오픈채팅 의존과는 다르다
— 보고서도 알림톡은 "알림 채널로만 선택 적용"을 열어 두었다.

**이 결정이 만드는 선행 조건과 위험. 설계에 반드시 반영한다:**

| 항목 | 내용 |
|---|---|
| **선행 조건** | 카카오 비즈니스 채널 개설 + **알림톡 템플릿 사전 승인**이 끝나야 차주 알림이 동작한다. 이것 없이는 QR을 인쇄해도 서비스가 성립하지 않는다 |
| **단일 경로 위험** | SMS 대체가 없으므로 차주가 채널을 차단했거나 카카오를 쓰지 않으면 **도달 경로가 0이다** |
| **도달 ≠ 성공** | 발송 성공을 확인 성공으로 표시하지 않는다. 정본 B-2의 "차주 메시지 확인 대기중"이 정확한 표현이다 |
| **본문 제약** | 링크 + 기본 문구만. 호출자가 쓴 문구는 알림톡에 싣지 않고 **링크 안에서** 보여 준다 |
| **착수 금지** | live Kakao Provider 연동은 AGENTS.md §10에 남아 있다. 별도 지시 전까지 실제 발송을 붙이지 않는다 |

**미결 — 도달하지 못한 차주를 어떻게 하나.** 알림톡이 실패했을 때 호출자에게 무엇을
보여 줄지, 관리사무소 에스컬레이션으로 넘길지 정해야 한다. 보고서 §09가 미응답
에스컬레이션을 다루므로 그 경로를 쓰는 것이 자연스럽다. (§13-K)

#### ✅ 확정: PWA 시안 4장을 그대로 적용

`docs/design-canon/pwa/` 4장이 개발 기준이다.

**구현 방식에 대한 제 가정**(다르면 알려 주십시오): 화면은 시안 그대로 만들되,
**설치 없이 모바일 브라우저에서 먼저 동작**하게 만든다. 보고서 §02가 QR 스캔이 설치된
PWA를 반드시 여는 것은 아니라고 밝혔고, 호출자는 무회원·무설치이기 때문이다. PWA 설치는
차주의 재방문과 Push를 위한 향상이며, 설치 시 같은 화면이 뜬다. 시안을 바꾸는 이야기가
아니라 같은 화면이 어디서 돌아가느냐의 문제다.

#### 시안에서 읽은 구현 사양

- 상단 taptolk 로고, 그 아래 흰 카드 스택. 배경은 아주 옅은 회보라
- **차량번호는 브랜드색 대형 수치** — 카드 중앙, 그 아래 한 줄 설명
- 선택지 = 아이콘 칩 + 한 줄. **가장 많이 쓰는 멘트는 브랜드 틴트 + 브랜드 테두리**이고
  위에 `★가장 많이 사용된 멘트` 라벨이 붙는다
- `멘트 더 보기 >` 는 중앙 정렬 텍스트 링크
- 직접 입력은 테두리만 있는 넓은 textarea, placeholder 한 줄
- 하단 primary 버튼 1개. **로고 마크 + 라벨**(메시지 보내기 · 응답 보내기 · 응답 새로 고침)
- **마지막 화면의 `확인했습니다.` 버튼에는 로고 마크가 없다.** 보내는 동작이 아니라
  종료 확인이기 때문으로 읽힌다
- 하단 탭 `MESSAGES · ALERT · HISTORY · SETTINGS`, 활성 탭만 브랜드색
- 시각은 `호출 시간` / `응답 시간` 두 줄, 라벨과 값 사이 넓은 간격

#### 보고서가 정한 제약 (설계에 그대로 반영)

- **목적형 세션.** 무제한 채팅 금지. 최초 요청 → 차주 답장 → 확인 → 추가 1~2회 → 완료
- **응답 링크는 1회용·만료** (30~60분), 128비트 이상 난수 토큰, **해시로 저장**
- **URL에 전화번호·차량번호를 넣지 않는다**
- A와 B의 전화번호 **상호 비공개**. A 번호는 암호화 저장, B 번호는 선택·임시
- 남용 방지: 차량·기기·IP Rate Limit, 금칙어·문맥 필터, 전화번호/SNS ID 교환 패턴 차단,
  세션별 발송 한도(SMS 비용 공격 방어), Idempotency
- **로그에 원문 전화번호 금지** — AGENTS.md의 기존 제약과 같다

이 제약들은 대부분 이미 구현되어 있다(`response_tokens`, `caller_blocks`,
`contact_sessions`, redacted audit). **새로 만드는 것이 아니라 PWA 화면이 이 제약을
지키게 하는 문제다.**

## 13. 추가된 확정 필요 항목

| 표시 | 항목 |
|---|---|
| **F** | [A] 차주 활성화 플로우와 ALERT/HISTORY/SETTINGS 탭의 시안 — 이 4장에는 없다 |
| ~~G~~ | ~~카카오 위험 보고서~~ | **읽음 — §12.5 반영** |
| ~~H~~ | ~~알림톡 본문에 직접 입력 메시지를 담을지~~ | **확정됨 — 링크 + 기본 메시지만** |
| ~~I~~ | ~~기본 알림 채널~~ | **확정됨 — 알림톡. SMS 미사용** |
| ~~J~~ | ~~PWA 시안 적용~~ | **확정됨 — 4장 그대로 적용** |
| **K** | 알림톡이 도달하지 못한 차주를 어떻게 처리하나 — SMS 대체가 없으므로 경로가 0이 된다 |
