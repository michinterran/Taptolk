# Taptolk Session Handoff — 2026-07-28 14:14 KST (Codex)

이 문서 하나만 읽고 이어받는다. **세션 상태 기록이다** — 설계 근거는
`DESIGN_SYSTEM.md` + `docs/design-canon/`, 범위 근거는 `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`와
승인된 `docs/development/` 문서다. handoff 산문을 화면·범위 근거로 인용하지 않는다(`AGENTS.md`).

> 이 문서는 `docs/handoff-0726-1419.md`를 **이어간다**(대체 아님). 0726 문서의 §0 읽기 순서·
> §2 운영모델·§7 착수금지·§8 외부게이트·§10 보고규약은 그대로 유효하다.
> **이번 세션에서 0726 handoff의 §4 WIP 목록과 §5 Step A/B가 상당수 스테일임을 확인했다**(아래 §3).

## 0. 새 세션이 먼저 읽을 순서
`docs/handoff-0726-1419.md` §0과 동일. 이 문서는 8번 자리(세션 상태)에 이 파일을 대신 읽는다.

## 1. 체크포인트 (직접 재확인할 것 — 하드코딩 믿지 말 것)
```bash
git rev-parse --short HEAD                                   # 작성 시점: 48fc974
git rev-list --count origin/codex/phase-1-foundation..HEAD  # 작성 시점: 37 (원격 push 안 함)
git status --short                                           # 작성 시점: clean
```
- 경로 `/Users/benjaminsong/Documents/Taptolk` · 브랜치 `codex/phase-1-foundation`
- 환경 부트스트랩은 0726 handoff §1.5 / `docs/development/local-setup.md`. Node 24.18.0 · pnpm 10.34.5.
- 이번 세션 기준으로 로컬 패키지 dist 빌드됨, `.env.local` 존재, `next dev` 정상 기동 확인.

## 2. 확정된 운영자 결정 (이번 세션, 2026-07-28)
1. **스티커 슬라이스 커밋(`48fc974`)은 단일 커밋 유지.** 재분할하지 않는다.
2. **CONTRACTS C-5/C-6 갱신은 디자인 레인(Claude) 과제로 이관.** 코드(RESUME + 단일 action
   엔드포인트)는 되돌리지 않는다. 정본 문서만 코드에 맞춘다(§4 목록). **Codex는 CONTRACTS를 수정하지 않는다.**
3. Codex는 Step B 정본 대조 audit을 수행 → **완료**(§3).

## 3. 이번 세션에서 한 것

### Step 0 — 스티커 상태 제어 슬라이스 (완료·검증됨)
Owner Settings [C-5]/[C-6]의 SUSPEND/RESUME/RELEASE 실행을 UI→route→service→repo→RPC→pgTAP까지 배선.
커밋 `48fc974 feat(owner): wire sticker settings actions` (10파일, 전부 Codex 레인, tokens.css 미포함):
- DB: `supabase/migrations/20260728120000_owner_settings_sticker_controls.sql`
  (RPC `update_owner_sticker_state(jsonb)` — security definer·`search_path=''`·
   public/anon/authenticated revoke·service_role만 grant·`qr_asset_status_logs`+`audit_logs` 기록·
   전화번호 미노출), `supabase/tests/database/phase_5_owner_activation.sql` (plan 53→59)
- 백엔드: `packages/application/src/owner-activation-service.ts`(+`.test.ts`, `index.ts` export),
  `apps/web/owner/owner-activation-route.ts`, `apps/web/owner/supabase-owner-activation-repository.ts`,
  `apps/web/app/api/owner/settings/sticker/route.ts`
- UI/카피: `apps/web/components/owner-tab-views.tsx`, `apps/web/content/owner-tabs-copy.ts`
  (KO/EN `resume*`·`settingsEmpty*`·`settingsSaved`)
- ⚠️ 이 커밋은 세션 외부 프로세스(리포 git 신원 `michinterran`)가 생성했다 — §5 리스크 참고.

### Step A — 로컬 dev 500 (owner/offline): 코드 변경 없음
`GET /ko/owner/offline`는 클린 빌드에서 **200 정상 서빙**(정본 카피 렌더 확인). `page.tsx`에 JSON 파싱
경로가 없어 0726 §5의 "JSON 처리 버그" 서술은 스테일. 실제 원인은 stale `.next`/미빌드 패키지
환경 아티팩트로, 0726 §1.5 복구 절차(`rm -rf .next` → `build:packages` → dev 재기동)로 해소된다.
→ **없는 버그의 픽스를 만들지 않았다.**

### Step B — CONTRACTS 대조 audit: 신규 구현 불필요 (선행 세션이 이미 처리)
브랜치가 원격보다 37 앞서며, 0726 §5 Step B 목록은 그 커밋들 이전에 작성된 스테일 목록이었다.
- **B-1 activationCode sweep:** 앱/서비스 코드에 `activationCode`/`activation_code` 전무.
  요구사항 제거 완료(`20260724110817_drop_owner_activation_code_requirement.sql`, `ee345ec`). 누출·회귀 없음. **클린.**
- **B-2 재진입 엔드포인트 2개:** `POST /api/owner/session/reclaim/request-otp`·`/verify` 모두 구현.
  zod 스키마·응답(`challengeId`,`expiresInSeconds` / `sessionExpiresAt`+HttpOnly `tt_owner_session`
  쿠키)·전화번호 미노출 모두 CONTRACTS [R]와 일치. **완료.**
- **B-3 스캔 판정:** `scan-entry.ts`/`scan-entry-decision.ts`가 자산 상태(inspect_public_contact /
  inspect_owner_activation) = 스티커 소지·바인딩 기반으로만 판정. **쿠키 존재만으로 [C]를 띄우지
  않는다**(스캔→[C] 경로 자체가 없음, 차주는 [B]→[R]로). CONTRACTS 현재 상태 서술과 일관. **완료.**
- **B-4 PWA 필드 (C-1/C-3/C-4):** 전부 존재·계약 준수.
  - C-1 MESSAGES: `OwnerContactMessageItem`(callerMessage·createdAt·reasonCode·replyAvailable?·
    sessionId·status·vehiclePlateLast4) + listMessages 엔드포인트 ✓
  - C-3 ALERT: Web Push(`OwnerPushSubscriptionState`, push config/state/subscribe/unsubscribe) ✓.
    "알림톡 채널 상태"만 미보유이나 **계약이 부재를 허용**(`상태 확인 불가`)하고 외부 Kakao 게이트 대기(§8).
  - C-4 HISTORY: `OwnerContactHistoryItem`(createdAt·reasonCode·result ANSWERED|UNANSWERED·
    responseSeconds number|null) ✓. 미응답은 null→`—`(`41ba805`에서 처리). **추가 필드 없음.**
- **B-5 "연락 부탁드립니다" reason code:** 이미 구현됨. `apps/web/content/public-contact-copy.ts`
  `CONTACT_REQUEST` KO "연락 부탁드립니다" / EN "Please reply"(`19116ec`). **완료.**

## 4. 디자인 레인(Claude)으로 넘길 CONTRACTS 스테일 마커 — **Codex 수정 금지**
코드가 정본보다 앞서 있어 정본 문서가 코드에 맞게 갱신되어야 한다(운영자 결정 2):
- `docs/design-canon/CONTRACTS.md` **C-5/C-6**: `RESUME`(다시 사용) 동작과 **단일 action 엔드포인트**
  (`POST /api/owner/settings/sticker` + `action`) 반영. 현재 표는 "중지/해지 두 엔드포인트"만 기재.
- **[R] §108** 🔴 "엔드포인트 2개가 없다. Codex가 만들어야 한다" 마커 해제 — 두 엔드포인트 구현 완료.
- **[A] A-2 §71 / A-4** 🔴 "서버가 아직 activationCode를 필수로 요구" 마커 해제 — 요구사항 제거 완료.
- 스캔 진입 §27 "세션-자산 증명 security-definer RPC(신규)"는 **여전히 미래 항목**(정확, 변경 불필요).

## 5. 미결 리스크
- **세션 외부 자동 커밋 프로세스:** 이번 세션 중 Codex가 `git commit`을 실행하지 않았는데도 리포
  git 신원으로 커밋(`48fc974`)이 생성됐다. IDE 자동 커밋/백그라운드 툴로 추정. 레인별 분리 커밋
  규율을 지키려면 **어떤 도구가 자동 커밋하는지 운영자가 식별**하는 것이 좋다(미해결).
- **pgTAP 실DB 미실행:** `db:check`는 정적 검사만 통과. 신규 `update_owner_sticker_state`
  assertion 6건(plan 59)은 Docker/Supabase Local에서 `supabase test db`로 실제 실행해야 검증 완결.

## 6. 자동 검증 (이번 세션 결과)
- `corepack pnpm verify` → **11/11 tasks 성공**(lint·typecheck·test·db:check·verify:secrets·
  verify:logo·verify:service-stage·verify:production-cron·validate:design-system·validate:wcj·build)
- 개별: design-system PASS(신규 drift 0, 기존 298 부채) · wcj PASS(100/100, 155 files) ·
  db:check PASS(73 migration/29 test) · @taptolk/application build PASS · vitest 11 passed(신규 sticker 포함) ·
  @taptolk/web typecheck PASS · dev `GET /ko` `GET /ko/owner/offline` 200

## 7. 수동 검증 대기
- Owner Settings [C-5]/[C-6] 화면 320/768/1280/1920·키보드·스크린리더·대비 눈대조(로그인 뒤 화면 → 스테이징 필요, 스텝 D 이후)
- 신규 스티커 RPC pgTAP 실행(Docker) — §5

## 8. 외부/운영자 게이트
`docs/handoff-0726-1419.md` §8과 동일(스텝 D 스테이징 로그인, 스텝 E 파일럿, 스텝 F BM).
추가: pgTAP 실행용 Docker/Supabase Local.

## 9. Exact Next Step (하나만)
**신규 `update_owner_sticker_state` RPC의 pgTAP를 Docker/Supabase Local에서 실제 실행하여 검증
완결** (`corepack pnpm dev:local` 또는 `supabase db reset && supabase test db`). 통과 후 0726 handoff
§5 "이후 🟣 토큰 이관"(`workorder-codex-token-migration-0725.md`: 스테일 baseline `--update-baseline`
→ 클러스터별 치환)으로 진행.

## 10. 보고 형식 / handoff 규약
`docs/handoff-0726-1419.md` §10 그대로 유지: 구현 완료 / 자동 검증 / 수동 검증 대기 / 외부 게이트 /
다음 한 단계로 구분. credential·전화번호·OTP·QR token·secret은 Git·문서·로그·스크린샷에 남기지 않는다.
이 handoff 규약을 다음 handoff에도 이어간다.
