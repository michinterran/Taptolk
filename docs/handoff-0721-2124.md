# Taptolk Admin Command Center UX Handoff — 2026-07-21 21:24 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Starting commit: `cc7a809`
- Checkpoint commit: this handoff is included in the Admin Command Center UX commit created after
  this document.
- Production deployment: not performed.
- Active Production Cron definitions: `0`; deferred verifier passed.
- Decision: `ADMIN_COMMAND_CENTER_UX_IMPLEMENTED / MANUAL_BROWSER_REVIEW_PENDING`

## Completed scope

- Reworked the authenticated administrator IA around a left-rail command center.
  - `고객사` became `계약 고객`.
  - `사업장` became `관리 현장`.
  - `계정 승인` became `계정·권한`.
  - `QR 제작·재고` became `QR 제작 관리`.
- Kept role boundaries:
  - Super Admin and platform roles see platform portfolio, reporting, revenue readiness, and
    account-permission work.
  - Management-company roles stay in their approved customer workspace with operations, QR, managed
    locations, and reports.
  - Browser metadata still does not decide role, scope, or route authority.
- Replaced duplicate home shortcut cards with a more operational dashboard:
  - portfolio table;
  - priority queue;
  - operations flow bars;
  - provider-delivery quality bars;
  - customer > management company > managed location hierarchy diagram.
- Added `/admin/reports` for scoped operations analytics using the existing server-authorized
  operations aggregate.
- Added `/admin/platform/revenue` for platform revenue-readiness monitoring. It does not invent
  sales amounts; it separates recorded provider cost from future user-approved contract pricing,
  plan, and settlement setup.
- Added an account-permission matrix to `/admin/platform/access` so the page is not only an
  approval queue.
- Upgraded `/admin/qr-inventory` into a QR production wizard:
  - template/sticker preview inspired by the provided concept image;
  - managed-location brand upload moved into the wizard flow;
  - synchronized quantity input with preset dropdown and plus/minus controls;
  - UI cap remains `1-100` per Batch;
  - larger needs such as 400 or 1,000 are explained as consecutive 100-unit production lots.
- Preserved the existing server actions, RBAC checks, PostgreSQL RLS scope boundaries, redacted
  audit behavior, public zero-admin-link contract, immutable logo verification, and QR enum/action
  labels.
- Preserved user-owned untracked documents without staging, overwriting, or deleting them.

## Validation evidence

- `corepack pnpm format`: PASS; 349 files checked.
- `corepack pnpm typecheck`: PASS; 19/19 tasks.
- `corepack pnpm validate:wcj`: PASS; WCJ 100 / C100 / J100 / W100 over 108 files.
- `corepack pnpm test`: PASS; 57 files / 384 tests.
- `corepack pnpm verify`: PASS.
  - lint: 349 files
  - typecheck: 19/19 tasks
  - unit: 57 files / 384 tests
  - DB static contract: 61 migrations / 25 database tests
  - secret scan: 592 text files
  - logo integrity: PASS
  - service-stage guard: PASS
  - Production Cron deferred verifier: PASS
  - WCJ: 100 / C100 / J100 / W100 over 108 files
  - production build: PASS; new dynamic routes include `/[locale]/admin/reports` and
    `/[locale]/admin/platform/revenue`
- `corepack pnpm verify:production-cron:deferred`: PASS; active Production Cron definitions remain
  zero.

## PDCA report

- Plan:
  - Raise the administrator pages from functional lists to a role-specific command center.
  - Keep the current DB, RBAC, RLS, MFA-optional pilot, QR quantity, and provider boundaries intact.
  - Add revenue management as readiness/reporting, not fictional revenue.
- Do:
  - Implemented dashboard, reporting, revenue-readiness, permission-matrix, and QR wizard UI layers.
  - Reused the existing `OperationsDashboardModel` and QR server actions instead of adding new data
    authority.
  - Added KO/EN typed copy and route coverage.
- Check:
  - WCJ initially caught non-semantic table markup, button-type detection, and raw color usage.
    Those were corrected before final validation.
  - `pnpm verify` passed end-to-end.
- Act:
  - Manual browser review should now focus on visual completeness and authenticated role journeys:
    Super Admin dashboard, management-company dashboard, QR production wizard, account-permission
    matrix, reports, and revenue readiness.
  - If accepted, follow with a Preview deployment and authenticated staging smoke for the new
    report/revenue/QR wizard surfaces. Do not deploy Production without separate instruction.

## Unresolved risks and manual gates

- Manual authenticated browser review is pending.
- Preview deployment has not yet been created for this specific commit.
- The QR sticker preview is a UI preview only; final printable template assets and exact design
  specifications still require user-provided approved artwork.
- Live Kakao AlimTalk provider, SMS/CAPTCHA provider, Production Supabase project/region/secret,
  Vercel plan changes, Cron activation, production domain connection, and monitoring/rollback
  ownership remain outside this checkpoint.
- Real revenue invoicing still requires user-approved contract pricing, settlement model, and
  provider reconciliation setup.

## Exact next development step

Commit and push this checkpoint. Then, if the user wants a deployable browser review, create a
Vercel Preview deployment only. Do not deploy Production, connect `taptolk.com`, activate Cron,
start Task 3 access grants/Test Lab/persona work, or configure live Kakao/SMS/CAPTCHA providers
without a separate explicit instruction.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next step,
and required user-owned external setup, without credentials or secret values.
