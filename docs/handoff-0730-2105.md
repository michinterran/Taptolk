# Taptolk handoff — 2026-07-30 21:05 KST

## Git checkpoint

- Branch: `codex/phase-1-foundation`
- HEAD: `4c658ff`
- Ahead of `origin/codex/phase-1-foundation`: 40 commits
- Console Design System v2 baseline: `53c770e`
- Revenue fixture read boundary: `4c658ff`
- The worktree still contains broad pre-existing WIP outside these two explicit commits.
- Never use `git add -A` or `git add .`; stage only lane-owned paths.

## Completed

- Promoted `docs/design-canon/CONSOLE_DESIGN_SYSTEM_V2.md` to the binding console baseline.
- Fixed authority drift between `DESIGN_SYSTEM.md` and the current v2 table/type tokens.
- Bound the shared shell, search, locale, theme, soft-navigation, panel, table, progress, and data-hygiene rules.
- Defined `전체 플랫폼` as the current authorization-scope home link, not a filter or primary action.
- Added the route migration matrix and kept unreviewed surfaces at `수동 QA 대기` or `확장 대기`.
- Added a new migration that excludes fixture management companies and sites from the revenue read model
  before list and metric aggregation.
- Removed display-name inference from the revenue application mapper.

## Validation evidence

- `corepack pnpm validate:design-system` — PASS, 0 recorded violations.
- `corepack pnpm validate:wcj` — PASS, Total 100 / C 100 / J 100 / W 100.
- `corepack pnpm db:reset:local` — PASS, all 80 migrations applied.
- `corepack pnpm exec supabase test db` — PASS, Files 29 / Tests 676.
- `corepack pnpm verify` — PASS, including lint, typecheck, 69 files / 450 tests,
  DB check, secret scan, logo, service-stage, deferred Production Cron contract, design/WCJ, and build.
- Local routes returned HTTP 200:
  - `/ko/admin/platform`
  - `/ko/admin/platform/revenue`
  - `/ko/admin/qr-inventory`

## Manual QA pending

- Compare the dark and light console against the supplied YouTube Studio references at the same viewport.
- Confirm the brand companion text, scope home, search button/Enter, KO/ENG spacing, and theme persistence.
- Confirm internal navigation, QR selection review, quantity, and pagination do not reload the full shell.
- Complete the actual QR generation/progress/SVG download journey.
- Confirm revenue has no nested panel, partial table-header fill, stretched Won icon, or fixture company labels.

## External gates

- Do not start live Kakao provider work, Production Supabase, Production Cron activation, or Vercel
  production settings without external approval.

## Exact next step

Apply the v2 baseline to the same QR operations read model in this order:

1. `/ko/admin/operations`
2. `/ko/admin/reports`
3. `/ko/admin/platform/management-companies/[companyId]`
4. `/ko/admin/sites/[siteId]`

For each slice, preserve the existing service/RBAC/read-model behavior, use the shared v2 shell and
primitives, run `corepack pnpm validate:design-system` and `corepack pnpm validate:wcj`, and keep
manual dark/light/browser journey acceptance separate from automated gates.

## Session handoff protocol

Create `docs/handoff-MMDD-HHmm.md` when a major feature unit is complete or when the same error begins
repeating and a fresh session would reduce context and token waste. Record branch and commit, completed
scope, validation evidence, unresolved risks, exact next step, and user-owned external setup. Never
include credentials or secret values. After writing the handoff, notify the operator that it is a safe
checkpoint and recommend continuing from that handoff path.
