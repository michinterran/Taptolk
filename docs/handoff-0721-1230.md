# Taptolk Kakao AlimTalk Ephemeral Contact Handoff — 2026-07-21 12:30 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Feature base commit: `e34bd41`
- Feature implementation commit: the commit containing this handoff
- Task 2 implementation commit: `bf510c3`
- Production-deployed source commit: `f022ca0`
- Production deployment: `dpl_DTvj4RjQ68MnVYAXQKR6KexiEEM7` READY
- Stable URL: `https://taptolk.vercel.app`
- Active Production Cron definitions: `0`
- Decision: `ALIMTALK_EPHEMERAL_CONTACT_AUTOMATED_COMPLETE / LIVE_PROVIDER_AND_MANUAL_GATES_PENDING`

## Completed Scope

- Replaced SMS-specific owner-notification types and configuration with a provider-neutral
  `OwnerNotificationProvider` and `OWNER_CONTACT_REQUEST_V1` payload.
- Added the `KAKAO_ALIMTALK` database channel while preserving legacy enum history; all new Owner
  contact intents are normalized to that channel and workers claim only that channel.
- Removed caller free text and plate data from the provider payload. Only bounded reason code and a
  one-time Taptolk URL cross the provider boundary.
- Added caller completion UI, route, service, repository, and service-role-only RPC.
- On resolution or expiry, atomically redact message body/hash, revoke response tokens, cancel
  undelivered notifications, mark participants left, and preserve terminal/audit/aggregate history.
- Clear both caller cookies after successful completion.
- Separated Owner notification configuration from Owner phone-verification configuration; both
  remain Production fail-closed without approved external providers.
- Updated KO/EN typed copy, master specification, environment example, design amendments, PDCA
  documents, authenticated staging E2E, and pilot checklist.
- Did not start Task 3 access grants, Test Lab, or persona work.

## Validation Evidence

- `pnpm validate:wcj` immediately after the web component change: PASS, 100 / C100 / J100 / W100,
  93 files.
- Clean local Supabase reset: all 59 migrations applied.
- Local pgTAP: 24 files / 604 tests PASS.
- Staging migrations `20260721040000` and `20260721041000` applied to the linked staging project.
- Linked pgTAP: 24 files / 604 tests PASS; new feature contract 14/14 PASS.
- Authenticated staging public-contact lifecycle: 5/5 PASS, including AlimTalk intent, retry/lease
  recovery, Owner reply, caller resolve, cookie removal, content redaction, token revocation,
  participant close, audit, expiry cleanup, and residue cleanup.
- Final `pnpm verify`: lint 331; typecheck 19/19; unit 54 files/336 tests; DB 59 migrations/24
  tests; secret scan 557; logo, Service-stage, deferred Cron, WCJ, and Production build PASS.

## Unresolved Risks and Gates

1. No live Kakao AlimTalk send exists yet. The official dealer, business channel, approved
   informational template text/ID, credentials, receipt/cost mapping, and failure rehearsal remain
   user-owned external gates.
2. Owner phone ownership verification is a separate external gate; an informational AlimTalk
   template is not verification.
3. Manual keyboard, VoiceOver/TalkBack, computed contrast, and representative iOS/Android review
   remain pending.
4. Production Supabase project/region, CAPTCHA provider, Production secrets, Vercel plan, Cron
   activation, and monitoring/rollback/incident owners remain undecided.
5. Production Cron active definitions must remain `0` until separately approved.
6. The unrelated untracked user file `docs/development/prd-public-surface-0721.md` was preserved and
   excluded from this feature commit.

## Exact Next Development Step

Do not implement the live provider or start Task 3 without a new explicit user instruction.

The next safe step is manual review of the completed A–B journey and preparation of the future
AlimTalk integration intake. When the contract is available, obtain only the approved values and
documents for:

1. official dealer and business-channel identity;
2. approved KO/EN `OWNER_CONTACT_REQUEST_V1` informational text and template ID;
3. provider-specific credential field names and secret-manager ownership;
4. receipt, cost, rate-limit, retry, and failure-rehearsal contracts;
5. a separately approved Owner phone-verification provider.

Then implement one live adapter behind the existing typed boundary, run linked pgTAP and the
authenticated staging lifecycle with provider sandbox evidence, and keep Production fail-closed
until manual approval.

## User-Owned External Setup

- The informational message template can and should be supplied at Kakao AlimTalk contract/channel
  connection time. No template ID or provider credential is needed for the current automated
  implementation checkpoint.
- No Production setting, plan, Cron, secret, or operations owner was changed.

## Continuation Prompt

```text
AGENTS.md와 docs/handoff-0721-1230.md를 읽고 Exact next development step부터 진행해.
Kakao AlimTalk ephemeral contact 구현은 자동 검증 완료 상태다. live provider와 최종 정보성
템플릿은 공식 계약·채널·템플릿 승인 자료가 제공되기 전에는 구현하거나 설정하지 마.
Task 3 access grant/Test Lab/persona도 별도 승인 전에는 시작하지 마. Production Supabase/리전,
CAPTCHA provider, Production secret, Vercel plan, Cron, 운영 담당자를 임의로 선택하지 마.
```

## Session Handoff Protocol

Create `docs/handoff-MMDD-HHmm.md` when a major feature unit is complete or when the same error
begins repeating and a fresh session would reduce context and token waste. Every handoff records
the current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and any required user-owned external setup. Never include credentials or secret values.
After writing the handoff, notify the user that this is a safe checkpoint and recommend opening a
new Codex session with the handoff path.
