# Taptolk Phase 0-9 Automated Development Closeout Handoff — 2026-07-20 07:17 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Closeout commit: `0e2a15e` (`fix: close phase 0-9 automated development gates`)
- GitHub quality run: `29705730777` PASS
- Production web deployment: `dpl_GY6MvpfwEgCadGGuexntBdK9VvNP` READY
- Stable web URL: `https://taptolk.vercel.app`
- Root Directory: `apps/web`
- Active Production Cron definitions: `0`
- Authority: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Closeout report: `docs/04-report/phase-0-9-development-closeout.report.md`
- Pilot checklist: `docs/deployment/pilot-readiness-checklist.md`
- Decision: `AUTOMATED_DEVELOPMENT_READY / EXTERNAL_PILOT_GATES_PENDING`

## Completed scope

- Closed all 14 tracked PDCA features with analysis and completion reports.
- Preserved the approved `1..100` per-Batch contract. The 1,000-item proof remains ten approved
  100-item Batches; no single 1,000-item Batch contract was added.
- Fixed Linux Chromium login/signup horizontal overflow at 320 CSS pixels while keeping short
  control labels non-wrapping.
- Re-ran all 22 linked pgTAP files and authenticated linked-staging E2E.
- Passed complete repository verification and local Chromium/mobile browser smoke.
- Passed GitHub Actions in the actual Linux Chromium environment.
- Deployed the verified closeout commit to the existing authorized `taptolk` Vercel project.
- Confirmed `apps/web`, Node 24.x, READY deployment, stable KO/EN/health 200, cleanup 503, and
  zero active Cron definitions.
- Did not select providers, create or mutate a Production database project, activate Cron, or
  configure a secret value.

## Validation evidence

- Linked pgTAP: all 22 database test files PASS.
- Authenticated linked-staging E2E: 28 PASS and one intentional opt-in 10x100 skip.
- Local browser smoke: 30/30 PASS across Chromium and mobile profiles.
- Lint: 320 files PASS.
- Typecheck: 19/19 PASS.
- Unit: 53 files and 326 tests PASS.
- DB static contract: 55 migrations and 22 database tests PASS.
- Secret scan: PASS after the final handoff.
- WCJ: 100 / C100 / J100 / W100 over 87 files.
- Immutable logo, deferred Cron verifier, and Production build: PASS.
- GitHub Actions run `29705730777`: PASS including Linux Chromium browser smoke.
- Vercel deployment `dpl_GY6MvpfwEgCadGGuexntBdK9VvNP`: READY.
- Stable route evidence: `/ko` 200, `/en` 200, `/api/health` 200, and unconfigured
  `/api/internal/privacy-cleanup` 503.
- Live project evidence: Root Directory `apps/web`, Node 24.x, active Cron definitions 0.

## Unresolved risks

1. A clean local Supabase reset remains unexecuted because no Docker, Podman, Colima, OrbStack,
   or Lima runtime is installed and running. Linked staging evidence does not replace this gate.
2. A separate approved Production Supabase project and region do not exist in the accessible
   project inventory.
3. Production SMS and CAPTCHA providers and their adapters remain undecided and unimplemented.
4. Representative iOS/Android, VoiceOver/TalkBack, computed contrast, keyboard, and physical
   85mm print checks remain manual.
5. Production monitoring, alerting, rollback, incident ownership, domain, and pilot window remain
   undecided.
6. The actual-service Cron run, same-hour replay, duplicate-zero query, monitoring, secret
   rotation, and rollback rehearsal are intentionally deferred.
7. GitHub Actions reports a non-blocking warning that `actions/checkout@v4` and
   `actions/setup-node@v4` still target the deprecated Node 20 action runtime while the job is
   forced onto Node 24.

## Exact next step

Do not add product scope and do not change the QR quantity policy. Complete the remaining gates in
this order:

1. obtain approval for one Docker-compatible local runtime, install/start it, then run a clean
   local Supabase reset and the complete pgTAP suite;
2. approve a separate Production Supabase project and data region, then grant access and apply
   verified migrations;
3. select Production SMS and CAPTCHA providers and authorize adapter implementation before
   configuring secrets;
4. provide representative devices, assistive-technology operators, and the target 85mm printer;
5. assign Production monitoring, rollback, incident, domain, and pilot-window owners;
6. at actual service launch only, move the Vercel project to an hourly-Cron-capable plan, activate
   the reviewed template, and follow
   `docs/deployment/production-privacy-cleanup-cron-runbook.md`;
7. verify one authorized aggregate-only run, same-hour replay, cleanup/audit duplicate 0,
   monitoring, secret rotation, and rollback rehearsal;
8. mark the pilot `READY` only after every open checklist item is signed off.

If a quantity above 100 per Batch is requested, stop and obtain separate quantity-policy design
approval before changing schema, UI, Worker, Queue, or export contracts.

## User-owned external setup

- Choose and approve one Docker-compatible local runtime. Do not send credentials.
- Name the approved separate Production Supabase project and region, then grant the required
  project access through the provider's own access control.
- Name the approved Production SMS and CAPTCHA providers; do not paste tokens or secret values
  into chat.
- Provide representative iOS/Android devices, VoiceOver/TalkBack operators, and the target 85mm
  printer.
- Name the accountable Production monitoring, rollback, incident, domain, and pilot-window
  owners.
- At actual service launch, approve the Vercel plan change and active Cron release separately.
- Never place database credentials, provider tokens, Cron bearers, phone numbers, messages, OTPs,
  public QR tokens, activation tokens, cookies, authorization headers, or secret values in Git,
  documentation, logs, screenshots, or chat.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and required user-owned external setup, without credentials or secret values.
