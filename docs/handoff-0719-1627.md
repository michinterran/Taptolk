# Taptolk handoff — 2026-07-19 16:27 KST

## Current branch and commit

- Branch: `codex/phase-1-foundation`
- Commit: `7ebc28dbee77cc162235c057412aba042a91f119`
- Commit subject: `test: cover QR dispatch failure pipeline`
- GitHub push/deploy: not performed
- Five unrelated duplicate migration files are untracked and intentionally preserved; see risks.

## Current phase

- Official development remains **Phase 1** because Docker-local reset and runtime pgTAP are open.
- Phase 3 now has a complete provider-neutral delivery foundation through the Queue publisher port.
- No real Queue SDK/API call, credential use, Cron/HTTP route, resident Worker consumer, QR
  issuance, renderer, Asset/token creation, or Production runtime was added.

## Completed scope in this continuation

### Application dispatch coordinator

- Added `QrGenerationDispatchCoordinator`.
- Added the shared versioned Queue DTO and provider publisher port.
- Connected bounded claims to publication acknowledgement and delivery-failure acknowledgement.
- Added safe outcomes for:
  - publication success;
  - provider failure with retry scheduled;
  - publication success followed by DB acknowledgement failure;
  - provider failure followed by DB failure acknowledgement failure.
- Unknown provider errors reduce to `QUEUE_UNAVAILABLE`; raw provider payloads are not propagated.
- Publish-success/ack-failure never writes a false provider failure and leaves the lease
  recoverable for stable-job republish.

### Delivery retry policy

- Added validated exponential delivery backoff with injected clock and random source.
- Base delay, maximum delay, and jitter ratio are explicit configuration, not Route/UI/provider
  constants.
- Added a 24-hour absolute ceiling, overflow-safe exponent cap, deterministic jitter tests, and
  validation for attempts/config/clock/random values.

### DB RPC repository adapter

- Added the `@taptolk/db` server-only adapter for the three reviewed dispatcher RPCs.
- The adapter receives an injected RPC client and does not own credentials.
- RPC parameters and responses are mapped through explicit allowlists.
- Malformed response rows and wrong transition results fail closed.
- PostgreSQL errors reduce to `CONFLICT`, `FORBIDDEN`, `VALIDATION`, or `UNAVAILABLE` without raw
  provider/database details.

### Cross-layer failure injection

- Added a provider-neutral pipeline test using the real Application service, coordinator, retry
  policy, and DB repository adapter.
- Proved:
  - claim → strict Queue DTO → publish → `QUEUED` acknowledgement;
  - provider rate limit → redacted exponential `RETRY_WAIT`;
  - publish success → acknowledgement persistence failure → no false failure write.

## Commits created

- `c1de690` — `feat: coordinate QR generation dispatch outcomes`
- `503f6f1` — `feat: add QR delivery retry policy`
- `450e1ea` — `feat: adapt QR dispatcher database RPCs`
- `7ebc28d` — `test: cover QR dispatch failure pipeline`

## Validation evidence

- `pnpm verify`: PASS
  - Biome: 186 files
  - Typecheck: 10 packages / 16 tasks
  - Vitest: 25 files / 170 tests
  - DB static check: 27 filesystem migrations / 12 database tests
  - Secret scan: 291 text files
  - Immutable logo SHA-256: PASS
  - WCJ: 100 total, C 100, J 100, W 100, 49 files
  - Application, DB, Worker, and Web production builds: PASS
- Focused coordinator/Worker contract: 24 passed.
- Focused retry policy/coordinator: 19 passed.
- Focused DB RPC adapter: 14 passed.
- Cross-layer dispatch pipeline: 3 passed.

## Unresolved risks and open gates

- Docker is still unavailable. Local Supabase reset and runtime pgTAP have not run; Phase 1 is not
  complete.
- Five untracked migration files appeared at the start of this continuation:
  - `20260718151819_phase_1_admin_account_approval.sql`
  - `20260718155102_allow_super_admin_profile_review.sql`
  - `20260719005929_phase_1_management_company_management.sql`
  - `20260719010000_phase_1_tenant_management.sql`
  - `20260719011000_phase_1_admin_profile_rls_performance.sql`
- Each is byte-identical to an already tracked migration with a different timestamp. They were not
  edited, staged, committed, or deleted. They raise the static migration count from 22 to 27 and
  must be resolved before any further `supabase db push` or migration-history claim.
- Real Supabase Queue publication and dispatcher runtime composition are outside the completed
  provider-neutral unit and require a fresh reviewed Plan/Design boundary.
- Production Worker host selection and benchmark gates remain open.
- Manual WCJ keyboard, screen-reader, computed contrast, responsive, and real-device checks remain.

## Exact next development step

1. Resolve ownership of the five byte-identical untracked migration duplicates before running any
   Supabase CLI mutation. Do not silently stage or delete them.
2. Recheck Docker. If available, run local reset and all pgTAP tests against the reviewed 22-file
   migration history.
3. Create a separate Plan/Design unit for a staging-only Supabase Queue publisher and bounded
   dispatcher runtime composition:
   - server-only credential boundary;
   - fixed claim limit, lease, duration budget, and retry config;
   - stable v1 Queue DTO;
   - publish-success/ack-failure duplicate safety;
   - no resident generation work inside the dispatcher.
4. Do not implement QR issuance/rendering, public tokens, activation codes, storage outputs, or
   Production host manifests in that publisher unit.

## Required user-owned external setup or decision

- Confirm whether the five untracked byte-identical migration files may be removed as duplicate
  artifacts or must be retained for another active task.
- Install/start Docker Desktop to close the local reset/runtime pgTAP gate.
- A real Queue integration unit will later require staging server credentials through the secure
  environment only; never place credentials in chat, Git, docs, logs, or browser code.

## Session handoff protocol

Continue to follow the repository `AGENTS.md` Session handoff protocol: create
`docs/handoff-MMDD-HHmm.md` after a major unit or repeating error, record branch/commit/scope/
evidence/risks/exact next/user setup, and never include credentials or secret values.
