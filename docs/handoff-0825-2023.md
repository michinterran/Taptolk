# Taptolk Handoff — 2026-08-25 20:23 KST

## Current state

- Repository: `/Users/benjaminsong/Documents/Taptolk`
- Branch: `codex/phase-1-foundation`
- HEAD before uncommitted work: `1c1540c`
- Worktree contains pre-existing user WIP. Do not reset, clean, stash, or broadly stage it.
- No commit or deployment was made for this checkpoint.

## Completed this session

P0-1 QR generation approval bypass was closed in the current worktree:

- `/admin/qr-inventory` now enters the existing design → sample → independent final Super Admin approval workflow.
- The former direct operations screen is preserved at `/admin/qr-inventory/operations` for read/progress monitoring.
- The legacy direct-generation application service now fails with `APPROVAL_REQUIRED`.
- The legacy direct-generation Supabase RPC is no longer executable by authenticated browser sessions.
- Direct-generation action tests now assert the approval-required redirect.
- Existing SOLAPI SMS provider boundary remains unchanged; future Kakao AlimTalk remains disabled/fail-closed.

## Verification evidence

- `pnpm verify` passed.
- 96 test files / 575 tests passed.
- Typecheck passed across 20 tasks.
- DB check: 100 migrations / 38 database tests registered.
- Design-system validation passed.
- WCJ passed: 100 / C100 / J100 / W100.
- Production build passed.

## Pending P0 gates

1. Preview manual role test: requester submits QR batch; independent Super Admin approves; self-approval is rejected.
2. Preview SOLAPI SMS journey using an approved test number: contact request → SMS receipt → response link → owner reply → caller receipt.
3. SOLAPI delivery Webhook duplicate and failure/retry test.
4. Confirm Production Cron/worker activation and monitoring ownership.
5. Resolve the activation-code policy mismatch between the current OTP implementation and stale master/design documentation.

Do not send real SMS without explicit test-number approval. Do not record phone numbers, OTPs, public tokens, response tokens, or secrets in notes or screenshots.

## Exact next development step

Start with P0 manual verification on the newest Preview. After the P0 external gates and activation-code policy are closed, begin P1 in this order:

1. Unify QR generation → print → shipping → delivery → partial/full receipt into one batch timeline.
2. Add delivery/receipt evidence and next-owner CTAs.
3. Convert QR generation, delivery, receipt, assignment, replacement, and revoke mutations to the shared async action/pending/error pattern.
4. Add explicit owner consent checkbox and policy links.
5. Add visible generation failure retry/recovery states.

## Safe continuation prompt

Continue in `/Users/benjaminsong/Documents/Taptolk`. Read `docs/handoff-0825-2023.md` first. Preserve all dirty WIP. Begin with P0 Preview manual verification, not P1 code changes. Verify QR requester/Super Admin approval separation, SOLAPI SMS end-to-end delivery/Webhook/retry, Production Cron status, and activation-code policy before starting P1. Keep SOLAPI SMS as the current channel and preserve the provider boundary for a future Kakao AlimTalk adapter.
