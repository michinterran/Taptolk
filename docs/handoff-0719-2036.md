# Taptolk Phase 4 Authenticated Staging Handoff — 2026-07-19 20:36 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Checkpoint commit: `58b9da4` (`test: prove phase 4 staging inventory journey`)
- Authority: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Detailed audit: `docs/architecture/phase-2-4-gap-audit.md`
- Implementation report: `docs/04-report/phase-2-4-implementation.report.md`

## Completed scope

- Added a service-only staging delivery command that advances the generated Batch through
  print-file-ready → printer → printed → shipped → delivered while preserving status history
  and redacted audit.
- Added a constrained service-only E2E cleanup command. It accepts only bounded E2E tenants
  and actors, preserves production history guards outside the cleanup transaction, and leaves
  no QR, customer, admin, or Auth fixture residue.
- Added a secretless 20-item generated/printed Batch fixture. QR and activation secrets exist
  only in process memory for command submission and are neither persisted in test artifacts nor
  logged.
- Proved authenticated delivery → full receipt (`PRINTED` → `IN_STOCK`) → manual assignment →
  duplicate QR/vehicle rejection → CSV validate/atomic commit → Super Admin replacement/final
  revocation through the real KO/EN UI.
- Proved ended Bindings, status history, inventory transactions, actor order, and redacted audit
  remain durable after replacement and revocation.
- Corrected generation Job and Batch guards so same-state `PROCESSING`/`GENERATING` progress
  updates are allowed only for bounded counters while execution and approval metadata remain
  immutable and counters cannot regress.
- Added a linked pgTAP runner that captures every TAP line through the linked database CLI and
  fails on any `not ok`, plan mismatch, or assertion-count mismatch.
- Applied migrations through `20260719111544` to `taptolk-staging`; all 36 local and remote
  migration versions are aligned.

## Validation evidence

- `pnpm verify`: PASS
- Vitest: 43 files / 270 tests PASS
- DB static contract: 36 migrations / 15 database tests PASS
- Linked staging pgTAP: 15 files PASS, including 32/32 execution-guard and 14/14
  delivery/cleanup assertions
- Authenticated staging Playwright: 19 tests PASS
- Phase 4 serial journey: 15 tests PASS
- Duplicate QR and active vehicle Binding rejection: PASS
- Final approval/cancellation race: one commit, one conflict PASS
- Staging cleanup: QR, customer, admin, and Auth residue zero PASS
- WCJ: 100 / C100 / J100 / W100, 54 files
- Secret scan: 369 text files PASS
- Immutable logo SHA-256: PASS
- Production build: PASS
- Migration history: local/remote aligned through `20260719111544`
- Supabase advisor recheck: no new exposure from the delivery/cleanup service functions;
  existing intentional authenticated command/read-model warnings and hardening backlog remain.

## Unresolved risks

1. Docker remains unavailable, so local reset has not run. The same migration chain passed the
   complete linked staging runtime pgTAP suite.
2. The actual Worker 1,000-item generation → export → Queue consume → interruption/resume
   journey has not run end to end on staging.
3. Actual 85mm printing, real-device QR decode, keyboard, screen-reader, computed contrast, and
   responsive manual QA remain.
4. Supabase advisors retain intentional authenticated `SECURITY DEFINER` warnings, three
   server-only RLS/no-policy INFO notices, unindexed foreign-key/permissive-policy performance
   notices, and disabled leaked-password protection.

## Exact next development step

Run the actual 1,000-item Worker/Queue/export staging acceptance without weakening the current
1–100 per-Batch slice contract:

1. provision ten independently approved 100-item Batches with deterministic acceptance labels;
2. dispatch each durable generation Job through the actual Worker and `qr-generation` Queue;
3. interrupt at least one Job after a committed chunk, let its lease expire, and resume it
   without duplicate ordinals, tokens, QR assets, or activation rows;
4. complete all 1,000 generated QR items and verify duplicate count 0 and QR decode 100%;
5. commit PDF/CSV/ZIP/manifest exports and verify byte size/checksum/storage ledger for every
   Batch;
6. prove Queue read → visibility retry → archive behavior and zero active poison messages;
7. delete only the acceptance fixtures through an explicitly bounded cleanup path and verify
   zero tenant/Auth/Queue residue;
8. finish with complete linked pgTAP, authenticated staging E2E, and `pnpm verify`.

If a single 1,000-item Batch is required instead of ten 100-item Batches, approve a new
quantity-policy design first because the current Phase 2–4 slice deliberately constrains one
Batch to 1–100.

## User-owned external setup

- Keep the staging Worker runtime available for the bounded 1,000-item acceptance window.
- Enable Supabase leaked-password protection when the project plan permits it.
- Provide access to an 85mm printer and representative iOS/Android devices for physical QA.
- Do not share database passwords, service-role credentials, encryption keys, Queue secrets,
  or generated QR/activation secrets in chat, Git, logs, screenshots, or documents.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record
the current branch and commit, completed scope, validation evidence, unresolved risks, exact
next step, and required user-owned external setup, without credentials or secret values.
