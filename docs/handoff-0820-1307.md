# Handoff 2026-08-20 13:07 KST

## Branch and commit

- Worktree: `/private/tmp/taptolk-qr-active-cleanup.A8LXwi/worktree`
- Branch: `codex/qr-operations-active-cleanup`
- Code commit: `98c2936 feat: clarify QR site operations workflow`
- Baseline: `origin/codex/solapi-webhook-retry`
- Ahead/behind after code commit: `0 9`

## Completed scope

- Reworked the site QR operations page around four operator work areas:
  - `Production and delivery`
  - `Receipt and inventory`
  - `Owner registration status`
  - `Exception requests`
- Clarified the default QR use path:
  - Admin confirms production, delivery, and site receipt only.
  - After receipt, the owner scans the QR and completes vehicle registration on mobile.
  - Admin vehicle entry and CSV import are now framed as advanced preassignment, not the default flow.
- Replaced confusing unavailable dash blocks in production rows with readable unavailable/completed labels.
- Fixed the production confirmation dialog so it opens centered in the viewport instead of the top-left corner.
- Added receipt UI detail for selected delivered batches:
  - expected quantity
  - current batch status
  - full receipt action
  - partial receipt quantity action using the existing quantity receipt server action
- Added exception workflow copy explaining lost, damaged, replacement, and revocation handling without exposing public QR tokens or owner contact details.

## Validation evidence

- `corepack pnpm validate:design-system`
  - PASS: no new design-system drift.
- `corepack pnpm validate:wcj`
  - PASS: `Total 100 · C 100 · J 100 · W 100 · 185 files`.
- `corepack pnpm verify`
  - PASS:
    - lint
    - typecheck
    - `94` test files / `567` tests
    - `db:check` with `99` migrations and `38` database tests
    - secrets/logo/service-stage/production-cron checks
    - design-system and WCJ checks
    - Next build
- Authenticated local Chrome QA on `http://127.0.0.1:3105`:
  - `/ko/admin/qr-inventory/sites/2486036d-ca22-4354-a720-335e09648e2a?view=assignment`
    - `Owner registration status`, vehicle preassignment, and CSV preassignment labels rendered.
  - `/ko/admin/qr-inventory/sites/2486036d-ca22-4354-a720-335e09648e2a?view=inventory`
    - selected receipt panel rendered expected quantity, current receipt quantity, full receipt, and partial receipt controls.
  - `/ko/admin/qr-inventory/sites/2486036d-ca22-4354-a720-335e09648e2a?view=production`
    - production confirmation dialog opened centered; cancel was used only.
  - `/ko/admin/qr-inventory/sites/2486036d-ca22-4354-a720-335e09648e2a?view=exceptions`
    - exception workflow note rendered.

## Important non-actions

- Original dirty WIP worktree `/Users/benjaminsong/Documents/Taptolk` was not reset, cleaned, stashed, reverted, or overwritten.
- No `git add -A` or `git add .` was used.
- No production setting, deployment, database migration, SOLAPI secret, or real SMS was changed.
- No QR generation, receipt, vehicle assignment, replacement, revocation, or other operational form was submitted during browser QA.

## Unresolved risks and limits

- The current receipt read model exposes requested quantity and batch status, but not a per-batch received/remaining quantity. The partial receipt form relies on the existing server-side quantity validation rather than showing a computed remaining quantity in the browser.
- Local authenticated QA confirms the rendered localhost UI only. Staging and real-device QR scan acceptance remain separate gates.
- The existing QR lifecycle proof still requires the operator to confirm the physical/logical QR reaches an owner-scannable state and then scan it on a real mobile device.

## Exact next development step

Use the authenticated local or staging admin session to manually verify one real end-to-end QR activation journey without creating duplicate QR:

1. Open the QR site operations page.
2. Confirm an existing delivered test batch is received into stock.
3. On a real phone, scan the corresponding QR after it is in the owner-registration-ready state.
4. Confirm the owner mobile registration flow starts, accepts vehicle registration, and changes the QR from owner-registration-ready to usable.
5. If the scan still shows an unusable-sticker screen, inspect the QR asset lifecycle state and public scan lookup path before changing production or resending any SMS.

## Required user-owned external setup

- User must provide the real-device scan result or authenticated staging screenshot for the QR activation journey.
- Production deploy and production database changes require explicit approval before any action.
