# Taptolk handoff — 2026-07-19 14:26 KST

## Current branch and commit

- Branch: `codex/phase-1-foundation`
- Commit: `8e7fcb81ad9bd978c93ab90df05c4f9f2a2415b1`
- Commit subject: `feat: add QR final generation approval flow`
- GitHub push/deploy: not performed
- Worktree at handoff creation: clean

## Completed scope

### Application

- Added the typed `QrFinalGenerationApprovalService` contract and unit coverage.
- Commands authorize membership before repository access, reload an authoritative RLS-scoped
  Batch snapshot, and independently enforce scope, version, active parents, approved Design and
  Sample, requester ownership, maker-checker, and no-existing-job rules.
- Added request, independent Super Admin approval, and pre-generation cancellation commands.

### PostgreSQL and schema

- Added `20260719050000_qr_final_generation_approval.sql`.
- Added the durable `qr_generation_jobs` ledger/outbox intent with separate delivery and execution
  attempts, fixed max execution attempts, state metadata checks, immutable identity, guarded
  transitions, composite Batch scope FK, uniqueness, RLS, and no browser table privilege.
- Added redacted read RPCs and reviewed SECURITY DEFINER command RPCs.
- Final approval atomically commits Batch `GENERATION_APPROVED`, one
  `PENDING_DELIVERY` generation job, and one redacted audit row.
- Added matching Drizzle schema exports and structural pgTAP coverage.
- Applied migration `20260719050000` to the linked staging project. Local and remote migration
  histories match through that version.

### Web and journey

- Added the Supabase final-approval repository and Server Actions.
- Connected the QR inventory page to the separate final-approval read model.
- Added KO/EN final request, final approval queue, pre-generation cancellation, status, error, and
  boundary copy.
- UI says final approval prepares a durable job; it does not claim Queue delivery or QR generation.
- Stable per-render request IDs support command idempotency. Browser hidden status/scope values are
  not final-approval authority.

### Staging acceptance

- Extended the authenticated staging journey and cleanup fixture.
- Verified Management Admin/Site Admin/Super Admin scope and MFA flow, sample replacement,
  original-requester final request, independent Super Admin approval, Batch
  `GENERATION_APPROVED`, exactly one revision-1 `PENDING_DELIVERY` job, zero delivery/execution
  attempts, null Queue message ID, redacted audit actor order, and zero fixture residue.
- A separate staging SQL cleanup query confirmed zero ephemeral Tenant/Company/Site rows and zero
  `PENDING_DELIVERY` jobs after the run.

## Validation evidence

- `pnpm verify`: PASS
  - Biome: 176 files
  - Typecheck: 10 packages / 16 tasks
  - Vitest: 19 files / 102 tests
  - DB static check: 20 migrations / 11 database tests
  - Secret scan: 270 text files
  - Immutable logo SHA-256: PASS
  - WCJ: 100 total, C 100, J 100, W 100, 49 files
  - Next production build: PASS, including `/[locale]/admin/qr-inventory`
- `playwright test --config=playwright.staging.config.ts qr-inventory-sample.spec.ts`:
  9 passed
- `supabase db push`: migration `20260719050000` applied successfully
- Staging catalog verification: enum, table, forced RLS, scoped policy, constraints, indexes,
  triggers, SECURITY DEFINER/search path, function grants, audit uniqueness, and batch guard
  transitions confirmed

## Unresolved risks and open gates

- Docker is unavailable. `supabase test db --linked` still enters the CLI Docker runner and fails
  with `LegacyDockerRunError`.
- Docker-local `supabase db reset` and runtime pgTAP have not run. Phase 1 is not complete.
- The approval-versus-cancellation concurrent race is covered by row-lock design and unit policy
  but not yet by a two-actor staging race test.
- Queue dispatcher functions, provider publish, Worker, issuance, rendering, Asset/token creation,
  progress/retry UI, and failure injection are not implemented.
- Keyboard, screen-reader, computed contrast, 320/768/1280/1920 responsive, and real-device review
  remain manual gates.
- The branch has not been pushed and no app deployment was performed.

## Exact next development step

1. Recheck Docker. If available, run local Supabase reset and all pgTAP tests before adding another
   migration.
2. Add one dedicated authenticated staging race case that places a fresh Batch in
   `FINAL_APPROVAL_PENDING`, submits requester cancellation and independent Super Admin approval
   concurrently, and proves exactly one terminal outcome, audit consistency, and zero-or-one job
   according to the winning command.
3. Once that race gate is green, implement the smallest dispatcher-only contract:
   Application service plus server-only PostgreSQL functions to claim bounded
   `PENDING_DELIVERY` jobs, record Queue publication, and record a delivery failure/backoff.
4. Do not add a Queue provider call, resident Worker, renderer, QR Asset, public token, activation
   code, or Production host manifest in that dispatcher contract.
5. Run focused tests, `pnpm validate:wcj` after any Web change, authenticated staging acceptance,
   and `pnpm verify`.

## Required user-owned external setup

- Install or start Docker Desktop only if local reset/pgTAP evidence is desired in the next
  session.
- Do not paste database passwords, Supabase keys, Queue credentials, or any secret into chat,
  Git, documentation, or browser code.
- No Queue vendor or Production Worker host selection is required for the exact next step.

## Session handoff protocol

Continue to follow the repository `AGENTS.md` Session handoff protocol: create a new
`docs/handoff-MMDD-HHmm.md` when a major feature unit completes or repeated errors make a fresh
session safer. Every handoff must record branch and commit, completed scope, validation evidence,
unresolved risks, exact next step, and required user-owned external setup, and must never contain
credentials or secret values.
