# Taptolk Production Privacy Cleanup Cron Operations Handoff — 2026-07-20 05:33 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Starting checkpoint: `215bb11` (`docs: hand off production cleanup cron`)
- Implementation commit: `78cd2c2` (`feat: add production privacy cleanup cron`)
- Authority: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Completion report: `docs/04-report/production-privacy-cleanup-cron.report.md`
- Production runbook: `docs/deployment/production-privacy-cleanup-cron-runbook.md`
- Pilot checklist: `docs/deployment/pilot-readiness-checklist.md`
- Decision: `AUTOMATED_READY / MANUAL_GATES_PENDING / PRODUCTION_ACCESS_BLOCKED`

## Completed scope

- Confirmed the requested branch and checkpoint without changing the approved `1..100` per-Batch
  QR quantity contract.
- Confirmed the linked `taptolk-staging` project is healthy in Seoul and local/remote migrations
  match through `20260719184000`.
- Audited the current Vercel account without deploying or mutating settings:
  - the accessible team is Hobby and cannot deploy the hourly `0 * * * *` schedule;
  - the accessible project inventory has no `taptolk` Production project;
  - there is no local Vercel project link, so live Root Directory `apps/web` is unverified.
- Confirmed the accessible Supabase inventory has `taptolk-staging` but no separate Production
  Taptolk project.
- Confirmed Production SMS and CAPTCHA providers were not selected. The current code has
  fail-closed Production boundaries but no approved real provider adapters, so no vendor or
  credential shape was invented.
- Re-ran the full linked pgTAP, authenticated staging E2E, and repository verification.
- Updated the pilot checklist and PDCA completion report with the external precondition audit.
- Did not deploy, create a Production project, upgrade billing, choose a provider, mutate an
  environment value, invoke Production, or expose any credential or protected identifier.

## Validation evidence

- Complete linked pgTAP: all 22 database test files PASS with command exit 0.
- Production privacy-cleanup Cron pgTAP: 16/16 PASS.
- Authenticated staging E2E: 28 PASS, one intentional opt-in 1,000-item acceptance skip.
- `pnpm verify`: PASS on the starting checkpoint and again after the documentation updates.
- Lint: 319 files; typecheck: 19/19 tasks; unit: 53 files and 326 tests.
- Secret scan: 508 text files; WCJ: 100 / C100 / J100 / W100 over 87 files.
- Immutable logo, Production Cron static contract, and production build: PASS.
- No Production request or mutation was attempted because the plan/project/Supabase preconditions
  failed before the deployment boundary.

## Unresolved risks

1. The intended Vercel team is Hobby and cannot run the required hourly schedule.
2. The actual `taptolk` Production Vercel project is not accessible, so its ownership, deployment
   permission, Root Directory, environment names, Cron registry, logs, and rollback target cannot
   be verified.
3. No separate Production Supabase project is accessible, so Production migration history and
   cleanup ledger/audit evidence cannot be verified.
4. Production SMS and CAPTCHA providers and adapters remain undecided. Public Contact and
   notification delivery must remain fail closed.
5. Authorized aggregate-only Cron run, same-hour replay idempotency, cleanup/audit duplicate 0,
   monitoring, secret rotation, rollback, and incident ownership remain unverified.
6. Representative device, accessibility, computed contrast, physical 85mm print, domain, alert,
   and operator pilot gates remain open.

## Exact next step

Do not add product scope, do not change the QR quantity policy, and do not deploy the hourly
manifest to the Hobby team.

User-owned setup is required in this order:

1. upgrade the intended Vercel team to Pro or Enterprise, or grant access to the already approved
   Pro/Enterprise team;
2. create/import or grant access to the real `taptolk` Production Vercel project, link it to the
   approved repository, set Root Directory exactly to `apps/web`, and grant project settings,
   Production deployment, environment, Cron, and runtime-log access;
3. create or grant access to a separate Production Supabase project and approve its region;
4. select the Production SMS provider and CAPTCHA provider, including permission to implement the
   corresponding real adapters and register the approved sender where applicable;
5. name the Production monitoring/alert operator, incident owner, rollback approver, and pilot
   window.

After those non-secret settings are confirmed, resume the runbook at Production migration
verification. Configure secret values only in the deployment secret manager, deploy the exact
approved commit, then verify one authorized aggregate-only run, a same-UTC-hour replay,
cleanup/audit duplicate 0, monitoring, secret rotation, and rollback rehearsal. Re-run linked
pgTAP, Production-safe authenticated E2E, and `pnpm verify` before pilot approval.

If a quantity above 100 per Batch is requested, stop and obtain a separate quantity-policy design
approval before changing schema, UI, Worker, Queue, or export contracts.

## Required user-owned external setup

- Vercel Pro/Enterprise team approval or access.
- The actual `taptolk` Production Vercel project with Root Directory `apps/web` and the required
  non-secret permissions.
- A separate approved Production Supabase project and data region.
- Approved SMS and CAPTCHA provider decisions plus adapter-implementation authorization.
- Production monitoring, alerting, rollback, incident-owner, domain, device, accessibility, print,
  and pilot-window decisions.
- Never send database passwords, service credentials, provider tokens, Cron bearers, encryption
  or HMAC keys, OTPs, activation codes, public QR tokens, cookies, authorization headers, phone
  numbers, or message bodies through Git, documentation, logs, screenshots, or chat.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and required user-owned external setup, without credentials or secret values.
