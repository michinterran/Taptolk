# Taptolk Production Privacy Cleanup Cron Handoff — 2026-07-20 05:13 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Implementation commit: `78cd2c2` (`feat: add production privacy cleanup cron`)
- Authority: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Completion report: `docs/04-report/production-privacy-cleanup-cron.report.md`
- Production runbook: `docs/deployment/production-privacy-cleanup-cron-runbook.md`
- Pilot checklist: `docs/deployment/pilot-readiness-checklist.md`
- Decision: `AUTOMATED_READY / MANUAL_GATES_PENDING`

## Completed scope

- Preserved the approved `1..100` per-Batch QR quantity contract. No single 1,000-item Batch
  contract was added.
- Added a service-role-only PostgreSQL selector for bounded, oldest-due privacy-cleanup tenants.
- Added a sequential scheduler coordinator with `1..100` tenant and `1,000..55,000 ms` duration
  policy bounds.
- Added deterministic version-8 request UUIDs per tenant and UTC hour so duplicate or overlapping
  scheduler deliveries reuse the existing cleanup request ledger.
- Added strict Supabase mapping, aggregate-only results, per-tenant failure isolation, and
  non-2xx partial-failure reporting.
- Added authorized `GET /api/internal/privacy-cleanup`, a credential-free hourly Vercel Cron
  manifest, and a static contract verifier in `pnpm verify`.
- Added a secret-manager-only Production setup, monitoring, replay, rotation, and rollback
  runbook.
- Applied the selector migration to linked staging and completed PDCA Plan, Design, Do, Check,
  Act, and Report with a 100% design match.
- Did not deploy Production, select providers, mutate Production environment values, or expose
  any credential.

## Validation evidence

- Focused application/config/handler tests: 30/30 PASS.
- New linked Cron pgTAP: 16/16 PASS.
- Complete linked pgTAP suite: all 22 database test files PASS.
- Authenticated/local smoke E2E: 30/30 PASS across Chromium and mobile profiles.
- Final `pnpm verify`: PASS.
- Lint: 319 files; typecheck: 19/19; unit: 53 files and 326 tests.
- DB static contract: 55 migrations and 22 database test files.
- Secret scan: 506 text files after final documentation.
- WCJ: 100 / C100 / J100 / W100 over 87 files.
- Immutable logo verification, Production Cron static verification, and production build: PASS.
- No credential or secret value was written to Git, documentation, logs, or this handoff.

## Unresolved risks

1. The actual Production Vercel plan, project Root Directory, Cron bearer, first run, replay,
   monitoring, rotation, rollback, and incident owner remain unverified.
2. Production SMS and CAPTCHA provider receipt, cost reconciliation, and failure behavior remain
   unverified.
3. Representative iOS/Android scan/contact/reply journeys, VoiceOver/TalkBack, keyboard,
   computed contrast, and physical 85mm print review remain manual gates.
4. Scheduler invocation may be duplicated or overlap and failed Cron requests are not assumed to
   be retried. The implementation is idempotent, but Production monitoring and operator response
   still require rehearsal.
5. Automated readiness must not be represented as Production pilot approval until every open
   manual item in the checklist is signed off.

## Exact next step

Do not add more product scope and do not change the QR quantity policy. Execute the external pilot
gates in this order:

1. select approved Production SMS and CAPTCHA providers and configure their values only through
   the deployment secret manager;
2. verify the Production Vercel plan supports the approved hourly schedule and confirm
   `apps/web` as the project Root Directory;
3. follow `docs/deployment/production-privacy-cleanup-cron-runbook.md` to configure the Cron
   bearer, deploy the implementation commit, and observe one authorized aggregate-only run;
4. replay the same hourly invocation and prove the cleanup ledger absorbs it without duplicate
   mutations or audit rows;
5. rehearse monitoring, secret rotation, rollback, and incident ownership;
6. complete provider receipt/cost reconciliation, representative devices, accessibility,
   computed contrast, physical print, domain, alert, and operator gates;
7. record Production pilot approval only after every manual checklist item passes.

If a quantity above 100 per Batch is later requested, stop and obtain a separate quantity-policy
design approval before changing schema, UI, Worker, Queue, or export contracts.

## User-owned external setup

- Provide approved Production SMS and CAPTCHA accounts and configure their secrets only through
  the deployment secret manager.
- Provide access to the actual Production Vercel project and approve deployment only when the
  runbook preconditions are satisfied.
- Provide representative iOS/Android devices, screen readers, and the target 85mm printer.
- Provide Production domain, monitoring, alerting, rollback, incident-owner, and pilot-window
  decisions.
- Never share database passwords, service credentials, provider tokens, Cron bearers, encryption
  or HMAC keys, OTPs, activation codes, public QR tokens, cookies, authorization headers, phone
  numbers, or message bodies in Git, documentation, logs, screenshots, or chat.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and required user-owned external setup, without credentials or secret values.
