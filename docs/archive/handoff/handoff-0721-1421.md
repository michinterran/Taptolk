# Taptolk Surface Separation Staging Gate Handoff — 2026-07-21 14:21 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Starting commit: `6fe2efe`
- Surface implementation commit: `06e873f`
- Staging evidence commit: `013a54b`
- GitHub Actions: `29802031226` PASS on `6fe2efe`
- Approved linked target: `taptolk-staging`, healthy
- Active Production Cron definitions: `0`
- Production deployment: **not performed**. The deployed source remains `f022ca0`.
- Decision: `SURFACE_SEPARATION_STAGING_AUTOMATED_COMPLETE / MANUAL_AND_EXTERNAL_GATES_PENDING`

## Completed scope

- Ran the skipped linked database gate against the approved staging project without
  applying a schema change. All tests use transaction rollback.
- Re-ran authenticated staging after the route group move and directly proved the four
  required authorization outcomes:
  - Site Admin plus MFA reaches the customer dashboard.
  - Super Admin plus MFA reaches the platform dashboard.
  - Site Admin is redirected away from the platform workspace.
  - An approval-pending account is denied from five administrator workspaces and sees
    zero rows across five representative RLS-protected operational tables.
- Added the missing approval-pending browser and RLS evidence to
  `e2e/staging/admin-role-routing.spec.ts`. The ephemeral account is deleted during
  cleanup and no credential or user content is printed.
- Made staging MFA verification deterministic during cold Next.js Server Action
  compilation by requiring at least 18 seconds in the current TOTP window.
- Revalidated the existing QR, caller/contact, Owner activation, administrator, Site
  CRUD, maker-checker, tenant-isolation, cleanup, and QR concurrency journeys.
- Reviewed WCJ `J005` and confirmed that the rewrite matches the intended product
  contract: public landing/onboarding/header expose zero administrator links, while the
  administrator portal keeps one canonical sign-in and server-side role routing.

## Validation evidence

- `corepack pnpm db:test:linked`: 24 files / 604 assertions PASS
- `corepack pnpm e2e:staging`: 31 PASS / 1 intentional opt-in Worker acceptance skip
- Role-routing focused suite: 3/3 PASS
- `corepack pnpm verify:production-cron:deferred`: PASS, active definitions `0`
- `corepack pnpm verify`: PASS
  - lint: 339 files
  - typecheck: 19/19 tasks
  - unit: 56 files / 379 tests
  - DB static contract: 59 migrations / 24 database tests
  - secret scan: 574 text files
  - WCJ: 100 / C100 / J100 / W100 over 99 files
  - Production build: PASS
- Post-document `corepack pnpm verify:secrets`: 575 text files PASS
- GitHub Actions `29802031226`: PASS

## Resolved validation issues

1. The new approval-pending probe initially used a nonexistent table name. Administrator
   route denial had already passed; the REST probe was corrected to five real
   RLS-protected operational tables and then passed with zero visible rows.
2. A subsequent full run exposed an existing TOTP timing flake when a code had only a
   short validity window before cold Server Action compilation. The staging-only helper
   now keeps an 18-second minimum validity window. The final complete suite passed in a
   single run.

Neither issue changed application authorization, central RBAC, PostgreSQL RLS, MFA,
audit behavior, Production data, or Production deployment.

## Unresolved risks and manual gates

- Real iOS and Android journeys remain manual.
- VoiceOver, TalkBack, computed contrast, and final copy/design acceptance remain manual.
- The opt-in real 10x100 Worker Queue export test remains excluded from the default
  staging command; its earlier acceptance evidence is unchanged.
- Production deployment and `taptolk.com` domain connection were not authorized.
- Production Supabase, live Kakao AlimTalk, legal review, monitoring, rollback, and
  incident ownership remain external gates.

## Exact next development step

No further development step is authorized in this checkpoint. Stop and wait for user
direction. Do not start Task 3 access grant, Test Lab, persona features, the live Kakao
AlimTalk provider, Production deployment, or domain connection.

## Not started, by instruction

Task 3 access grant, Test Lab, persona features, live Kakao AlimTalk Provider,
Production deployment, `taptolk.com` domain connection, Production Supabase project or
secret setup, CAPTCHA or a new authentication provider, enterprise SSO, Vercel plan
changes, Cron activation, and monitoring, rollback, or incident owner assignment.

## User-owned external setup

- Approve and create the separate Production Supabase project when ready.
- Connect `taptolk.com` only after separately authorizing the domain work. Fix the
  canonical QR host before printing physical stickers.
- Complete Kakao business registration, dealer contract, and informational-template
  review before authorizing the live Provider.
- Complete legal review of terms and privacy drafts before service launch.
- Never provide database credentials, provider tokens, Cron bearers, phone numbers,
  messages, OTPs, QR, activation, or response tokens, cookies, authorization headers,
  or secret values in Git, documentation, logs, screenshots, or chat.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must
record the current branch and commit, completed scope, validation evidence, unresolved
risks, exact next step, and required user-owned external setup, without credentials or
secret values.
