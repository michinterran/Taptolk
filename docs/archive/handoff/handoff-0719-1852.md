# Taptolk Phase 2–4 Handoff — 2026-07-19 18:52 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Implementation commit: `e89c87e` (`feat: implement taptolk phases 2 through 4`)
- Workspace state at handoff creation: clean before this handoff document
- Authority: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Detailed audit: `docs/architecture/phase-2-4-gap-audit.md`
- Implementation report: `docs/04-report/phase-2-4-implementation.report.md`

## Completed scope

### Phase 2 — Brand·Sticker Design

- PNG/SVG validation, SVG active-content rejection, checksum and dimensions
- private brand asset Storage and audited registration
- four immutable 85mm/300 DPI templates
- Site/template/customer-logo Design Wizard
- actual QR PNG/SVG sample render and decode
- authenticated desktop/mobile private preview
- maker-checker design and sample approval

### Phase 3 — QR Issuance·Batch

- CSPRNG public token, Human Code checksum, activation code
- SHA-256 lookup hashes and AES-256-GCM ciphertext/key version
- 1,000 unique credentials, 50-item chunks, ordinal resume, duplicate-safe commit
- PGMQ read/visibility/archive Worker loop and bounded generation/export failure handling
- approved optional customer logo in sample and bulk render
- actual 85mm PDF, CSV, ZIP and checksum manifest
- scoped generation/export progress read model and KO/EN UI

### Phase 4 — Inventory·Assignment

- Batch receipt and atomic `IN_STOCK`
- manual assignment with protected vehicle plate
- CSV validation ledger, original-file non-retention and atomic commit
- database double-Binding prevention
- replacement and revocation with append-only history and redacted audit

## Validation evidence

- `pnpm verify`: PASS
- Vitest: 43 files / 270 tests PASS
- DB static contract: 32 migrations / 14 database tests PASS
- Secret scan: 360 text files PASS
- immutable logo SHA-256: PASS
- WCJ: 100 / C100 / J100 / W100, 54 files
- production build: PASS
- Playwright local smoke: desktop/mobile 28 tests PASS

## Unresolved risks

1. Docker is unavailable, so the 10 new migrations have not passed local reset or runtime pgTAP.
2. The migrations have not been applied to staging.
3. Staging does not yet have a verified `pgmq` extension, exposed `pgmq_public` API, or
   `qr-generation` Queue.
4. Authenticated staging Phase 2–4 journey E2E has not run against the new schema.
5. Actual 85mm print output, real-device decode, keyboard, screen-reader, computed contrast,
   and responsive manual QA remain.

Do not claim staging deployment, live Queue operation, or Phase 2–4 release acceptance until
these gates pass.

### 2026-07-19 live staging preflight update

- `taptolk-staging` is `ACTIVE_HEALTHY` on PostgreSQL 17.
- Migration history ends at `20260719052000`.
- `supabase db push --dry-run` connected successfully and reported exactly the ten
  `20260719060000`–`20260719069000` migrations as pending; it made no remote changes.
- `pgmq` and `pgtap` are available but not installed. No `pgmq`/`pgmq_public` schema was
  observed.
- Supabase security/performance advisors were read without changing staging. Existing
  intentional authenticated `SECURITY DEFINER` RPC warnings and pre-Phase-2 foreign-key
  index notices remain review items; the new migrations are not yet present for advisor review.

## Exact next development step

On a Docker-capable environment:

1. Run `pnpm db:start`.
2. Run `pnpm db:reset:local`.
3. Run the complete Supabase pgTAP suite.
4. Fix any SQL runtime error without weakening RLS, constraints, audit, or history retention.
5. Re-run `pnpm verify`.

After local SQL acceptance, ask the user to provision or authorize the staging Queue setup,
apply migrations, and run the authenticated staging Phase 2–4 journey.

## User-owned external setup

- Enable Supabase Queues/`pgmq` in `taptolk-staging`.
- Create the durable `qr-generation` Queue.
- Expose the Queue API through `pgmq_public`.
- Grant only the intended service-role Queue functions.
- Confirm staging environment variables without sharing values in chat, Git, logs, or docs.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record
the current branch and commit, completed scope, validation evidence, unresolved risks, exact
next step, and required user-owned external setup, without credentials or secret values.
