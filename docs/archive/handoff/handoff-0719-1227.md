# Taptolk Session Handoff — 2026-07-19 12:27 KST

## 1. Handoff rule

Create `docs/handoff-MMDD-HHmm.md` when a major feature unit is complete or when
the same error begins repeating and a fresh session would reduce context and
token waste.

Every handoff must record the current branch and commit, completed scope,
validation evidence, unresolved risks, exact next step, and any required
user-owned external setup. Never include credentials or secret values. Carry
this rule into every later handoff document, then notify the user that this is a
safe checkpoint and recommend opening a new Codex session with the handoff path.

## 2. Project authority and working state

- Highest specification: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Engineering contract: `AGENTS.md`
- Working branch: `codex/phase-1-foundation`
- Completed feature commit: `e1d361873291a3454049a59aad220ed2efcc6434`
- Remote branch before handoff commit: `origin/codex/phase-1-foundation`
- Branch position before handoff commit: ahead of remote by 3 commits
- Supabase environment: staging only
- Supabase project ref: `evpwzjkhfppdjivkyokh`
- Latest migration: `20260719032200_phase_1_site_lifecycle_request_visibility.sql`
- Working tree at handoff creation: clean before this handoff-only change

## 3. Completed feature unit

The Site lifecycle request/approval maker-checker vertical slice is complete.

- Tenant-owned `site_lifecycle_requests`
- Explicit `SUSPEND`, `REACTIVATE`, `CLOSE` actions
- Explicit `PENDING`, `APPROVED`, `REJECTED`, `CANCELLED` request states
- One pending request per Site
- Site/request optimistic version checks
- Existing direct Site lifecycle permissions preserved without widening
- Management Admin:
  - suspend/reactivate/close request in Management Company scope
- Site Admin:
  - suspend/reactivate request for exact Site
- Platform Operator:
  - suspend/reactivate direct authority preserved
  - close request authority preserved
- Super Admin:
  - all current direct authority preserved
  - independent approval/rejection queue
- Application and PostgreSQL requester/checker separation
- Request, approve, reject, cancel audited RPC commands
- Approval transaction atomically updates Site, request, and redacted audit
- KO/EN request, pending, cancel, approve, and reject states
- Request reasons hidden from Site Operator and Read Only in Application and RLS
- Cross-tenant and sibling-Site tampered requests rejected
- PDCA Plan, Design, Analysis, and Report completed at 100% match

Primary artifacts:

- `packages/application/src/site-lifecycle-request-service.ts`
- `apps/web/admin/supabase-site-lifecycle-request-repository.ts`
- `apps/web/admin/site-lifecycle-request-actions.ts`
- `apps/web/components/site-catalog-view.tsx`
- `supabase/migrations/20260719030000_phase_1_site_lifecycle_requests.sql`
- `supabase/migrations/20260719032200_phase_1_site_lifecycle_request_visibility.sql`
- `supabase/tests/database/phase_1_site_lifecycle_requests.sql`
- `e2e/staging/site-crud-tenant-isolation.spec.ts`
- `docs/04-report/site-lifecycle-maker-checker.report.md`

## 4. Validation evidence

- `pnpm verify`: passed using Node `v24.18.0` and Corepack `pnpm 10.34.5`
- Unit tests: 17 files, 80 tests passed
- WCJ: W 100, C 100, J 100, 47 files
- DB static contract: 17 migrations and 9 database tests
- Secret scan: 243 text files passed
- Immutable logo SHA-256 check: passed
- Next.js production build: passed
- Playwright Desktop/Mobile smoke: 26 tests passed
- `pnpm e2e:staging:sites`: 4 tests passed
- Staging local/remote migration history: 17 entries aligned
- Final staging cleanup:
  - Tenant: 0
  - Management Company: 0
  - Site: 0
  - Admin Profile: 0
  - Site lifecycle request: 0
  - Site E2E audit row: 0
  - Ephemeral Auth user: 0

Use the project-pinned package manager:

```sh
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" corepack pnpm verify
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" corepack pnpm e2e:smoke
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" corepack pnpm e2e:staging:sites
```

Do not use the Codex bundled `pnpm 11`; the repository requires pnpm 10.

## 5. Remaining risks and external settings

- Do not claim Phase 1 complete until Supabase Local migration reset and local
  pgTAP runtime pass.
- The `docker` executable is not installed, so those two Local DB gates remain
  open.
- WCJ and browser automation do not replace keyboard, screen-reader, computed
  contrast, responsive real-device, and real-journey manual review.
- Supabase Advisor authenticated `SECURITY DEFINER` RPC warnings remain an
  intentional command-boundary review item.
- Supabase leaked-password protection should be enabled before production
  readiness.
- MFA recovery and Admin idle-timeout policy remain open.
- Reject/cancel are covered by Application unit policy and static pgTAP
  contracts; the current staging browser suite exercises request and approve as
  the required maker-checker journey.
- No Production project, Vercel Preview, SMS provider, or Worker runtime was
  connected in this feature unit.

## 6. Exact next development step

Implement the QR inventory and batch lifecycle foundation, starting with the
smallest reviewed sticker-sample flow.

1. Add Plan and Design documents for `StickerDesignVersion`, `QrBatch`, and
   `QrAsset`, including glossary, ERD, field definitions, retention, and exact
   Tenant/Site ownership.
2. Freeze explicit state machines from the master specification before code:
   - Sticker design: `DRAFT → APPROVED → ARCHIVED`
   - QR Batch request/sample/final-approval/generation/failure/cancel states
   - QR Asset issuance/assignment/suspend/revoke/expire history
3. Preserve the approved authority split:
   - Management/Site Admin request Batch and approve sample in scope
   - Super Admin performs final bulk-generation approval
   - Platform Operator operates failures but cannot perform final approval
   - requester and final approver cannot be the same where maker-checker is
     required
4. Implement the first vertical slice only:
   - create/review/archive Sticker Design Version
   - request a small QR Batch
   - attach and approve/invalidate a sample artifact
   - do not start bulk generation yet
5. Add composite tenant/Site constraints, browser RLS, central RBAC, optimistic
   versions, terminal history, and same-transaction redacted audit.
6. Add KO/EN Platform/Company/Site queue states and explicit
   success/wait/failure behavior.
7. Add pgTAP contracts and authenticated staging E2E for scoped request,
   sample approval, cross-tenant denial, checker separation, and cleanup `0`.
8. Run WCJ after each web change, then `pnpm verify`, Desktop/Mobile smoke, and
   staging authenticated E2E.

Do not create `packages/qr-engine` or a Worker placeholder in this step. A new
package requires a real owner, public API, implementation, and tests. Bulk
generation remains behind the later Queue/Worker/Render/Export decision.

If Docker becomes available before or during the next slice, run the pending
Local reset and pgTAP acceptance first and record the result without widening
the QR sample scope.
