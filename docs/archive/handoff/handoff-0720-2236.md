# Taptolk Task 0 Quantity Contract Handoff — 2026-07-20 22:36 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Task 0 implementation commit: `1314532`
- Prior Claude collaboration brief commit: `7924d6b`
- Work order: `docs/development/codex-workorder-0720-2055.md`
- PDCA report: `docs/04-report/qr-batch-quantity-contract.report.md`
- Pilot checklist: `docs/deployment/pilot-readiness-checklist.md`
- Current stage: `DEVELOPMENT_TEST`
- Active Production Cron definitions: `0`
- Decision: `TASK_0_COMPLETE / TASK_1_NEXT / EXTERNAL_PILOT_GATES_PENDING`

## Completed scope

- Added forward-only migration `20260720210000_qr_batch_quantity_contract.sql`.
- Verified zero existing rows above 100 locally and in linked Staging before migration
  application.
- Added a locked preflight that aborts with SQLSTATE `23514` rather than deleting or rewriting
  any violating row.
- Restored the PostgreSQL and Drizzle `qr_batches.requested_quantity` CHECK to `1..100`.
- Restored the Application upper bound to `100`.
- Added unit coverage for `101` rejection and `100` acceptance.
- Added pgTAP coverage for RPC rejection, RPC acceptance, and direct-insert CHECK rejection.
- Applied the migration to the approved linked Staging project and verified the effective
  constraint.
- Preserved the ten normal Batches × 100 items acceptance contract.
- Did not modify the six unrelated `10000` constants or the three analysis-only items in work
  order sections 2.5 and 2.6.
- Did not select or configure any Production project, Provider, Secret, plan, Cron, or operations
  owner.

## Validation evidence

- Clean local Supabase reset: PASS twice.
- Local pgTAP: 23 files / 590 tests PASS.
- Linked Staging migration application: PASS.
- Linked pgTAP through `pnpm db:test:linked`: 23 files / 590 tests PASS.
- `pnpm verify`:
  - lint 324 files;
  - typecheck 19/19;
  - unit 53 files / 327 tests;
  - DB static 57 migrations / 23 tests;
  - Secret scan 534 files;
  - logo, deferred Cron, WCJ 100, and Production build PASS.
- Authenticated Staging:
  - default suite: 25 PASS before one transient Management Admin MFA navigation timeout; one
    skip and two not run;
  - immediate focused Site CRUD/tenant-isolation rerun: 4/4 PASS.
- Explicit 10x100 acceptance: 1 PASS in 1.1 hours.
  - Batches 10;
  - quantity 100 each;
  - total 1,000;
  - duplicate 0;
  - decode 1,000/1,000;
  - export/checksum 40/40;
  - poison active 0;
  - residue 0.

The first pgTAP attempt exposed an invalid new test fixture design configuration. The fixture was
corrected to the existing validated QR design schema; no validator or business rule was weakened.

The long acceptance emitted local Next.js development-cache persistence/compaction warnings but
the Worker, database, export, checksum, and cleanup assertions passed.

## Section 2.6 analysis

1. `render_jobs.requested_count` is structurally tied to one `qr_batch_id`, but no active creation
   path was found; it was not changed.
2. `issueQrBatch` is a generic credential helper and the active Worker calls it per 50-item chunk;
   it was not changed.
3. `buildPrintExportBundle` receives one Batch context in the active Worker path, while the engine
   API remains a reusable item-list boundary; it was not changed.

See the PDCA report for the full code-path evidence.

## Working tree coordination

During the long acceptance run, unrelated Claude-authored landing, copy, Owner UI, and E2E changes
appeared in the shared working tree, together with development notes. They were preserved and
excluded from Task 0 staging and commits. A later session must inspect `git status` and coordinate
those files before editing overlapping web or E2E areas.

## Unresolved risks and gates

1. Task 1 server-only stage policy and Service leak guard are not implemented.
2. The full authenticated Staging suite did not complete in one uninterrupted PASS run because of
   the transient MFA navigation timeout; the affected complete suite passed 4/4 on immediate
   rerun.
3. `render_jobs.requested_count`, `issueQrBatch`, and `buildPrintExportBundle` limits require a
   separate decision before any change.
4. Production Supabase project and region remain undecided.
5. Production SMS and CAPTCHA Providers and adapters remain undecided and unimplemented.
6. Production Secret configuration remains intentionally absent.
7. Representative device, accessibility, computed-contrast, and physical 85mm print checks
   remain manual.
8. Production monitoring, alerting, rollback, incident ownership, domain, and pilot window remain
   undecided.
9. Production Cron activation remains deferred. Hobby stays at zero active definitions.

## Exact next development step

Continue with Task 1 in `docs/development/codex-workorder-0720-2055.md`:

1. inspect the current `packages/config` server-only boundaries and production leak verifiers;
2. implement a typed server-only `DEVELOPMENT_TEST` / `SERVICE` policy;
3. fail closed for missing or invalid configuration;
4. prove browser inputs and `NEXT_PUBLIC_` values cannot select the stage;
5. add an extensible static Service test-surface leak guard to the `pnpm verify` chain;
6. run Task 1 unit tests and `pnpm verify`;
7. stop at the Task 1 boundary before beginning the login-routing Task 2.

Do not start Test Lab, access grants, or Persona sessions in Task 1. Do not select any external
Production setting.

## User-owned external setup

- Name or approve a separate Production Supabase project and data region only when entering the
  Production setup lane.
- Later, name the approved Production SMS and CAPTCHA Providers before adapter implementation.
- Name Production monitoring, rollback, and incident owners.
- At actual service launch only, approve any required Vercel plan change and active Cron release.
- Never provide credentials, tokens, phone numbers, messages, OTPs, cookies, authorization
  headers, or QR/activation/response material in Git, documentation, logs, screenshots, or chat.

## Continuation prompt

`docs/handoff-0720-2236.md를 읽고 Exact next development step부터 계속 진행해.`

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and required user-owned external setup, without credentials or secret values.
