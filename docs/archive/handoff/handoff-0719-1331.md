# Taptolk Session Handoff — 2026-07-19 13:31 KST

## Current branch and commit

- Project: `/Users/benjaminsong/Documents/Taptolk`
- Branch: `codex/phase-1-foundation`
- Current local HEAD: `a0c2c90`
- Tracked upstream at session start: `origin/codex/phase-1-foundation` `1b2ba84`
- Working tree: QR inventory/sample foundation changes are intentionally **uncommitted**.
- Staging schema: migrations through `20260719043000` are applied and local/remote history matches.

Do not report the current feature as committed or pushed until the working tree is reviewed and an
explicit Git action succeeds.

## Completed scope

The exact next step from `docs/handoff-0719-1227.md` is complete:

- Plan:
  `docs/01-plan/features/qr-inventory-sample-foundation.plan.md`
- Design:
  `docs/02-design/features/qr-inventory-sample-foundation.design.md`
- Analysis:
  `docs/03-analysis/qr-inventory-sample-foundation.analysis.md`
- Report:
  `docs/04-report/qr-inventory-sample-foundation.report.md`
- PDCA feature `qr-inventory-sample-foundation`: completed, match rate 100%.
- Central RBAC permissions for Sticker Design and QR Batch operations.
- Application service and unit tests for:
  - Design create, independent approve, archive
  - 1–100 Batch request
  - sample attach, independent approve, retained invalidation
  - requester cancellation
  - role-specific read and approval queues
- PostgreSQL and Drizzle foundation for:
  - `sticker_design_versions`
  - `qr_batches`
  - `qr_batch_samples`
  - `qr_assets`
  - append-only `qr_asset_status_logs`
- Full frozen Batch/Asset states, first-slice transition guards, composite tenant constraints, RLS,
  reviewed RPCs, same-transaction redacted audit, and no physical-delete command.
- Actor UUIDs are excluded from authenticated browser column grants and read DTOs. The scoped read
  RPC returns only current-actor booleans for maker-checker decisions.
- Canonical `/ko|en/admin/qr-inventory` UI with typed KO/EN copy and an explicit statement that
  sample approval does not start bulk generation.
- Dashboard entry points, unauthenticated route denial, and authenticated Staging journey coverage.

Deliberately not implemented:

- final bulk-generation approval
- Queue/Worker or renderer execution
- QR Asset issuance or public token generation
- SVG/PDF/ZIP production
- printer/shipping/distribution workflow commands

## Staging changes

Applied to linked project `taptolk-staging`:

- `20260719040000_qr_inventory_sample_foundation.sql`
- `20260719043000_qr_inventory_actor_visibility.sql`

`supabase migration list` confirms all 19 local and remote versions match through
`20260719043000`. No credential or secret value is recorded here.

## Validation evidence

- `corepack pnpm verify`: pass
  - Biome: 172 files
  - TypeScript: 10 packages / 16 tasks
  - Vitest: 18 files / 89 tests
  - DB static check: 19 migrations / 10 database tests
  - secret scan: 258 text files
  - immutable logo SHA-256: pass
  - WCJ: 100, C 100 / J 100 / W 100, 49 files
  - production build: includes `/{locale}/admin/qr-inventory`
- `corepack pnpm e2e:smoke`: 28/28 pass on Desktop Chrome and Pixel 7.
- `corepack pnpm e2e:staging:sites`: 10/10 pass.
  - QR inventory/sample: 6
  - existing Site lifecycle/isolation regression: 4
  - final QR/customer/admin/Auth cleanup residue: `0`
- Staging migration dry-run and actual push: pass.
- `git diff --check`: pass.

## Unresolved risks and gates

- Docker executable/daemon is unavailable, so Supabase Local reset and runtime pgTAP were not run.
  This still blocks any claim that all Phase 1 acceptance gates are complete.
- Staging lacks runtime pgTAP; real Auth/MFA browser journeys, scoped RLS behavior, constraints,
  audits, and cleanup were used as the live evidence.
- Keyboard, screen-reader, computed contrast, and real-device manual WCJ review remain open.
- The current working tree is uncommitted while the Staging schema already contains its two QR
  migrations. Review and commit/push should happen before another developer changes migration
  history.
- The sample slice intentionally stops at `SAMPLE_APPROVED`; extending it directly into generation
  would violate the approved state and architecture boundary.

## Exact next development step

1. Check whether Docker has become available. If it has, run the open database acceptance first:

   ```bash
   export PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH"
   corepack pnpm exec supabase db reset
   corepack pnpm exec supabase test db
   ```

2. If Docker is still unavailable, do not add a generation command yet. Create a separate PDCA
   Plan/Design feature named `qr-final-generation-approval` that freezes:
   - `SAMPLE_APPROVED → FINAL_APPROVAL_PENDING → GENERATION_APPROVED`
   - Batch requester and final approver separation
   - Super Admin-only final approval
   - idempotent Queue handoff contract
   - retry/failure/cancellation boundaries
   - a Queue/Worker runtime ADR

3. Stop after Plan/Design review before implementing Queue, Worker, renderer, Asset issuance, or
   token generation.

## Required user-owned external setup

- Install/start Docker Desktop only if the user wants to close the local reset/pgTAP gate.
- No database password, Supabase secret, token, or other credential should be placed in Git,
  documentation, chat, or browser code.
- No new external provider setup is required for the next Plan/Design step.

## Session handoff protocol

Continue to follow the repository `AGENTS.md` session handoff protocol:

- Create `docs/handoff-MMDD-HHmm.md` when a major feature unit is complete or when the same error
  begins repeating and a fresh session would reduce context and token waste.
- Record current branch and commit, completed scope, validation evidence, unresolved risks, exact
  next step, and required user-owned external setup.
- Never include credentials or secret values.
- Carry this protocol forward verbatim or by explicit reference in every later handoff.
- After writing a handoff, notify the user that it is a safe checkpoint and recommend opening a
  new Codex session with the handoff path.
