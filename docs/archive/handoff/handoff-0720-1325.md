# Taptolk Public Landing and Pilot Gate Handoff — 2026-07-20 13:25 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Public landing/Production commit: `6a78fd9`
  (`feat: add public landing and role onboarding`)
- Prior handoff commit: `3c420d1`
- Prior implementation/Production commit: `0e2a15e`
- GitHub quality run: `29716589923` PASS
- Production deployment: `dpl_BAgu1XGxQeN97nab1op8ZGovyoQ3` READY
- Stable URL: `https://taptolk.vercel.app`
- Root Directory: `apps/web`
- Active Production Cron definitions: `0`
- Pilot checklist: `docs/deployment/pilot-readiness-checklist.md`
- PDCA report: `docs/04-report/public-landing-onboarding.report.md`
- Decision: `AUTOMATED_READY / MANUAL_GATES_PENDING`

## Completed scope

- Replaced the public Phase/Foundation development screen with a localized product landing.
- Added localized `/{locale}/onboarding` with separate caller, vehicle owner, customer
  administrator, and Taptolk platform administrator paths.
- Kept caller and new-owner starts bound to issued QR/token context; no generic tokenless contact
  or activation entry was introduced.
- Separated customer administrator login at `/{locale}/admin/login` from platform administrator
  login at `/{locale}/admin/platform/login`.
- Reused the existing authentication action, callback, MFA, central RBAC, repository, and
  PostgreSQL RLS boundaries. Login area is preserved through failure and OAuth callback paths.
- Added typed KO/EN copy, semantic headings, responsive styles, WCJ source checks, Axe coverage,
  and 320 CSS pixel overflow coverage.
- Increased the staging TOTP fixture's minimum remaining validity from three to eight seconds to
  remove a boundary-window test race. Product authentication policy did not change.
- Closed the `public-landing-onboarding` PDCA cycle at 100% design match.
- Updated the pilot checklist and Phase 0-9 closeout addendum.
- Deployed the verified public-surface commit to Production with the default Cron-deferred
  manifest. No provider, Production database, secret, plan, or Cron decision was made.
- Preserved the `1..100` per-Batch contract. No single 1,000-item Batch contract was introduced.

## Validation evidence

- Clean local Supabase reset: NOT RUN. No Docker-compatible runtime is installed or active.
- Linked pgTAP: all 22 database test files PASS.
- Authenticated linked-staging E2E: 28 PASS and one intentional opt-in 10x100 acceptance skip in
  one complete run.
- Clean QR-inventory serial rerun: 15/15 PASS, including concurrency and residue-zero cleanup.
- `pnpm verify`: PASS.
- Lint: 324 files PASS.
- Typecheck: 19/19 tasks PASS.
- Unit: 53 files and 326 tests PASS.
- DB static contract: 55 migrations and 22 database tests PASS.
- Secret scan: 526 text files PASS.
- Immutable logo: required SHA-256 PASS.
- WCJ: 100 / C100 / J100 / W100 over 91 files.
- Production build: PASS.
- GitHub Actions `29716589923`: PASS including Linux Chromium browser smoke.
- Vercel deployment `dpl_BAgu1XGxQeN97nab1op8ZGovyoQ3`: READY and stable alias active.
- Production route evidence: `/ko`, `/en`, `/ko/onboarding`, `/en/onboarding`,
  `/ko/admin/login`, `/ko/admin/platform/login`, and `/api/health` return 200.
- Unconfigured `/api/internal/privacy-cleanup` returns 503 and performs no authorized run.
- Production browser evidence: no console errors; document width equals viewport width at 1440
  and 320 CSS pixels.
- Deferred Cron verifier: PASS. The deployed active manifest contains no Cron definition.

## Unresolved risks and gates

1. Formal Phase 1 completion remains blocked by the clean local Supabase reset. The pinned
   Supabase CLI is available through the workspace, but no Docker, Podman, Colima, OrbStack, or
   Lima command/socket/runtime is available.
2. A separate approved Production Supabase project and region remain undecided and inaccessible.
3. Production SMS and CAPTCHA providers and their adapters remain undecided and unimplemented.
4. Production secret configuration remains intentionally absent.
5. Representative iOS/Android, VoiceOver/TalkBack, computed contrast, keyboard, and physical
   85mm print checks remain manual.
6. Production monitoring, alerting, rollback, incident ownership, domain, and pilot window remain
   undecided.
7. Actual-service Cron activation, same-hour replay, duplicate-zero proof, monitoring, secret
   rotation, and rollback rehearsal remain intentionally deferred. Hobby stays at active Cron 0.
8. Local Vercel CLI is `56.2.1`; the current published version checked during this session is
   `56.3.2`. Upgrade the CLI before the next Vercel operation; do not change the Vercel plan or
   activate Cron as part of that tooling update.
9. GitHub Actions reports a non-blocking warning that `actions/checkout@v4` and
   `actions/setup-node@v4` target the deprecated Node 20 action runtime while the job is forced
   onto Node 24.

## Exact next development step

Stop before installation. Ask the operator to name exactly one Docker-compatible macOS runtime
product and explicitly approve installing and starting that named runtime. Do not choose a
product on the operator's behalf.

After that exact approval only:

1. install/start only the named runtime and confirm its Docker-compatible daemon responds;
2. run `corepack pnpm db:start`;
3. run `corepack pnpm db:reset:local`;
4. run `corepack pnpm exec supabase test db`;
5. record the clean reset and complete local pgTAP evidence without credentials or data values;
6. only then continue to the separate Production Supabase project/region decision.

Do not select a Production project/region, SMS/CAPTCHA provider, secret, monitoring owner, or
incident owner. Do not request a Vercel Pro/Enterprise decision until actual hourly Cron
activation time.

If a quantity above 100 per Batch is requested, stop and obtain separate quantity-policy design
approval before changing schema, UI, Worker, Queue, or export contracts.

## User-owned external setup

- Name one Docker-compatible macOS runtime product and explicitly approve its install/start.
- Later, name the approved separate Production Supabase project and region, then grant access
  through the provider's own access control.
- Later, name the approved Production SMS and CAPTCHA providers before adapter implementation.
- Provide representative devices, assistive-technology operators, and the target 85mm printer.
- Name the accountable Production monitoring, rollback, incident, domain, and pilot-window
  owners.
- At actual service launch only, approve the Vercel plan change and active Cron release.
- Never provide database credentials, provider tokens, Cron bearers, phone numbers, messages,
  OTPs, QR/activation/response tokens, cookies, authorization headers, or secret values in Git,
  documentation, logs, screenshots, or chat.

## Continuation prompt

`docs/handoff-0720-1325.md를 읽고 Exact next development step부터 계속 진행해.`

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and required user-owned external setup, without credentials or secret values.
