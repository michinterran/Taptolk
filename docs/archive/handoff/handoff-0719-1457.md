# Taptolk handoff — 2026-07-19 14:57 KST

## Current branch and commit

- Branch: `codex/phase-1-foundation`
- Commit: `36339387c2d14dfe5a1fd9f06d4f0ce63a20e87d`
- Commit subject: `feat: add QR generation dispatcher contract`
- GitHub push/deploy: not performed
- Worktree before this handoff file: clean

## Current phase

- Official development phase remains **Phase 1** because Docker-local reset and runtime pgTAP are
  still open acceptance gates.
- Phase 3 has an initial approved foundation: QR inventory/sample, final generation approval,
  durable generation intent, and the dispatcher-only delivery contract.
- No Queue provider, resident Worker consumer, QR issuance, renderer, Asset/token creation, or
  Production runtime was added.

## Completed scope

### Application dispatcher contract

- Added `QrGenerationDispatcherService` and an explicit repository boundary.
- Added bounded claim policy: limit 1–50 and lease 5–300 seconds.
- Added validated publication acknowledgement and delivery-failure retry inputs.
- Kept provider message identity and raw provider payloads out of returned DTOs.
- Added 15 focused unit tests covering validation, normalization, and retry-time bounds.

### PostgreSQL dispatcher contract

- Added migration `20260719051000_qr_generation_dispatcher_contract.sql`.
- Added service-role-only SECURITY DEFINER functions to:
  - claim ready or expired delivery intents with `FOR UPDATE SKIP LOCKED`;
  - record Queue publication and atomically advance the Batch to `GENERATION_QUEUED`;
  - record a redacted delivery failure and bounded retry without marking the Batch failed.
- Added optimistic version updates for expired lease reclaim so stale acknowledgements conflict.
- Browser roles have no EXECUTE privilege on dispatcher functions.
- Added structural pgTAP coverage in `qr_generation_dispatcher_contract.sql`.

### Approval/cancellation concurrency hardening

- Added an authenticated two-actor staging race between requester cancellation and independent
  Super Admin approval.
- The first expanded run found a real operational issue: the losing RPC could hold a row-lock wait
  until the outer request returned 504 even though the winning transaction committed.
- Added migration `20260719052000_qr_final_approval_lock_timeout.sql`, setting a 3-second DB lock
  wait for the two competing commands.
- Mapped PostgreSQL `55P03`/lock timeout to the existing retryable conflict surface.
- The rerun completed the race in 11.5 seconds and proved exactly one committed outcome.

### Staging acceptance

- Applied migrations `20260719051000` and `20260719052000` to linked staging.
- Verified the real server-only sequence:
  `PENDING_DELIVERY → DELIVERY_LEASED → RETRY_WAIT → DELIVERY_LEASED → QUEUED`.
- Verified delivery attempts increment independently, execution attempts remain zero, the provider
  message ID is stored only in the ledger, and the Batch advances atomically to
  `GENERATION_QUEUED`.
- Verified the claim/result allowlists and zero staging fixture residue.
- Catalog inspection confirmed SECURITY DEFINER, search path, grants, and function lock settings.

## Validation evidence

- `pnpm verify`: PASS
  - Biome: 178 files
  - Typecheck: 10 packages / 16 tasks
  - Vitest: 20 files / 117 tests
  - DB static check: 22 migrations / 12 database tests
  - Secret scan: 276 text files
  - Immutable logo SHA-256: PASS
  - WCJ: 100 total, C 100, J 100, W 100, 49 files
  - Next production build: PASS
- Authenticated staging QR journey: 11 passed in 1.2 minutes.
- Dispatcher Application focused tests: 15 passed.
- Supabase dry-run and remote migration apply: PASS.

## Unresolved risks and open gates

- Docker remains unavailable. Local `supabase db reset` and runtime pgTAP have not run, so Phase 1
  is not complete.
- The new pgTAP file has static validation and equivalent staging catalog evidence but still needs
  the Docker-local runtime.
- Provider publish-success plus acknowledgement-failure duplicate safety is designed but not yet
  failure-injection tested.
- Queue payload schema version/compatibility is not yet frozen in `apps/worker`.
- Queue provider integration, resident Worker generation consumer, QR issuance/rendering,
  progress/retry UI, and Production runtime selection remain intentionally unimplemented.
- Keyboard, screen-reader, computed contrast, responsive, and real-device review remain manual
  WCJ gates.
- The branch has not been pushed and no app deployment was performed.

## Exact next development step

1. Recheck Docker first. If available, run local Supabase reset and all pgTAP tests before another
   migration.
2. With Docker still unavailable, implement only the versioned, redacted Queue payload
   compatibility contract in `apps/worker`:
   - add an explicit schema version and generation revision;
   - keep the stable job/Tenant/Site/Batch identifiers and delivery attempt;
   - reject unknown keys and all prohibited token, contact, storage, reason, cookie, header, and
     credential fields;
   - add unit tests for supported-version parsing, unknown-version rejection, and payload
     allowlisting.
3. Do not add a Supabase Queue provider call, polling loop, resident Worker behavior, renderer,
   Asset/token issuance, or Production host manifest in that unit.
4. Run focused tests and `pnpm verify`; no WCJ run is required unless a Web file changes.

## Required user-owned external setup

- Install or start Docker Desktop only to close the local reset/runtime pgTAP gate.
- No Queue credential, provider selection, or Production Worker host decision is required for the
  exact next step.
- Never place database passwords, Supabase keys, Queue credentials, or any secret in chat, Git,
  documentation, logs, or browser code.

## Session handoff protocol

Continue to follow the repository `AGENTS.md` Session handoff protocol: create a new
`docs/handoff-MMDD-HHmm.md` when a major feature unit completes or repeated errors make a fresh
session safer. Every handoff must record branch and commit, completed scope, validation evidence,
unresolved risks, exact next step, and required user-owned external setup, and must never contain
credentials or secret values.
