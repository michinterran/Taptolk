# Taptolk Session Handoff — 2026-07-19 11:46 KST

## 1. Handoff rule

Create `docs/handoff-MMDD-HHmm.md` when a major feature unit is complete or when
the same error begins repeating and a fresh session would reduce context and
token waste.

Every handoff must record the current branch and commit, completed scope,
validation evidence, unresolved risks, exact next step, and any required
user-owned external setup. Never include credentials or secret values. Carry
this rule into every later handoff document, then notify the user that it is a
safe checkpoint and recommend opening a new Codex session with the handoff path.

## 2. Project authority and working state

- Highest specification: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Engineering contract: `AGENTS.md`
- Working branch: `codex/phase-1-foundation`
- Completed feature commit: `7f68f4aceb9d67a5908a8bf89457b1decdfb91c6`
- Remote branch before handoff commit: `origin/codex/phase-1-foundation`
- Supabase environment: staging only
- Supabase project ref: `evpwzjkhfppdjivkyokh`
- Latest migration: `20260719023725_phase_1_parent_scope_visibility.sql`
- Working tree at handoff creation: clean before this handoff-only change

## 3. Completed feature unit

Authenticated Site CRUD and Tenant Isolation browser E2E is complete.

- Staging-only `pnpm e2e:staging:sites` command
- Linked-project and `APP_ENV=staging` safety guard
- Ephemeral Auth users, passwords, TOTP, Tenant hierarchies, and memberships
- No credential, TOTP, trace, screenshot, video, or HTML report persistence
- Super Admin real KO/EN journey:
  - create
  - operational update
  - contract vehicle limit update
  - suspend
  - reactivate
  - close
- Management Admin same-company visibility and operational update
- Site Admin exact-Site visibility and operational update
- Cross-tenant and sibling-Site hidden-field tamper submit rejection
- Audit action/actor/redaction evidence
- Success and failure cleanup with staging residue `0`

Real browser execution also found and fixed:

- invalid direct PostgREST `sites → tenants` relation
- descendant membership inability to read its own parent Tenant/Management Company
- missing server-only fixture DML privilege contract

Primary artifacts:

- `e2e/staging/staging-fixture.ts`
- `e2e/staging/site-crud-tenant-isolation.spec.ts`
- `playwright.staging.config.ts`
- `apps/web/admin/supabase-site-catalog-repository.ts`
- `supabase/migrations/20260719021938_phase_1_service_role_fixture_boundary.sql`
- `supabase/migrations/20260719023725_phase_1_parent_scope_visibility.sql`
- `docs/03-analysis/site-management-authenticated-e2e.analysis.md`
- `docs/04-report/site-management-authenticated-e2e.report.md`

## 4. Validation evidence

- `pnpm e2e:staging:sites`: 3 tests passed
- `pnpm verify`: passed using Node `v24.18.0` and Corepack `pnpm 10.34.5`
- Unit tests: 16 files, 71 tests passed
- WCJ: W 100, C 100, J 100, 47 files
- DB static contract: 15 migrations and 8 database tests
- Secret scan: 233 text files passed
- Immutable logo SHA-256 check: passed
- Next.js production build: passed
- Playwright Desktop/Mobile smoke: 26 tests passed
- Local/remote staging migration history: 15 entries aligned
- Final staging cleanup:
  - Tenant: 0
  - Management Company: 0
  - Site: 0
  - Admin Profile: 0
  - Site E2E audit row: 0

Use the project-pinned package manager:

```sh
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" corepack pnpm verify
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" corepack pnpm e2e:smoke
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" corepack pnpm e2e:staging:sites
```

Do not use the Codex bundled `pnpm 11`; the repository requires pnpm 10.

## 5. Remaining risks and external settings

- Do not claim Phase 1 complete until Supabase Local migration reset and local
  pgTAP runtime pass.
- Docker remains unavailable, so those two Local DB acceptance gates are open.
- WCJ and browser automation do not replace keyboard, screen-reader, computed
  contrast, responsive real-device, and real-journey manual review.
- Supabase Advisor authenticated `SECURITY DEFINER` RPC warnings remain an
  intentional command-boundary review item.
- Supabase leaked-password protection should be enabled before production
  readiness.
- MFA recovery and Admin idle-timeout policy remain open.
- The Site lifecycle request/approval entity is not implemented; request-only
  roles correctly cannot perform direct lifecycle mutations.

## 6. Exact next development step

Implement the Site lifecycle request/approval maker-checker vertical slice.

1. Add Plan and Design documents for a tenant-owned Site lifecycle request
   entity and explicit state machine.
2. Define request actions for suspend, reactivate, and close without widening
   current direct-mutation permissions.
3. Enforce requester/approver separation, role scope, AAL, optimistic version,
   terminal states, and same-transaction redacted audit.
4. Add reviewed request, approve, reject, and cancel command functions plus
   pgTAP policy/privilege tests.
5. Replace the current request-only notice with KO/EN request controls and an
   approval queue for authorized platform roles.
6. Extend staging Auth E2E so Management Admin and Site Admin can request only
   in scope, Super Admin can approve, and cross-tenant requests never render or
   mutate.
7. Run WCJ after each web change, then `pnpm verify`, Desktop/Mobile smoke, and
   staging authenticated E2E.

If Docker becomes available before or during this slice, run the pending Local
reset and pgTAP acceptance first and record the result without changing the
maker-checker scope.
