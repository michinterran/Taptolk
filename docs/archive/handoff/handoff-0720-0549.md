# Taptolk Cron-Deferred Web Deployment Handoff — 2026-07-20 05:49 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Cron-deferred implementation commit: `896904e`
  (`chore: defer production cron activation`)
- Previous operations audit commit: `4f8ab49`
- Authority: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Runbook: `docs/deployment/production-privacy-cleanup-cron-runbook.md`
- Decision: `WEB_DEPLOYED / CRON_DEFERRED / MANUAL_GATES_PENDING`

## Completed scope

- Preserved the approved `1..100` per-Batch QR quantity contract.
- Kept the authenticated aggregate-only privacy-cleanup GET route, deterministic tenant/hour
  request identity, bounded coordinator, database selector, cleanup ledger, and audit contract.
- Removed active Cron registration from the default `apps/web/vercel.json` so the current Hobby
  web project can deploy without an unsupported hourly expression.
- Preserved the exact `/api/internal/privacy-cleanup` and `0 * * * *` contract in the inert
  `apps/web/vercel.production-cron.template.json`.
- Added default, `--require-deferred`, and `--require-active` static verification modes.
- Updated the Production runbook so actual service launch requires a dedicated activation commit,
  hourly-capable plan, Production Supabase, Production secrets, and operator rehearsal.
- Created and linked the authorized Vercel `taptolk` project.
- Verified live project settings: Root Directory `apps/web`, framework Next.js, Node 24.x, and
  active Cron definitions 0.
- Configured only the existing staging environment names for Preview through the Vercel secret
  manager without displaying or copying any value into source, docs, logs, screenshots, or chat.
- Excluded local `.vercel` linkage metadata from Biome input while keeping it Git-ignored.
- Deployed commit `896904e` successfully:
  - stable fail-closed deployment: `https://taptolk.vercel.app`;
  - staging-data Preview: `https://taptolk-prvt4xdd5-michinterrans-projects.vercel.app`.

## Validation evidence

- `pnpm verify:production-cron`: PASS in deferred state.
- `pnpm verify:production-cron:deferred`: PASS.
- Complete linked pgTAP: all 22 database test files PASS; Cron contract 16/16 PASS.
- Authenticated staging E2E: 28 PASS with one intentional opt-in skip.
- `pnpm verify`: PASS.
- Lint: 320 files; typecheck: 19/19; unit: 53 files and 326 tests.
- Secret scan: 510 text files.
- WCJ: 100 / C100 / J100 / W100.
- Vercel builds: two READY deployments using Next.js 16.2.10 and Node 24.18.0.
- Stable URL: `/ko` 200, `/en` 200, `/api/health` 200.
- Stable unconfigured `/api/internal/privacy-cleanup`: 503 fail closed.
- Project Cron registry: zero active definitions.
- Preview unauthenticated access redirects to Vercel SSO, preserving the staging access gate.

## Unresolved risks

1. The stable public deployment is a fail-closed development surface, not actual-service
   Production approval. Production data and provider credentials are absent.
2. The staging-data Preview requires Vercel team authentication; an external share bypass was not
   created.
3. A separate Production Supabase project, Production domain, SMS/CAPTCHA providers and adapters,
   monitoring, alerting, rollback approver, incident owner, and pilot window remain undecided.
4. Actual-service Cron activation still requires Pro or Enterprise. Hobby cannot run the approved
   hourly expression.
5. Authorized aggregate-only Production run, same-hour replay, cleanup/audit duplicate 0, secret
   rotation, monitoring, and rollback rehearsal remain actual-service gates.
6. Representative iOS/Android, VoiceOver/TalkBack, computed contrast, and physical 85mm print
   review remain manual gates.

## Exact next step

Continue development and review against the deployed, fail-closed web surface. Use the protected
Preview only with an authorized Vercel team login when staging-backed behavior is required.

At actual service launch:

1. approve the Production domain, Production Supabase project and region, SMS/CAPTCHA providers
   and adapters, monitoring/incident/rollback owners, and pilot window;
2. move the linked Vercel project to Pro or Enterprise;
3. configure Production values only through the Vercel secret manager;
4. activate `apps/web/vercel.production-cron.template.json` into `apps/web/vercel.json` in a
   dedicated reviewed release commit;
5. run `pnpm verify:production-cron:active`, linked pgTAP, authenticated Production-safe E2E, and
   `pnpm verify`;
6. follow the runbook for an authorized aggregate-only run, same-hour replay, cleanup/audit
   duplicate 0, monitoring, rotation, and rollback rehearsal.

Do not change the QR quantity policy. If more than 100 items per Batch is requested, obtain a
separate quantity-policy design approval before changing schema, UI, Worker, Queue, or export
contracts.

## Required user-owned external setup

- No additional setup is required for the current fail-closed web deployment.
- Vercel team login is required to open the staging-backed Preview.
- Actual service launch requires the Production decisions listed above.
- Never send database passwords, service credentials, provider tokens, Cron bearers, encryption
  or HMAC keys, OTPs, activation codes, public QR tokens, cookies, authorization headers, phone
  numbers, or message bodies through Git, documentation, logs, screenshots, or chat.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and required user-owned external setup, without credentials or secret values.
