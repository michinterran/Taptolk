# Taptolk Session Handoff — 2026-07-24 20:57 KST

이 문서만 읽고 이어받는다. 역할: **Claude는 디자인 전담, Codex가 백엔드**(운영자 확정
2026-07-24). 순서·진행은 `docs/development/plan-design-completion-0724.md`.

## 1. Checkpoint

- 브랜치 `codex/phase-1-foundation` · HEAD **`851ad49`** · 원격 push 안 함(로컬만 ahead 12)
- Production 변경 없음, Cron 활성 **0** 유지, staging 스키마 변경 없음
- **⚠️ Codex가 같은 브랜치에서 병렬 작업 중.** 커밋 전 `git status`로 Codex의 미커밋
  변경을 확인하고 **내 파일만 `git add`** 한다(전체 add 금지)
- 검토용 갤러리 아티팩트 발행됨(scratchpad, 미커밋):
  https://claude.ai/code/artifact/14a40894-c6ab-4c40-ad30-6713c3c354da
  — 실제 토큰·CSS로 모바일·공개·콘솔을 한 페이지에 렌더한 리뷰. 새로 만들려면
  `packages/ui/src/styles/tokens.css` + `apps/web/app/globals.css`를 iframe별로 넣는 방식(§5 참고)

## 2. 먼저 읽어라 — 이 세션에서 굳어진 것

- **정본이 화면의 유일한 근거다.** `DESIGN_SYSTEM.md` + `docs/design-canon/`.
  handoff 산문을 설계 근거로 인용하지 마라(§6 권위 순서)
- **화면 완료 = 7개 조건**(`DESIGN_SYSTEM.md` §0). 그중 6번: **디자인은 스키마를
  기다리지 않는다.** 서버가 안 주는 값은 `docs/design-canon/CONTRACTS.md`에 계약으로
  선언하고 화면은 비운다(`—`)
- **게이트:** `corepack pnpm validate:design-system`(래칫, 현재 부채 **310**) +
  `validate:wcj`. 콘솔 부채를 줄이면 `--update-baseline`으로 갱신해야 통과
- **관리자 화면은 로그인 뒤라 Claude가 직접 못 본다.** 정적 정본(HTML)·정적 하네스로
  검증하고, 실화면 대조는 로그인 캡처가 필요

## 3. 이 세션에서 한 것

### 완료 (정본 + 검증)

| 영역 | 상태 |
|---|---|
| **모바일 19장** | **완료.** [A]활성화·[R]재진입·[B]호출자·[C]차주탭·[E]. 402px 브라우저 검증 |
| **QR 위자드 6단계** | 정본 `console-qr-wizard.html` 재구성, 1487px 검증. 슈퍼어드민 전용 |
| **사이트 운영 화면** | 정본 `console-site-operations.html` 신설(입고·재고·디자인보관), 1487px 검증 |
| **공개 타입 스케일** | 랜딩·온보딩·로그인 헤드라인 위계 `--tt-pub-*`로 통일(56›34›22›18), 하네스 검증 |

### Codex로 넘긴 지시서 (`docs/development/`)

- `workorder-codex-mobile-backend-0724.md` — 모바일 백엔드(활성화 코드 제거·재진입 등)
- `workorder-drop-activation-code-0724.md` — 활성화 코드 서버 제거
- `workorder-codex-qr-wizard-6step-0724.md` — 위자드 5→6단계 데이터·라우팅
- `workorder-codex-site-operations-0724.md` — 사이트 입고 엔드포인트

## 4. 이 세션 확정 사항 (되돌리려면 사용자에게 물어라)

| | 확정 |
|---|---|
| 활성화 코드 | **없앤다.** 스티커 소지가 자격, 폐기·재발급으로 되돌림 |
| 차주 전화번호 | **브라우저로 절대 안 보낸다.** 뒤 4자리도. `AGENTS.md` Security |
| QR 제작 | **슈퍼어드민 전용.** 제작·발주·배송까지, 입고는 받는 사이트가 |
| 위자드 | **6단계** 회사›사이트›디자인›수량›검토›제작·발주·배송 |
| `management_company_features` | QR엔 불필요(슈퍼어드민 고정) |
| 사이트 등록 | **슈퍼어드민 직접만.** 요청→승인 UI는 §11 해제 전 보류 |
| 데이터 리셋 | 픽스처 유지 + **플래그로 숨김**(이름 필터 금지) |
| 공개 톤 | **차분한 정리**(히어로 56px) |

## 5. Exact next step — 공개·인증 마무리

공개 헤드라인 위계는 끝났다. **남은 것(§6 진행표의 P3):**

1. **작은 캡션·배지 크기 정리** — 공개 블록에 0.65~1.05rem 일회성 크기가 남아 있다.
   **⚠️ 콘솔 블록과 리터럴을 공유하므로 전역 치환 금지.** 셀렉터별로 `--tt-pub-caption-size`
   (0.875rem)·`--tt-pub-eyebrow-size`(0.8125rem)에 맞춘다. `.landing-*`·`.onboarding-*`·
   `.admin-auth-*`·`.admin-portal-*` 블록만
2. **로그인·가입·MFA 카드** — 간격·상태(로딩·오류·비활성)를 스케일에 맞춰 다듬기.
   staging은 "로그인 준비 중"(Supabase 미연결)이라 시각만 보이고, 정적 하네스로 검증
3. 끝나면 `--update-baseline`로 줄어든 부채 갱신

**검증 방법:** 로컬 dev가 **Codex의 `owner/offline` JSON 버그로 500**이다(내 변경 아님).
그래서 정적 하네스로 검증했다 — `tokens.css` + `globals.css`(`@import "tailwindcss"` 제거)를
합쳐 서빙하고 공개 클래스로 HTML을 만들어 JS로 크기 측정. 스크래치패드 `pub/` 참고.

## 6. 사용자 결정 대기

- **주 버튼 3점 마크 에셋** — 모바일 primary 마크가 로고 일부라 크롭 금지. 별도 파일 필요
- **로그인 살리기** — staging `taptolk.vercel.app` 로그인이 "로그인 준비 중"으로 막힘
  (입력창·버튼 disabled). **진단:** Supabase는 살아 있음(REST가 401 응답=연결 가능),
  그런데 배포된 앱이 유효한 키로 연결을 못 함 = **Vercel 배포에 Supabase 환경변수가
  없거나 안 맞음.** Claude 변경 때문 아님(로그인 코드 안 건드림·push 안 됨).
  Claude가 못 고침(secret·Vercel 설정, §11). **필요한 변수 목록은
  `docs/deployment/staging-login-env-checklist.md`에 있음**(값 제외).
  운영자가 Vercel Settings에 채우고 Redeploy 해야 콘솔 실화면 검증이 가능해진다

## 7. 환경 함정

- **`typecheck`가 `.next`를 지운다.** 지운 뒤 dev가 `reused`면 전 페이지 500 →
  `rm -rf apps/web/.next` → `build:packages` → **preview_stop 후 재기동**
- 브라우저 패널이 숨겨지면 scroll·get_page_text가 30s 타임아웃. 스크린샷·JS는 됨
- 로컬 로그인 불가(Supabase 미설정). 공개 화면만 로컬/하네스로 봄

## 8. 검증 (HEAD `422a5fb`)

`validate:design-system` PASS(부채 **310**) · `validate:wcj` Total 100 · `lint` 430 clean ·
`test` **439**. **미실행:** `verify` 전체(build 포함), `e2e:staging`, pgTAP.

## 9. 별도 지시 없이 착수 금지

Production 배포·도메인·Cron 활성화·**live Kakao/SMS**·Production Supabase·Vercel 설정·
승인 워크플로 신설·**S7·S9 임계 임의 결정**·QR `1..100` 계약 변경·로고 크롭/재색·
**화면 디자인을 handoff 산문 근거로 변경**

## 10. 보고 형식

구현/자동검증/수동검증 대기를 구분. PASS에 명령·범위·수. credential·전화번호·OTP·
secret은 Git·문서·로그·스크린샷에 남기지 않는다.
