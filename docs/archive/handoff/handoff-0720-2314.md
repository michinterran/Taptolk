# Taptolk Task 0 and Public Landing Production Handoff — 2026-07-20 23:14 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Task 0 implementation commit: `1314532`
- Task 0 workorder reconciliation commit: `e95779e`
- Public copy and landing design commit: `173ffcd`
- Bounded Vercel source commit: `f022ca0`
- GitHub quality runs: `29748841968` PASS, `29749378649` PASS
- Production deployment: `dpl_DTvj4RjQ68MnVYAXQKR6KexiEEM7` READY
- Stable URL: `https://taptolk.vercel.app`
- Active Production Cron definitions: `0`
- Current stage: `DEVELOPMENT_TEST`
- Decision: `AUTOMATED_DEVELOPMENT_READY / EXTERNAL_PILOT_GATES_PENDING`

## Completed scope

- Restored and verified the `1..100` per-Batch quantity contract across PostgreSQL, Drizzle,
  Application policy, unit tests, and pgTAP without changing historical migrations.
- Preserved the approved 10x100 acceptance contract; no single 1,000-item Batch was introduced.
- Recorded the workorder 2.6 analysis-only findings without changing `render_jobs.requested_count`,
  `qr-engine/issuance.ts`, or `qr-engine/print-export.ts`.
- Refined all five KO/EN user-facing content dictionaries and the landing visual.
- Kept EN `caller`, removed user-facing internal-development wording, and did not localize QR
  enum/action labels.
- Added a home link to the Owner unavailable state through typed KO/EN copy.
- Added an explicit `.vercelignore` boundary so local build/cache/test artifacts and environment
  files cannot enter the deployment source.
- Deployed the verified public surface to Production with the Cron-deferred manifest.

## Validation evidence

- Clean local Supabase reset: PASS twice.
- Local pgTAP: 23 files / 590 tests PASS.
- Linked migration state: `20260720210000` present on the approved linked Staging project.
- Linked pgTAP: 23 files / 590 tests PASS through `pnpm db:test:linked`.
- Authenticated Staging E2E: 28 PASS in one run; one separate 10x100 acceptance opt-in skip.
- Local `pnpm verify`: lint 325 files, typecheck 19/19, unit 53 files/327 tests,
  DB 57 migrations/23 tests, secret scan 538 files, WCJ 100 over 92 files, build 11/11 PASS.
- GitHub Actions `29748841968`: PASS including Linux Chromium smoke.
- GitHub Actions `29749378649`: PASS; unit 327, DB 57/23, secret scan 539, WCJ 100/92,
  build and Chromium smoke PASS.
- Production deployment `dpl_DTvj4RjQ68MnVYAXQKR6KexiEEM7`: READY and stable alias active.
- Production routes: `/ko`, `/en`, both KO/EN customer and platform login routes, and
  `/api/health` return 200.
- Unauthenticated `/api/internal/privacy-cleanup` returns 503 fail-closed.
- Deferred Production Cron verifier: PASS; active definitions remain zero.

## Unresolved risks and gates

1. Production Supabase project and data region remain undecided.
2. Production SMS and CAPTCHA providers and adapters remain undecided.
3. Production secrets remain intentionally absent.
4. Monitoring, rollback, incident ownership, domain, and pilot window remain undecided.
5. Representative iOS/Android, VoiceOver/TalkBack, computed contrast, keyboard, and physical
   85mm print checks remain manual.
6. Actual-service Cron activation and any Vercel plan change remain deferred.
7. GitHub Actions emits a non-blocking Node 20 action-runtime deprecation warning for
   `actions/checkout@v4` and `actions/setup-node@v4` while the job runs on Node 24.

## Exact next development step

Implement workorder Task 1 only:

1. add a typed server-only stage policy in `packages/config`;
2. fail closed for missing or invalid stage configuration;
3. prove browser-controlled input cannot change the stage;
4. add an extensible Service test-surface leak guard and include it in `pnpm verify`;
5. run focused tests and `pnpm verify`, then create a new Task 1 handoff.

Do not start Task 2 or Task 3 until Task 1 passes. Do not choose a Production project, region,
Provider, Secret, plan, Cron schedule, or operations owner.

## User-owned external setup

- Approve a separate Production Supabase project and data region when ready.
- Select SMS and CAPTCHA providers before provider-adapter or secret configuration.
- Name monitoring, rollback, and incident owners before the pilot.
- Approve Pro or Enterprise only when actual hourly Cron activation is requested.

## Session handoff protocol

Create `docs/handoff-MMDD-HHmm.md` when a major feature unit is complete or when the same error
begins repeating and a fresh session would reduce context and token waste. Every handoff records
the current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and any required user-owned external setup. Never include credentials or secret values.
After writing the handoff, notify the user that this is a safe checkpoint and recommend opening a
new Codex session with the handoff path.
