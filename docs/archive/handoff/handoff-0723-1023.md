# Taptolk Session Handoff — 2026-07-23 10:23 KST

> 새 세션이 이 문서만 읽고 이어받을 수 있게 쓴 핸드오프다.
> 직전 세션에서 Claude가 **디자인 시스템을 직접 구현**했고, 이후 **Codex가 실제 앱 이식을 시작**했다.

---

## 1. Checkpoint

- 저장소: `/Users/benjaminsong/Documents/Taptolk`
- 브랜치: `codex/phase-1-foundation`
- HEAD: **`a858b17`** — `refactor web design system surfaces` (Codex 작업)
- 원격: `origin/codex/phase-1-foundation`과 **동기화됨**
- Production 배포: **변경 없음.** Production Cron 활성 정의 **0** 유지
- Working tree: 아래 보호 문서만 untracked (그대로 보존할 것)
  - `docs/deployment/kakao-alimtalk-onboarding-guide.md`
  - `docs/development/prd-public-surface-0721.md`
  - `docs/development/prd-public-user-admin-portals-0721.md`
  - `docs/development/workorder-kakao-alimtalk-live-provider.md`
  - `docs/handoff-0722-1034.md`, `docs/handoff-0722-1051.md`, `docs/legal/`
  - `.claude/` (dev 서버 launch 설정)

---

## 2. 협업 모델 (중요 — 기본값이 바뀌었다)

원래는 `Claude=리뷰·지시서 / Codex=구현`이었으나, 사용자 지시로 **디자인 시스템 작업은 Claude가 직접 구현**했다.
현재는 **Codex가 전체 리뉴얼 이식을 실행**하고 있고, Claude는 리뷰·방향·지시서·세부 조정을 맡는다.
사용자 방침: **세세한 수정은 전체 페이지를 다 만든 뒤 진행**한다.

---

## 3. 지금까지 완료된 것

### 3.1 Claude (커밋 `b442624` → `35b7574` → `2e3c9ca`)
- **`packages/ui/src/styles/tokens.css` 확장** — 타입 스케일, 상태 틴트 쌍(success/warning/danger/info/neutral/brand ×surface·fg),
  `--tt-color-info`, surface/border 레벨, elevation(sm/md), `--tt-radius-xs|pill`, `--tt-focus-ring`, z-index,
  그리고 **미정의였던 `--tt-space-8` 버그 수정**. 기존 26토큰 이름은 불변(214개 `admin-*` 클래스 의존).
- **QR 엔진 홀로그램 템플릿 완성** — `ROUND_BLUE_HOLOGRAM_V1`이 이름대로 이리데선트 패싯 면을 렌더
  (이전엔 단색 평면). 패싯은 중앙 흰 QR 패널 **바깥에만** 배치 → QR 대비 불변. 홀로그램 디코드 테스트 추가.
  템플릿 코드 4종 세트·흰 패널·taptolk 마크 좌표는 **불변**(DB CHECK 제약·테스트 계약).
- **브라우저 검증된 프로토타입 4종 + 실제 렌더 스티커 이미지** 작성
- **문서 4종** 작성 (§5)

### 3.2 Codex (커밋 `a858b17`)
- **`packages/ui`에 프리미티브 신설** — `renewal-primitives.tsx`(507줄) + 테스트, `address-field.tsx`(Daum 주소검색), `button.tsx` 확장, `index.ts` export 확장
- **범위 전환(Scope switcher) 백엔드** — `packages/application/src/admin-scope-navigation-service.ts` + 테스트,
  `apps/web/admin/supabase-admin-scope-navigation-repository.ts`
- **공통 셸 + 화면 다수 이식** — `admin-page-header`, `management-company-catalog-view`, `site-catalog-view`,
  `management-company-workspace-view`, `site-workspace-view`, `tenant-catalog-view`, `admin-directory-view`,
  `admin-account-approval-view`, QR 3종 뷰, `admin-login-screen`, 공개 랜딩 일부, `globals.css` +923줄
- **프로토타입 HTML 4종 삭제** (아래 §4 주의)

---

## 4. ⚠️ 새 세션이 먼저 알아야 할 것

**프로토타입 4종이 working tree에서 삭제되었다.** 시각 기준이 필요하면 커밋 `2e3c9ca`에서 복원한다:

```bash
git show 2e3c9ca:apps/web/public/_design-prototype.html > /tmp/proto-list.html
git show 2e3c9ca:apps/web/public/_design-prototype-pages.html > /tmp/proto-pages.html
git show 2e3c9ca:apps/web/public/_design-prototype-detail.html > /tmp/proto-detail.html
git show 2e3c9ca:apps/web/public/_design-prototype-qr-wizard.html > /tmp/proto-wizard.html
```

`apps/web/public/_proto/*.png`(실제 렌더 스티커 5장)은 **아직 남아 있다.** 참조 전용이며
**Production 배포 전 제거**해야 한다.

---

## 5. Source of truth 문서 (읽는 순서)

1. `AGENTS.md`, `CLAUDE.md`
2. **`docs/development/workorder-full-site-renewal-0722.md`** ← Codex 실행 메인 지시서 (화면별 상세 규격 포함)
3. `docs/development/design-system-reference-0722.md`
4. `docs/development/workorder-p1-design-system-0722.md`
5. `docs/development/workorder-admin-design-system-and-metrics-0722.md` (로드맵·제품 정의 D1~D10)

---

## 6. 확정 결정 (임의 변경 금지)

1. **브랜드/CI 원본 유지·재해석 금지.** 로고는 `/brand/taptolk-logo.png` 원본 그대로(재도안·재색·텍스트 치환 금지)
2. **관리사명은 목록에 실제 이름.** **`management_code`는 별도 필드**로 상세페이지에서만 표시·관리
3. **주소검색 = Daum(카카오) 우편번호**, 무료·키 불필요, **반드시 `embed` 모드**(팝업은 차단됨 — 검증 완료),
   좌표 미저장·주소 문자열만
4. **미니멀 라인 아이콘**(stroke SVG). 이모지 금지
5. **범위 전환기 = 슈퍼어드민 전용.** persona/역할 가장이 아니라 **본인 권한을 하위로 좁히는 것**.
   서버 인가 + RLS + 감사(실제 actor + 범위) 필수
6. **용어: `건강 상태` → `리스크 등급` / `Risk level`**, 타일 `건강한 고객` → `저위험 고객`
7. **QR 위자드 6단계 유지.** 효율은 단계 수가 아니라 각 단계 내부 UX에서
8. **QR 수량 UX**: 슬라이더 상한을 고정하지 말 것. `계약차량한도 × 1.5`에서 파생하고 입력이 크면 확장.
   권장 = `계약한도 − 미배정재고`. 스냅 힌트·배치 세그먼트·초과 경고(차단 아님). per-Batch **1..100 불변**
9. **동적 QR**: 재배정·교체는 재인쇄 불필요. 그러나 **호스트는 QR에 구워진다** → 인쇄 후 변경 불가.
   canonical 호스트 미확정 시 실인쇄용 생성은 **fail-closed 차단**
10. **없는 데이터를 지어내지 않는다.** 데이터 없으면 `—`/빈 상태

---

## 7. 검증 증거 (2026-07-23 10:23, HEAD `a858b17` 기준)

| 게이트 | 결과 |
|---|---|
| `corepack pnpm validate:wcj` | **PASS** — Total 100 / C100 / J100 / W100, 126 files |
| `corepack pnpm typecheck` | **PASS** — 19/19 tasks |
| unit (ui + application + qr-engine) | **PASS** — 31 files / 157 tests |

**아직 실행하지 않음**: `corepack pnpm verify` 전체, `db:test:linked`, `e2e:staging`,
인증 상태 시각 검증, 반응형 320/768/1280/1920 수동 확인.
→ 화면 완료 보고 전 반드시 `verify` 통과시킬 것.

---

## 8. 남은 작업

### 8.1 화면 이식 (Codex 진행 중)
Codex가 손대지 않은 것으로 보이는 화면 — 프로토타입 `_design-prototype-pages.html` 기준으로 정렬 필요:
- **대시보드** (`admin-dashboard-view.tsx`) — 6지표 + 추이 차트 + 조치 필요 큐
- **운영 모니터링** (`operations-dashboard-view.tsx`) — 세션 테이블(개인정보 미표시) + 알림 전달 상태(Provider 접수 기준)
- **리포트** (`admin-analytics-view.tsx`)
- **매출 관리** (`admin-revenue-view.tsx`) — 전망 버전 표기 유지
- **관리자 프로필** (`admin-profile-view.tsx`)
- **공개 랜딩 전체 정렬** — 일부만 반영됨. 공개 표면 제약(관리자 링크 0, noindex, WCJ `J005`) 유지
- **QR 위자드 수량 단계** — 지시서 §6.6.1 상세 UX가 반영됐는지 확인 필요

> ⚠️ CSS 차트 함정: 막대 컨테이너를 `display:grid; align-content:end`로 하면 자식 `height:%`가 해석되지 않는다.
> **`display:flex; flex-direction:column; justify-content:flex-end`** 를 쓸 것.

### 8.2 스키마 슬라이스 (미착수)
각각 migration + repository + service + RBAC + RLS + pgTAP + UI:

| # | 항목 | 상태 |
|---|---|---|
| S1 | `management_code` (관리회사·사이트) | 착수 가능 |
| S2 | `address`(우편번호·도로명/지번·상세) | 착수 가능 (UI는 `address-field.tsx` 완료) |
| S3 | 계약 시작/종료일 → D-day·만료 임박(30일) | 착수 가능 |
| S4 | 플랜 등급 STANDARD/BUSINESS/ENTERPRISE | 착수 가능 (표시용 라벨, 과금 없음) |
| S5 | 차량 용량 사용률 = 활성 바인딩 ÷ 계약한도 | 착수 가능 |
| S6 | QR 활성화율 = 활성 ÷ 발급총량 | 분모 소스 확인 필요 |
| S7 | 응답 품질 버킷 | **🚫 임계 미확정 — 사용자 확인 후** |
| S8 | 미해결 건수 | 기존 operations aggregate의 company scope 재사용 가능성 먼저 확인 |
| S9 | **리스크 등급** 스코어 룰 | **🚫 룰 미확정 — 사용자 확인 후** |
| S10 | 현실적 합성 데모 시드 | 착수 가능. **E2E 결정적 픽스처 불변**, 별도 시드 |

---

## 9. 사용자 결정 대기 (임의로 정하지 말 것)

1. **응답 품질 임계**(S7) — 예: 좋음 ≥85% / 보통 60–85% / 낮음 <60% (제안값, 미확정)
2. **리스크 등급 룰**(S9) — 어떤 지표 조합으로 위험/주의/양호/보통을 판정할지
3. **canonical QR 호스트** — apex `taptolk.com` vs `www`. **실물 스티커 인쇄 전 필수**
   (`PUBLIC_QR_BASE_URL`이 QR 이미지에 구워져 인쇄 후 변경 불가)
4. **스티커 하단 taptolk 마크 색상** — 첨부 실물은 흰색(녹아웃) 버전이나, 현재 렌더는 원본 컬러.
   재색은 금지이므로 **승인된 화이트/모노 브랜드 자산이 있으면 제공** 필요. 없으면 현행 유지
5. **랜딩·로그인 시안** — 프로토타입 없이 Codex가 디자인 시스템으로 설계 중. 완성 후 검토 필요

---

## 10. Exact next step

1. `git status`, `git log --oneline -5`로 실제 상태를 다시 확인한다 (Codex가 추가 커밋했을 수 있음)
2. `docs/development/workorder-full-site-renewal-0722.md` §10 실행 순서를 이어간다:
   **남은 관리자 화면(대시보드·운영·리포트·매출·프로필) → 공개 랜딩·로그인 → 스키마 슬라이스**
3. 필요 시 §4 명령으로 프로토타입을 복원해 시각 기준으로 대조한다
4. 웹 변경마다 `corepack pnpm validate:wcj`, 화면 완료 전 `corepack pnpm verify`
5. 전체 화면이 끝난 뒤 **사용자와 세부 수정 라운드**를 진행한다 (사용자 방침)
6. 마무리 시 `apps/web/public/_proto/` 제거 + 최종 `verify` + 새 handoff 작성

---

## 11. 별도 명시 지시 없이 착수 금지

Production 배포 · `taptolk.com` 도메인 연결 · Cron 활성화 · live Kakao/SMS/CAPTCHA Provider ·
Production Supabase 프로젝트/리전/Secret · Enterprise SSO · Vercel 요금제 변경 · 담당자 지정 ·
Task 3 access grant / Test Lab / persona · **승인 워크플로 신설**(레퍼런스의 "승인 대기" 큐) ·
**S7·S9 임계·룰 임의 결정** · **canonical QR 호스트 확정** · QR `1..100` per-Batch 계약 변경.

---

## 12. 보고 형식

**구현 완료 / 자동 검증 완료 / 수동 검증 대기**를 구분한다. PASS 주장에는 명령·범위·테스트 수를 붙인다.
"Production"은 실제 Production에 배포·검증된 경우에만 쓴다.
credential·전화번호·OTP·토큰·secret은 Git·문서·로그·스크린샷에 남기지 않는다.
