# Taptolk Design Renewal Handoff — 2026-07-23 10:24 KST

새 세션이 이 문서만 읽고 바로 이어받을 수 있도록 현재 커밋, 검증, 배포, 남은 작업을 정리한다.
이 문서는 `docs/handoff-0723-1023.md` 이후 상태를 반영한 최신 handoff다.

## Checkpoint

- 저장소: `/Users/benjaminsong/Documents/Taptolk`
- 브랜치: `codex/phase-1-foundation`
- HEAD: `a858b17` — `refactor web design system surfaces`
- 원격 상태: `origin/codex/phase-1-foundation`과 동기화됨
- Preview 배포: 완료, READY
  - URL: `https://taptolk-m5xu5qkyc-michinterrans-projects.vercel.app`
  - Inspect: `https://vercel.com/michinterrans-projects/taptolk/FVpyXJKLoHrBtn8j36NKemMd9NQM`
  - Deployment ID: `dpl_FVpyXJKLoHrBtn8j36NKemMd9NQM`
- Production 배포: 변경 없음
- Production Cron: 활성 정의 0 유지. deferred verifier 통과
- Working tree: 구현 코드는 커밋 완료. 아래 untracked 파일은 사용자/타 작업 소유 가능성이 있으므로 임의 삭제·커밋 금지
  - `.claude/`
  - `docs/deployment/kakao-alimtalk-onboarding-guide.md`
  - `docs/development/prd-public-surface-0721.md`
  - `docs/development/prd-public-user-admin-portals-0721.md`
  - `docs/development/workorder-kakao-alimtalk-live-provider.md`
  - `docs/handoff-0722-1034.md`
  - `docs/handoff-0722-1051.md`
  - `docs/handoff-0723-1023.md`
  - `docs/legal/`

## Completed scope in commit `a858b17`

- Shared design-system primitives added in `packages/ui`
  - `renewal-primitives.tsx`
  - `renewal-primitives.test.tsx`
  - `address-field.tsx`
  - `button.tsx` and `index.ts` exports updated
- Admin scope navigation application slice added
  - `packages/application/src/admin-scope-navigation-service.ts`
  - `packages/application/src/admin-scope-navigation-service.test.ts`
  - `apps/web/admin/supabase-admin-scope-navigation-repository.ts`
  - `packages/application/src/index.ts` export updated
- Admin/public screens moved toward the confirmed design system
  - public landing `/[locale]`
  - admin login `/[locale]/admin/login`
  - shared admin page header and scope controls
  - management-company catalog/detail
  - site catalog/detail
  - tenant catalog
  - account approval and admin directory
  - QR batch/progress/inventory sample/assignment views
- Terminology cleanup
  - `건강 상태` replaced with `리스크 등급 / Risk level`
  - `건강한 고객` concept moved to `저위험 고객`
- Static HTML design prototypes removed from production public routes
  - `apps/web/public/_design-prototype.html`
  - `apps/web/public/_design-prototype-detail.html`
  - `apps/web/public/_design-prototype-pages.html`
  - `apps/web/public/_design-prototype-qr-wizard.html`
- `packages/ui/src/styles/tokens.css` formatted so repository lint passes.

## Validation evidence

All evidence below was run on 2026-07-23 KST at HEAD `a858b17`.

- `corepack pnpm verify`: PASS
  - `corepack pnpm lint`: PASS, 388 files
  - `corepack pnpm typecheck`: PASS, 19/19 tasks
  - `corepack pnpm test`: PASS, 62 files / 396 tests
  - `corepack pnpm db:check`: PASS, 65 migrations / 29 database tests
  - `corepack pnpm verify:secrets`: PASS, 654 text files scanned
  - `corepack pnpm verify:logo`: PASS, immutable logo hash verified
    `971b7d919208f172a96dbc25c8ec9f641fc01522a1aa7a35fc90feb86f2e546f`
  - `corepack pnpm verify:service-stage`: PASS, SERVICE fail-closed boundary verified
  - `corepack pnpm verify:production-cron`: PASS, deferred active manifest and credential-free hourly Production template verified
  - `corepack pnpm validate:wcj`: PASS, Total 100 / C100 / J100 / W100, 126 files
  - `corepack pnpm build`: PASS, Next build generated 51 static pages and dynamic admin/public routes
- Vercel Preview deploy from a clean detached worktree at `a858b17`: PASS
  - READY URL: `https://taptolk-m5xu5qkyc-michinterrans-projects.vercel.app`
  - Vercel remote build: PASS
- Smoke network check:
  - `/ko` and `/ko/admin/login` route requests hit Vercel Deployment Protection first, then Vercel login.
  - This confirms the deployment is protected before app access; it is not a Taptolk app error.

## Preview access status

- The Preview deployment is protected by Vercel Authentication / Deployment Protection.
- Team members can access after Vercel login and 2FA setup.
- External unauthenticated testers will be redirected to Vercel login unless a Vercel Shareable Link is created or Deployment Protection settings are changed.
- Codex attempted to create a temporary share URL through the Vercel connector, but it failed with `403 Forbidden`.
- Do not disable Deployment Protection or change Vercel project/team/security settings without explicit user approval.
- Local Vercel CLI is outdated: `56.4.1`. Vercel remote build used `56.5.0`.
  - Recommended before next deploy: `pnpm add -g vercel@latest` or `npm i -g vercel@latest`.

## Source of truth to read first in the next session

Read in this order:

1. `AGENTS.md`
2. `CLAUDE.md`
3. `docs/development/workorder-full-site-renewal-0722.md`
4. `docs/development/design-system-reference-0722.md`
5. `docs/development/workorder-p1-design-system-0722.md`
6. `docs/development/workorder-admin-design-system-and-metrics-0722.md`
7. `packages/ui/src/styles/tokens.css`
8. `packages/qr-engine/src/render.ts`
9. this handoff

## Confirmed rules that must not be reinterpreted

- Brand/CI source is immutable. Use `/brand/taptolk-logo.png` as-is. No recolor, redraw, crop, mask, filter, text replacement, or logo extraction.
- Management-company list shows the real management-company name.
- `management_code` is a separate field and belongs in detail/edit surfaces, not as a replacement for company name.
- Address search uses Daum/Kakao Postcode in embed mode, not popup. Store address strings only; do not invent geocoding fields.
- Use minimal line icons. No emoji icons in admin/public UI.
- Scope switcher is super-admin only and narrows the current actor's authorized scope. It is not persona switching.
- Authorization remains server-side RBAC + PostgreSQL RLS + redacted audit.
- Use `리스크 등급 / Risk level`; do not reintroduce `건강 상태`.
- QR quantity remains 1..100 per Batch.
- Production Cron remains 0/deferred unless explicitly approved.
- Do not fabricate missing metrics. If schema/data does not exist yet, show honest empty state or `—`.

## Prototype/reference status

- Static HTML prototypes were removed from `apps/web/public` in `a858b17`.
- If visual comparison is needed, restore temporary copies from the previous commit, outside production public routes:

```bash
git show 2e3c9ca:apps/web/public/_design-prototype.html > /tmp/taptolk-prototype-shell.html
git show 2e3c9ca:apps/web/public/_design-prototype-pages.html > /tmp/taptolk-prototype-pages.html
git show 2e3c9ca:apps/web/public/_design-prototype-detail.html > /tmp/taptolk-prototype-detail.html
git show 2e3c9ca:apps/web/public/_design-prototype-qr-wizard.html > /tmp/taptolk-prototype-qr-wizard.html
```

- `apps/web/public/_proto/` still contains reference QR/sticker images:
  - `BRANDED.png`
  - `SQUARE_DARK_PREMIUM_V1.png`
  - `ROUND_WHITE_MINIMAL_V1.png`
  - `ROUND_BLUE_HOLOGRAM_V1.png`
  - `ROUND_PURPLE_GRADIENT_V1.png`
- These are reference-only public assets. Remove or relocate them before any Production release unless the user explicitly approves keeping them public.

## Unresolved risks and manual acceptance

- Operator visual acceptance is still pending for the full renewed site.
- Preview is deployed but protected; external testing requires Vercel team login or a Shareable Link created by a user with the right Vercel permission.
- The latest visible complaint was that public landing/admin login initially looked unchanged. Codex then made visible changes and redeployed, but final human visual acceptance is still pending.
- Authenticated admin flows were not manually walked through on the deployed Preview in this handoff because Deployment Protection blocked unauthenticated HTTP/screenshot checks.
- Responsive/accessibility manual review remains pending:
  - 320 / 768 / 1280 / 1920 widths
  - keyboard navigation
  - screen reader pass
  - computed contrast
  - real admin journey with authorized account
- Schema slices for real operational metrics are not complete. Do not invent metrics in UI.

## Exact next development step

1. In the new session, run:

```bash
cd /Users/benjaminsong/Documents/Taptolk
git status --short --branch
git log --oneline -5
```

2. Confirm:
   - branch is `codex/phase-1-foundation`
   - HEAD is `a858b17`
   - branch is synchronized with `origin/codex/phase-1-foundation`
   - only protected untracked docs/folders remain

3. Have the user open the Preview URL after completing Vercel login/2FA:
   - landing: `https://taptolk-m5xu5qkyc-michinterrans-projects.vercel.app/ko`
   - admin login: `https://taptolk-m5xu5qkyc-michinterrans-projects.vercel.app/ko/admin/login`

4. If the user wants external testers without Vercel login, ask them to create a Vercel Shareable Link or approve changing Deployment Protection. Do not assume permission.

5. Continue the renewal in this order from `docs/development/workorder-full-site-renewal-0722.md` §10:
   - remaining admin screens: dashboard, operations, reports, revenue, profile
   - public landing and admin/company login polish after all pages are present
   - QR wizard quantity UX confirmation
   - schema slices for real metrics
   - remove/relocate `apps/web/public/_proto/` if Production release approaches

6. After every web change:

```bash
corepack pnpm validate:wcj
```

7. Before reporting a screen or phase complete:

```bash
corepack pnpm verify
```

8. Always report the three statuses separately:
   - implementation complete
   - automated verification complete
   - manual/operator acceptance pending or complete

## Do not do without explicit instruction

- Production deployment or Production promotion
- Domain connection or canonical QR host decision
- Production Cron activation
- Live Kakao/SMS/CAPTCHA provider setup
- Production Supabase project/region/secret selection
- Vercel plan/security/team setting changes
- Enterprise SSO
- QR `1..100` per-Batch contract changes
- Risk-level scoring rule invention
- Response-quality threshold invention
- Committing or deleting protected untracked docs/folders

## Safe restart prompt for the new session

Use this prompt in a fresh Codex session:

```text
docs/handoff-0723-1024.md를 읽고 Exact next development step부터 진행해.
디자인 시스템은 재발명하지 말고 확정 문서와 커밋 a858b17 상태를 기준으로 이어가.
구현/자동검증/수동검증 대기를 구분해서 보고하고, 웹 변경마다 corepack pnpm validate:wcj,
화면 완료 전 corepack pnpm verify를 실행해.
```

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next step,
and required user-owned external setup. Never include credentials, OTPs, phone numbers, QR/activation
materials, cookies, tokens, authorization headers, provider secrets, or secret-like values.
