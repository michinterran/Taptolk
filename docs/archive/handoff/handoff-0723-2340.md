# Taptolk Session Handoff — 2026-07-23 23:40 KST

이 문서만 읽고 이어받는다. 이전 문서는 `docs/handoff-0723-1745.md`.

## 1. Checkpoint

- 브랜치 `codex/phase-1-foundation` · HEAD **`5086eea`** · 원격 push 안 함
- Production 변경 없음, Cron 활성 **0** 유지
- staging DB **스키마 변경 없음.** 데모 데이터만 추가(§3)

## 2. ⚠️ 먼저 읽어라 — 이번 세션에서 바뀐 규칙

**`DESIGN_SYSTEM.md`와 `docs/design-canon/`이 화면의 유일한 정본이다.**
handoff·워크오더·PRD의 산문을 **설계 근거로 인용하지 마라.** 그렇게 해서 이미 컴펌된
시안과 다른 화면이 여러 번 만들어졌다. `AGENTS.md`에 권위 순서를 명문화했다.

**화면 작업 전에 정본을 띄운다:**
```bash
cd docs/design-canon && python3 -m http.server 8907
```
`reference-1-admin-console.png`, `pwa/*.png`는 이미지로 연다.

**게이트 2개를 모두 통과해야 끝난 것이다:**
```bash
corepack pnpm validate:design-system   # 토큰 드리프트·정본 존재·카드인카드
corepack pnpm validate:wcj             # KO/EN·접근성·여정 (레이아웃은 검사 안 함)
```
`validate:design-system`은 **래칫**이다. `config/design-system-baseline.json`이 기존
부채 **313건**을 기록하고, 신규 위반은 즉시 실패하며 목록은 줄어들 수만 있다.
줄었으면 `--update-baseline`으로 갱신해야 통과한다.

## 3. 이번 세션 커밋 (14건, `8de883d` → `5086eea`)

| 커밋 | 내용 |
|---|---|
| `8de883d` | **S10 합성 데모 시드** `scripts/seed-demo.mjs` |
| `adb784e` | 테이블·타이포·사이드바 토큰화 (레퍼런스1 실측) |
| `42f05cc` | 제작 추적 보드 + 승인 큐 요약 |
| `57bfb22` | 토큰 계약 문서화(§5.2) |
| `c56af55` | **정본을 저장소로 + 게이트 신설** |
| `78b3e72` | QR 흐름 5단계 확정 |
| `5c5a21b` | 콘솔 페이지 프리미티브 |
| `12a3933` | **위자드 5단계 복귀**(3구역 폐지) |
| `a983b96` | 제작 추적 = 세로 5단계 트랙 |
| `e8392e3` | 위자드 패널을 정본 컨트롤로 |
| `999dfae` | 각 단계에 맞는 내용 배치 |
| `93e5702`~`5086eea` | 콘솔 계층 기획 + PWA 기획 |

## 4. 확정 사항 (되돌리려면 사용자에게 물어라)

| | 확정 |
|---|---|
| QR 흐름 | **5단계** 한 페이지 — 현장·디자인·수량·검토승인·제작입고. `?section=` 금지 |
| 권한 | 구역 분리 대신 **단계 잠금** |
| 알림 채널 | **카카오 알림톡. SMS 미사용**(알림톡 불가 시 대체 가능하나 알림톡 먼저) |
| 알림톡 본문 | **링크 + 기본 문구만.** 호출자 문구는 안 실음 |
| PWA | 시안 4장 그대로. **모바일 웹이 구현 기준**, PWA는 향상 |
| 주민등록번호 | **제외** |
| 스티커 | **1장 = 차량 1대** |
| 활성화 | 사이트 **선택 없음**. 주소 보여주고 확인 → 차량번호 → 전화번호 인증 |
| 재진입 | 차량번호 → 전화번호 인증, **둘 다 바인딩과 일치**해야 통과 |
| 에스컬레이션 | 180초(보고서 §09) 후 호출자 화면에 관리사무소 `tel:` 버튼 |
| 재호출 제한 | **슈퍼어드민 설정.** 코드에 기본값 박지 않음 |
| 멘트 더 보기 | 도입. 카탈로그에서, 고정 순서 |

## 5. 사용자 결정 대기 (콘솔)

| | 항목 |
|---|---|
| **A** | 위자드 5→**6단계**(관리회사 선택 추가) |
| **B** | `management_company_features` 신설 — 관리회사별 기능 토글 |
| **C** | 고객관리 리셋 — **화면에서 `e2e-` 필터**(권장) vs staging 삭제 |
| **E** | 사이트 등록 = 직접 vs 요청·승인 (`site:create`와 `site:create-request`가 둘 다 있음) |

## 6. Exact next step

**`docs/design-canon/pwa/README.md`대로 차주 화면 구현.** 순서:

1. 모바일 토큰 — 흰 카드 스택, 브랜드색 대형 차량번호, 화면당 primary 1개, 하단 탭.
   **콘솔처럼 토큰 먼저 세우고 화면을 얹는다.** 게이트 셀렉터도 같이 등록
2. 스캔 진입 라우팅 — 서버가 자산 상태로 [A]/[B]/[C]/[E] 분기
3. [A] 활성화 3단계 + 재진입
4. [C] 탭 4개 · [E] 사용 불가

**스키마는 이미 다 있다. 새 테이블을 만들지 마라** — `owners`, `owner_devices`,
`vehicles`, `qr_bindings`, OTP 테이블이 Phase 5에 있다. 저장 계약은
`docs/design-canon/pwa/README.md` §2 "서버가 저장하는 것" 표.

## 7. 🔴 미해결 결함

1. **`renderSticker`가 특정 payload에서 자기 출력을 decode 못 한다.**
   `.../s/DEMO0000DLVR`는 템플릿 2종에서 실패. 결정론적. 실제 생성에서도 일부 자산이
   `QR_DECODE_MISMATCH`로 실패할 수 있다. 재현은 `docs/handoff-0723-1745.md` §7
2. **샘플 미리보기 404** — `authenticated`에 `qr_batch_samples` SELECT 권한이 없어
   `/api/admin/qr-samples/[id]`가 항상 실패. security-definer RPC 마이그레이션 필요
   (staging DB 변경 → 승인 대기)
3. **디자인 보관 기능에 화면이 없다** — 위자드에서 목록을 뺐고 사이트 운영 화면이 없다.
   권한·서버 액션은 살아 있다
4. `operations-dashboard-view.tsx`가 단위 `"s"` 하드코딩 → KO에서 "12.3s"

## 8. 환경 함정 (반복해서 겪음)

- **`.next`를 지운 채 dev 서버가 `reused`로 잡히면 전 페이지 500.**
  `rm -rf apps/web/.next` → `corepack pnpm build:packages` → **preview_stop 후 재기동**
- `corepack pnpm typecheck`가 `.next`를 지운다
- 워크스페이스 패키지를 고치면 `build:packages` 전에는 dev 서버가 옛 dist를 본다
- **PDF는 `pypdf`로 읽힌다.** poppler 불필요

## 9. 검증 (HEAD `5086eea`)

`lint` 397 · `typecheck` 19/19 · `test` 423 · `validate:design-system` PASS(부채 313) ·
`validate:wcj` Total 100 · `verify:secrets` 674 · 위자드 전 경로 200.
**미실행**: `corepack pnpm verify` 전체(build 포함), `e2e:staging`, 로컬 pgTAP.

## 10. 데모 시드

```bash
TAPTOLK_STAGE=DEVELOPMENT_TEST corepack pnpm seed:demo
```
멱등. `7a9704b1-0d00-4000-8000-…` 범위를 소유. E2E 픽스처 무변경.

## 11. 별도 지시 없이 착수 금지

Production 배포 · 도메인 연결 · Cron 활성화 · **live Kakao/SMS Provider** ·
Production Supabase · Enterprise SSO · Vercel 설정 · 담당자 지정 ·
Task 3 access grant/Test Lab/persona · 승인 워크플로 신설 · **S7·S9 임계 임의 결정** ·
QR `1..100` 계약 변경 · 보호 대상 untracked 문서 커밋/삭제

## 12. 보고 형식

구현 완료 / 자동 검증 완료 / 수동 검증 대기를 구분한다. PASS 주장에 명령·범위·수를 붙인다.
credential·전화번호·OTP·토큰·secret은 Git·문서·로그·스크린샷에 남기지 않는다.
**확인 요청 시 링크를 함께 준다.** 관리자 화면은 로그인이 필요하므로 사용자 캡처로 대조한다.
