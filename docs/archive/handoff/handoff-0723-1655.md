# Taptolk Session Handoff — 2026-07-23 16:55 KST

이 문서만 읽고 이어받을 수 있게 작성한다. 이전 문서는 `docs/handoff-0723-1147.md`.

## 1. Checkpoint

- 저장소: `/Users/benjaminsong/Documents/Taptolk`
- 브랜치: `codex/phase-1-foundation`
- HEAD: **`1af1c57`**
- 원격 push: **하지 않음.** 로컬 커밋만 존재
- Production 배포: 변경 없음. Production Cron 활성 정의 **0** 유지
- staging DB: `20260723120000_taptolk_platform_direct_management.sql` **적용됨** (사용자 승인 후)
- 로컬 Supabase: 기동 중, migration 66개 적용, pgTAP 29 files / 636 tests PASS

## 2. 이번 세션에서 가장 중요한 교훈 (반드시 읽을 것)

**화면을 하나씩 고치는 접근은 실패했다.** 사용자가 여러 차례 "왜 디자인시스템을 안 따르냐"고
지적했고, 원인은 화면이 아니라 **프리미티브와 CSS 규칙**에 있었다.

globals.css를 파싱해보니 **68개 규칙이 각자 카드를 정의**하고 있었다:
padding `0.85rem`/`1rem`/`1.1rem`/`1.25rem`, radius `0.8rem`/`1rem`/`2rem`.
이 상태에서는 한 화면을 고쳐도 다음 화면이 그대로다.

→ **새 세션은 반드시 규칙·토큰 레벨에서 작업할 것.** 화면별 CSS를 개별 수정하지 말 것.

## 3. 이번 세션 커밋 (14건, `9c5a1b8` → `1af1c57`)

### 3.1 QR 제작 (발주 구역)
- `9c5a1b8` 발주 구역에서 재고·CSV·배정 블록 제거(section 가드 누락이었음), 중복 제목 제거
- `06a3ad4` **발주를 `?step=design|quantity` 2페이지로 분할.** 이음매는 도메인이 갈라지는 지점
  (1·2단계=디자인 생성 액션, 3·4단계=배치 요청 액션)
- `e4536d0` 브랜드 로고 업로드에 전용 폼 카드 부여(목록/승인 카드 스타일을 폼에 쓰고 있었음)

### 3.2 정직성 (발명된 수치 제거)
- `0faa2f6` 대시보드: 분모 0을 1로 강제해 **"전송 품질 100%"**·**"안정 100%"** 로 보이던 것 제거.
  `percent()`가 `null` 반환 → UI가 `—`. **사이트별 health 컬럼 삭제**(S9 미확정 침범, 임계 하드코딩)
- `5c1667f` 리포트: 요청 해결률·전달률·에스컬레이션률 동일 처리. 80/50 톤 임계 제거

### 3.3 디자인 시스템 (핵심)
- `0e4a9b4` `.tt-button`이 시안과 어긋나 있던 것 정렬(3rem/750 → 2.5rem/600)
- `d944456` **버튼을 토큰 계약으로** — `--tt-button-*` 9개 신설, 문서 §5.1
- `dfdcf13` **`.tt-stat-tile`이 `.tt-side-card`와 카드 스타일 공유**하던 것 분리.
  스트립이 카드가 되고 타일은 셀 + 구분선 (박스-인-박스의 근원)
- `70f1c33` **레이아웃 토큰 + 단일 콘솔 카드 규칙** 신설. 문서 §5.0
- `8c16697` 지표 스트립에 섹션 여백 누락분 추가, card-radius `1.25rem → 0.875rem`
- `b53ddeb` `.tt-data-table-wrap`/`.tt-filter-bar`/`.tt-empty-state`가 패널 안에서 프레임 그리던 것 제거
- `1af1c57` **콘솔 폭** — 셸마다 76rem/1280px/88rem로 각자 중앙정렬하던 것을 토큰으로 통일

## 4. 확정된 디자인 시스템 규칙

`docs/development/design-system-reference-0722.md` **§5.0(레이아웃)·§5.1(버튼)** 이 계약이다.

| 토큰군 | 위치 |
|---|---|
| `--tt-card-padding` / `-radius` / `-gap`, `--tt-section-gap` | `packages/ui/src/styles/tokens.css` |
| `--tt-console-max-width` / `-gutter` | 같은 파일 |
| `--tt-button-*` (9개) | 같은 파일 |
| 콘솔 표면 규칙 블록 | `apps/web/app/globals.css` **최하단** |

**작업 원칙 3가지:**
1. 새 관리자 패널 → globals.css 최하단 블록에 **셀렉터만 추가**. 카드 스타일 재작성 금지
2. 값 변경 → **토큰을 바꾼다.** 콘솔 전체가 함께 움직인다
3. **카드 안에 카드 금지.** 구분이 필요하면 헤어라인

## 5. 검증 증거 (HEAD `1af1c57`)

| 게이트 | 결과 |
|---|---|
| `corepack pnpm typecheck` | PASS 19/19 |
| `corepack pnpm lint` | PASS 391 files |
| `corepack pnpm test` | PASS 63 files / 414 tests |
| `corepack pnpm validate:wcj` | PASS Total 100, 127 files |
| `corepack pnpm db:check` | PASS 66 migrations / 29 database tests |
| 로컬 pgTAP | PASS 29 files / 636 tests |
| `db:test:linked` (staging) | PASS 12 files / 360 tests |

**아직 실행하지 않음**: `corepack pnpm verify` 전체(build 포함), `e2e:staging`.

## 6. 시각 검증 상태 — 중요

- Claude는 **자격증명을 입력하지 않으므로 관리자 화면에 로그인할 수 없다.**
  이번 세션의 모든 콘솔 변경은 **자동 검증만 통과**했고, 사용자가 스크린샷을 보내주는 방식으로 확인했다.
- 웹앱 `.env.local`은 **staging**(`evpwzjkhfppdjivkyokh`)을 본다. 로컬 DB에는 관리자 계정이 없다.
- 확인 요청 시 **반드시 링크를 함께 줄 것.** (사용자 지적사항)

```
http://localhost:3000/ko/admin/reports
http://localhost:3000/ko/admin/platform
http://localhost:3000/ko/admin/operations
http://localhost:3000/ko/admin/platform/revenue
http://localhost:3000/ko/admin/accounts
http://localhost:3000/ko/admin/qr-inventory
http://localhost:3000/ko/admin/qr-inventory?step=quantity
http://localhost:3000/ko/admin/qr-inventory?section=approvals
http://localhost:3000/ko/admin/qr-inventory?section=tracking
```

## 7. 환경 함정 (반복해서 겪음)

- **`corepack pnpm typecheck`는 `.next`를 삭제한다**(`scripts/clean-next-output.mjs`).
  dev 서버 실행 중 돌리면 서버가 500으로 깨진다. → **typecheck 후 dev 서버를 반드시 재시작**한다.
- dev 서버는 `preview_start`(name: `taptolk-web`)로 띄운다. `.claude/launch.json`에 `autoPort: true`.
- Docker Desktop이 켜져 있어야 로컬 Supabase가 뜬다.

## 8. 남은 작업

### 8.1 디자인 (다음 세션 주 작업)
1. **테이블 행 높이·타이포·사이드바**를 레퍼런스1 기준으로 토큰화 (아직 화면별 값)
2. **승인 큐 · 제작 추적 보드** — 시안대로 미구현. `?section=approvals|tracking`은 구역만 나뉜 상태
3. 발주 4단계 중 1·2단계가 한 페이지에 묶여 있음(도메인 제약). 필요 시 추가 분할 검토

### 8.2 기능 (Codex 몫)
- QR inventory 모델에 `contractVehicleLimit`·미배정 재고·최근 발주 연결
  (현재 수량 단계에서 계약 관련 표시가 전부 `—`)
- 사이트를 Taptolk 직영에 붙이는 조작 UI
- 합성 데모 시드(S10). **승인 큐·추적 보드를 실제 데이터로 보려면 필요**

### 8.3 이월된 결함
- `operations-dashboard-view.tsx`가 단위 `"s"`를 하드코딩 → KO에서 "12.3s" (KO/EN parity 위반)
- 대시보드에 연락 요청 추이 차트 없음, 운영 화면에 세션 테이블·알림 전달 상태 카드 없음(§6.7)

## 9. 사용자 결정 대기

1. **응답 품질 임계**(S7) · **리스크 등급 룰**(S9) — 미확정. 관련 UI는 이번 세션에 제거함
2. **canonical QR 호스트** — apex vs www. 실물 인쇄 전 필수
3. 스티커 하단 흰색 taptolk 로고 자산
4. 콘솔 시각 승인

## 10. 별도 명시 지시 없이 착수 금지

Production 배포·promotion · `taptolk.com` 도메인 연결 · Cron 활성화 ·
live Kakao/SMS/CAPTCHA Provider · Production Supabase 프로젝트/리전/Secret · Enterprise SSO ·
Vercel 요금제/보안 설정 변경 · 담당자 지정 · Task 3 access grant / Test Lab / persona ·
승인 워크플로 신설 · **S7·S9 임계 임의 결정** · QR `1..100` per-Batch 계약 변경 ·
보호 대상 untracked 문서·폴더 커밋/삭제

## 11. 보고 형식

**구현 완료 / 자동 검증 완료 / 수동 검증 대기**를 구분한다. PASS 주장에는 명령·범위·테스트 수를 붙인다.
"Production"은 실제 Production에 배포·검증된 경우에만 쓴다.
credential·전화번호·OTP·토큰·secret은 Git·문서·로그·스크린샷에 남기지 않는다.
**사용자에게 확인을 요청할 때는 링크를 함께 준다.**

## 12. Session handoff protocol

`AGENTS.md`의 Session handoff protocol을 계속 따른다. 이후 모든 handoff는 현재 branch/commit,
완료 범위, 검증 증거, 미해결 위험, `Exact next step`, 사용자 소유 외부 설정을 기록하고
secret은 포함하지 않는다.
