# Taptolk handoff — 2026-07-19 17:35 KST

## Current branch and commit

- Branch: `codex/phase-1-foundation`
- Commit: `46213fbdd7bbd9da4fb5c15d65a9b377cacf1c8d`
- Commit subject: `feat: connect staging QR queue dispatcher`
- GitHub push/deploy: not performed
- Working tree at handoff creation: clean before this handoff document

## Current phase

- Official development remains **Phase 1** because Docker-local reset and runtime pgTAP are open.
- The Phase 3 delivery foundation now reaches a real, staging-only Supabase Queue publisher and
  bounded internal dispatcher Route.
- No authenticated live Queue publication, resident Queue consumer, QR issuance, renderer,
  Asset/token creation, Storage output, or Production runtime was added.

## Completed scope

### Migration history cleanup

- Removed five untracked timestamp duplicates after proving that each was byte-identical to a
  tracked canonical migration.
- Confirmed the duplicate timestamps were absent from linked staging migration history.
- Restored static verification to the canonical 22 migrations and 12 database tests.

### Supabase Queue publisher

- Added an injected `pgmq_public.send` adapter for the strict v1 QR generation Queue DTO.
- Queue name and request timeout come from validated server configuration.
- Queue delay is fixed at zero; retry timing remains in the durable job ledger/Application policy.
- Accepts exactly one positive provider message identifier and normalizes it to a string.
- Reduces explicit throttling to `QUEUE_RATE_LIMITED`.
- Reduces malformed, permission, network, abort, and unknown errors to `QUEUE_UNAVAILABLE`.
- Raw provider responses are not propagated.

### Bounded dispatcher runtime

- Added a bounded Application runtime that claims one job per coordinator call.
- Enforces fixed total claim count, lease, duration budget, and one-at-a-time publication.
- Stops on an empty claim or before another claim after budget exhaustion.
- Returns only aggregate safe outcome counts.
- Existing stable-job publish-success/ack-failure recovery behavior remains unchanged.

### Server-only staging composition

- Added validated Queue/runtime/retry variables to `@taptolk/config`.
- Added exact timing-safe internal bearer authorization.
- Added a staging-only server composition boundary using the existing secret Supabase client.
- Added `GET /api/internal/qr-generation-dispatch` as a Node/dynamic/no-store internal Route.
- Request input cannot select Queue, policy, job, Tenant, Site, Batch, or provider behavior.
- Response/log output contains no credential, Queue payload, Queue message ID, job identity, or
  provider/database detail.

### PDCA

- Plan:
  `docs/01-plan/features/qr-generation-queue-dispatch-runtime.plan.md`
- Design:
  `docs/02-design/features/qr-generation-queue-dispatch-runtime.design.md`
- Analysis:
  `docs/03-analysis/qr-generation-queue-dispatch-runtime.analysis.md`
- Report:
  `docs/04-report/qr-generation-queue-dispatch-runtime.report.md`
- Match rate: 100% for 36 code-level Design obligations.

## Validation evidence

- `pnpm verify`: PASS
  - Biome: 198 files
  - Typecheck: 10 packages / 17 tasks
  - Vitest: 30 files / 234 tests
  - DB static check: 22 migrations / 12 database tests
  - Secret scan: 302 text files during full verify; 303 after report/status update
  - Immutable logo SHA-256: PASS
  - WCJ: total 100, C 100, J 100, W 100, 50 files
  - Application, DB, Worker, and Web production builds: PASS
- Internal Route production-start smoke:
  - local/missing secure configuration returns `503 UNAVAILABLE`;
  - staging-shaped configuration without bearer returns `401 UNAUTHORIZED`;
  - both responses are `Cache-Control: no-store` and include a request ID.
- Focused new-boundary tests: 69 passed.

## Unresolved risks and open gates

- Docker is not installed (`docker: command not found`). Local Supabase reset and runtime pgTAP
  have not run; Phase 1 is not complete.
- The `qr-generation` Queue has not been enabled/created or permission-verified in
  `taptolk-staging`.
- No authenticated live publication was executed and no staging delivery job was mutated.
- The internal Route is not scheduled by a Vercel manifest; Production scheduling remains out of
  scope.
- The dispatcher database RPC calls do not yet have an invocation-owned abort signal. Claim count
  and Queue send time are bounded, but database request timeout remains an infrastructure
  follow-up.
- A resident Worker Queue reader/visibility/ack adapter and generation execution services do not
  exist.
- Manual WCJ keyboard, screen-reader, computed contrast, responsive, and real-device checks remain
  open for user-facing surfaces.

## Exact next development step

1. Recheck Docker. If available, run local Supabase reset and all runtime pgTAP tests against the
   canonical 22-file migration history.
2. Create a separate Plan/Design unit for **staging Queue provisioning and live publisher proof**:
   - durable Basic Queue named `qr-generation`;
   - `pgmq_public` exposure only where required;
   - server-role send permission;
   - no `anon` or browser `authenticated` Queue grants;
   - secure staging variables only;
   - one controlled test job with no unrelated pending delivery rows.
3. After Queue setup is reviewed, invoke the internal Route with the secure staging credential and
   prove:
   - claim → strict v1 message → Queue message ID → durable `QUEUED` acknowledgement;
   - aggregate-only response/logs;
   - publish-success/ack-failure stable-job republish safety.
4. Only after the live publisher proof, create a separate Plan/Design unit for a bounded Worker
   Queue read/visibility/archive adapter. Do not implement QR issuance/rendering in that adapter
   unit.

## Required user-owned external setup

- Install/start Docker Desktop to close the local reset/runtime pgTAP gate.
- Enable/create the staging Queue and review its server-only permissions in the Supabase Dashboard
  or an approved repeatable infrastructure path.
- Configure staging values for the new Queue/runtime variables and `CRON_SECRET` through the
  deployment secret store only.
- Never place database passwords, Supabase secret keys, Cron credentials, authenticator material,
  or real Queue payloads in chat, Git, docs, logs, or browser code.

## Session handoff protocol

Continue to follow the repository `AGENTS.md` Session handoff protocol: create
`docs/handoff-MMDD-HHmm.md` after a major unit or repeating error, record branch/commit/scope/
evidence/risks/exact next/user setup, and never include credentials or secret values.
