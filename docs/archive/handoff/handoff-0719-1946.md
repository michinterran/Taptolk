# Taptolk Phase 2–4 Staging Handoff — 2026-07-19 19:46 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Checkpoint commit: `053f6e4` (`chore: apply phase 2 through 4 staging gate`)
- Authority: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Detailed audit: `docs/architecture/phase-2-4-gap-audit.md`
- Implementation report: `docs/04-report/phase-2-4-implementation.report.md`

## Completed scope

- Applied migrations `20260719060000`–`20260719070000` to `taptolk-staging`.
- Confirmed all 33 local and remote migration versions are aligned.
- Installed staging `pgtap` 1.3.3 and `pgmq` 1.5.1.
- Exposed `pgmq_public` and added it to local API schema configuration.
- Created durable `qr-generation` Queue with RLS.
- Confirmed anon/authenticated have no Queue table privileges and service role/Postgres have
  the intended Queue DML privileges.
- Proved Queue Data API send → read → archive → empty with a server-only credential.
- Corrected 12 stale pgTAP assertions so they test the current schema and RPC-only mutation
  model without weakening RLS or constraints.
- Replaced retryable SQLSTATE `40001` in final approval/cancellation business conflicts with
  non-retryable user exceptions. This prevents hosted RPC retry storms while preserving
  optimistic-concurrency conflict handling.
- Hardened the staging race test to report both action outcomes and verified exactly one
  cancellation/approval result commits.

## Validation evidence

- `pnpm verify`: PASS
- Vitest: 43 files / 270 tests PASS
- DB static contract: 33 migrations / 14 database tests PASS
- Linked staging pgTAP: 14 files PASS
- Authenticated staging Playwright: 15 tests PASS
- Final approval/cancellation race: one commit, one conflict PASS
- Staging cleanup: QR, customer, admin, and Auth residue zero PASS
- WCJ: 100 / C100 / J100 / W100, 54 files
- Secret scan: 362 text files PASS
- immutable logo SHA-256: PASS
- production build: PASS
- migration history: local/remote aligned through `20260719070000`

## Unresolved risks

1. Docker remains unavailable, so local reset has not run. The same migration chain passed
   linked staging runtime pgTAP.
2. The actual Worker 1,000-item generation → export → Queue consume → resume journey has not
   run end to end on staging.
3. Phase 4 receipt → manual assignment → CSV commit → replacement → revocation needs an
   authenticated staging UI E2E.
4. Actual 85mm printing, real-device QR decode, keyboard, screen-reader, computed contrast,
   and responsive manual QA remain.
5. Supabase advisors retain intentional authenticated `SECURITY DEFINER` warnings, three
   server-only RLS/no-policy INFO notices, unindexed foreign-key INFO notices, and disabled
   leaked-password protection.

## Exact next development step

Add an authenticated staging Phase 4 journey that:

1. provisions a generated/printed Batch fixture without retaining secrets;
2. receives the complete Batch and verifies every QR becomes `IN_STOCK`;
3. assigns one QR manually and commits a CSV assignment for another;
4. proves duplicate vehicle/QR Binding is rejected;
5. replaces and revokes QR assets while preserving ended Bindings, status history, inventory
   transactions, and redacted audit rows;
6. cleans all Phase 4 and Auth fixture residue;
7. finishes with the complete linked pgTAP suite, staging E2E, and `pnpm verify`.

After that gate, run the actual 1,000-item Worker/Queue/export journey before Phase 5.

## User-owned external setup

- Enable Supabase leaked-password protection when the project plan permits it.
- Provide access to an 85mm printer and representative iOS/Android devices for physical QA.
- Do not share database passwords, service-role credentials, encryption keys, Queue secrets,
  or generated QR/activation secrets in chat, Git, logs, screenshots, or documents.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record
the current branch and commit, completed scope, validation evidence, unresolved risks, exact
next step, and required user-owned external setup, without credentials or secret values.
