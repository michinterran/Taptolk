# Taptolk Session Handoff — 2026-07-26 13:09 KST

이 문서만 읽고 이어받을 수 있게 쓴다. 단, 이 문서는 **세션 상태 기록**이다.
설계 근거는 `DESIGN_SYSTEM.md` + `docs/design-canon/`, 범위 근거는
`TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`와 승인된 `docs/development/` 문서다.
handoff 산문을 화면·범위 근거로 쓰지 않는다.

## 0. 새 세션이 먼저 읽을 순서

1. `AGENTS.md` — 최신 엔지니어링 계약. 2026-07-26 기준 운영자 지시로 이전 AGENTS 내용을 대체함.
2. `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md` — 최상위 제품 범위.
3. `docs/governance/OPERATING_MODEL.md` — Claude/Codex/Antigravity 레인 경계.
4. `DESIGN_SYSTEM.md` §0 — 화면 완료의 정의.
5. `docs/governance/DESIGN_CONSISTENCY_RULES.md` — 디자인 시스템 강제 규칙.
6. `docs/design-canon/CONTRACTS.md` — 화면별 서버 DTO 계약.
7. `docs/development/PROJECT_COMPLETION_PLAN.md` — 남은 작업 순서.
8. 이 문서 `docs/handoff-0726-1309.md`.

## 1. 현재 체크포인트

- 작업 경로: `/Users/benjaminsong/Documents/Taptolk`
- 브랜치: `codex/phase-1-foundation`
- 현재 로컬 HEAD: `931622a docs(design): console compare checklist + session handoff (0725-1314)`
- 원격 상태: `origin/codex/phase-1-foundation`보다 **로컬이 21커밋 앞섬**.
- 현재 작업트리: **미커밋 변경 있음**. 새 세션은 절대 `git add -A` 또는 `git add .` 하지 말 것.
- 마지막으로 원격에 올라간 구현 기준은 `9887dc3 Complete owner PWA reply flow design`이다.
- 2026-07-24 기준 데모 alias `https://taptolk-demo.vercel.app/ko`는 `9887dc3` 계열 preview에 연결·검증됐으나,
  2026-07-25 이후 로컬 21커밋과 현재 WIP는 아직 원격/데모에 반영되지 않았다.

## 2. 현재 미커밋 변경 — 보존 필요

다음 파일은 현재 워킹트리에 남아 있다. 대부분 Codex 구현 레인 파일이다.
새 세션은 먼저 내용을 재확인하고, 의도된 변경이면 **명시 경로만 stage**해서 분리 커밋한다.

```text
M  apps/web/app/globals.css
M  apps/web/components/operations-dashboard-view.tsx
M  apps/web/content/operations-copy.ts
M  config/design-system-baseline.json
M  packages/qr-engine/src/render.ts
?? supabase/migrations/20260724000000_grant_qr_batch_samples_columns.sql
```

현재 diff의 의미:

- `apps/web/app/globals.css`
  - 공개/관리자 일부 caption/eyebrow/lead font-size 리터럴을 `--tt-pub-*` 토큰으로 치환 중.
- `config/design-system-baseline.json`
  - 위 token 치환으로 사라진 font-size baseline 항목 일부 제거.
- `apps/web/components/operations-dashboard-view.tsx`
  - 운영 대시보드 응답시간 단위를 하드코딩 `"s"`에서 locale copy로 이동.
- `apps/web/content/operations-copy.ts`
  - `secondsUnit` KO/EN 추가. KO는 `"초"`, EN은 `"s"`.
- `packages/qr-engine/src/render.ts`
  - QR decode 전 `sharp(...).flatten({ background: "#FFFFFF" })` 적용. 투명 배경 QR 판독 안정화 의도로 보임.
- `supabase/migrations/20260724000000_grant_qr_batch_samples_columns.sql`
  - `qr_batch_samples`의 storage metadata 3개 컬럼에 authenticated column-level SELECT grant.
  - 기존 migration 수정이 아니라 새 migration이다.

주의:

- 이 WIP는 아직 현재 세션에서 `verify`하지 않았다.
- 이 WIP를 버리거나 되돌리지 말 것. 의도가 불명확하면 운영자에게 물어본다.
- `packages/ui/src/styles/tokens.css`는 Claude 디자인 레인 소유다. Codex가 임의 수정하지 않는다.

## 3. 최근 완료된 흐름

### 3.1 2026-07-24 PWA 데모/답장 흐름

커밋 `9887dc3 Complete owner PWA reply flow design`에서 완료·푸시·데모 배포됨.

구현된 사용자 흐름:

- sender QR 스캔 후 메시지 작성 화면.
- sender 대기 화면.
- 차주 PWA `MESSAGES` 탭의 받은 메시지 목록.
- 차주 메시지 상세/답장 페이지.
- 차주가 선택 멘트 또는 직접 입력으로 답장.
- sender 대기 화면에서 답장 확인 상태.

검증 증거(2026-07-24 세션):

- `corepack pnpm verify` PASS.
- Supabase staging migration `20260724150500_owner_pwa_message_reply_flow.sql` push 완료.
- Vercel preview READY.
- `taptolk-demo.vercel.app` alias 갱신.
- `/ko`, `/ko/owner/messages/{uuid}`, `/api/owner/push/config`, `/api/internal/notification-dispatch` smoke 200.
- 데모 dispatch watcher 실행 확인.

보안 계약:

- 차주 전화번호는 브라우저로 내려가지 않는다. 뒤 4자리도 안 된다.
- 응답/로그/문서에 OTP, QR token, activation code, response token, secret을 남기지 않는다.

### 3.2 2026-07-25 디자인/거버넌스 로컬 커밋

현재 로컬 HEAD까지 원격보다 앞선 21개 커밋은 주로 디자인·거버넌스·완료계획 문서다.
주요 커밋:

```text
931622a docs(design): console compare checklist + session handoff (0725-1314)
3d13aab docs(design): console/platform state coverage canon (0725)
a66071e docs(design): token-migration workorder + micro-label token
f88518c docs(design): record operator decisions on reference vs system (0725)
a8fb895 docs(design): auth card state polish spec (login/signup/MFA)
53fefa5 docs(design): author console/platform screen data contracts
ae1e623 docs(design): full screen inventory and Codex prep sheet (0725)
f2e21b7 docs(governance): operating model, design consistency rules, completion plan
```

중요 산출물:

- `docs/governance/OPERATING_MODEL.md`
- `docs/governance/DESIGN_CONSISTENCY_RULES.md`
- `docs/development/PROJECT_COMPLETION_PLAN.md`
- `docs/development/design-page-inventory-0725.md`
- `docs/design-canon/CONTRACTS.md`
- `docs/design-canon/console-states.md`
- `docs/design-canon/console-compare-checklist.md`
- `docs/development/workorder-codex-auth-states-0725.md`
- `docs/development/workorder-codex-token-migration-0725.md`

이 문서들은 아직 원격에 push되지 않은 로컬 커밋 위에 있다.

## 4. 레인 규칙 — 새 세션에서 특히 중요

`docs/governance/OPERATING_MODEL.md`가 binding이다.

- Claude: 디자인 권위 레인. 토큰·정본·카피/데이터 계약·워크오더 작성.
- Codex: 구현 레인. 앱 코드, route/API, Supabase, tests, baseline, globals.css 적용의 유일한 작성자.
- Antigravity: 리뷰·제안 전용. 메인라인에 직접 커밋하지 않는다.

절대 규칙:

- `git add -A`, `git add .` 금지.
- 한 커밋에 레인을 섞지 않는다.
- 다른 레인의 미커밋 변경은 stage하지 않는다.
- 화면은 handoff 산문이 아니라 정본과 CONTRACTS를 보고 구현한다.
- 정본으로 답이 안 나오면 추측 구현하지 말고 Claude/운영자에게 되돌린다.

## 5. Exact Next Step

### Step 0 — 현재 WIP 안정화

새 세션의 첫 작업은 새 기능이 아니라 **현재 WIP를 잃지 않고 닫는 것**이다.

1. `git status --branch --short`와 `git diff --stat`으로 현재 상태를 다시 확인.
2. 위 §2의 6개 WIP 파일을 직접 읽고, 의도된 변경인지 확인.
3. 의도된 변경이면 아래 검증을 먼저 실행:

```bash
corepack pnpm validate:design-system
corepack pnpm validate:wcj
corepack pnpm --filter @taptolk/qr-engine build
corepack pnpm db:check
```

4. 통과하면 WIP 파일만 명시적으로 stage:

```bash
git add apps/web/app/globals.css \
  apps/web/components/operations-dashboard-view.tsx \
  apps/web/content/operations-copy.ts \
  config/design-system-baseline.json \
  packages/qr-engine/src/render.ts \
  supabase/migrations/20260724000000_grant_qr_batch_samples_columns.sql
```

5. 커밋은 구현 레인으로 분리한다. 예:

```bash
git commit -m "Stabilize token migration and QR sample preview WIP"
```

6. 그 뒤에만 다음 Step A로 간다.

### Step A — 기반 복구

정본: `docs/development/PROJECT_COMPLETION_PLAN.md` §3.1, §4.

- 로컬 dev 500 문제부터 확인/수정:
  - 대상: `apps/web/app/[locale]/(public)/owner/offline`
  - 이전 문서에 owner/offline JSON 처리 버그로 기록됨.
  - `corepack pnpm dev`로 공개·콘솔 화면이 200 렌더되는지 확인.
- `.next` 로컬 잔재가 grep/검증을 오염시키면 삭제 후 재검증. 단, 소스 파일 삭제와 구분.

### Step B — 미결 배선

정본: `docs/design-canon/CONTRACTS.md`, `docs/development/PROJECT_COMPLETION_PLAN.md` §4 Step B.

우선순위:

1. 활성화 완결 응답에서 `activationCode` 회귀가 없는지 sweep.
2. 차주 재진입 엔드포인트 2개(`/api/owner/session/reclaim/*`)가 최신 계약과 맞는지 확인.
3. 스캔 판정 RPC/route가 스티커 소지 기반 판정으로 일관되는지 확인.
4. PWA/owner 메시지·히스토리 필드가 CONTRACTS와 일치하는지 확인.
5. "연락 부탁드립니다" reason code + KO/EN copy가 아직 없다면 추가.

이미 2026-07-24에 PWA 답장 흐름은 상당 부분 구현됐으므로, 새 세션은 먼저 **현재 코드가 최신 CONTRACTS와 맞는지 audit**하고 빠진 필드만 추가한다.

## 6. 검증 기준

작은 slice마다:

```bash
corepack pnpm validate:design-system
corepack pnpm validate:wcj
```

DB/API 또는 공유 도메인 변경이 있으면:

```bash
corepack pnpm db:check
corepack pnpm --filter @taptolk/application build
corepack pnpm --filter @taptolk/web typecheck
```

phase/slice 완료 보고 전:

```bash
corepack pnpm verify
```

자동 검증과 별개로, 화면 변경은 320/768/1280/1920 및 실기기/키보드/스크린리더/대비를 수동 검토해야 한다.
WCJ 통과는 레이아웃 정본 일치 증거가 아니다.

## 7. 착수 금지

별도 운영자 승인 없이 금지:

- Production 배포, Production Supabase, live domain 연결.
- Production Cron 활성.
- live Kakao/SMS provider 연결 또는 secret 설정.
- Vercel 설정 변경.
- QR `1..100` per-batch 계약 초과.
- 승인 워크플로 신설.
- S7/S9 임계값 임의 결정.
- 로고 crop/recolor/redraw/filter/stretch.
- 차주 전화번호를 브라우저 응답/화면/로그/문서에 노출. 뒤 4자리도 금지.
- 없는 값을 0/default/mock 값으로 내려보내기.
- 기존 migration 수정.

## 8. 외부/운영자 게이트

Codex가 임의로 정하지 말 것:

- Production Supabase 프로젝트/리전.
- SMS/CAPTCHA/Kakao provider 및 요금제.
- 개인정보 보유기간.
- 인쇄 규격·재질.
- Pilot 고객/운영 담당/incident 담당.
- Vercel Production Cron plan 및 activation.

스테이징 로그인 복구는 운영자가 Vercel Supabase env를 설정하고 redeploy해야 콘솔 실화면 눈대조가 가능하다.
경로: `docs/deployment/staging-login-env-checklist.md`.

## 9. 새 세션에 붙여넣을 명령 프롬프트

```text
Taptolk 저장소 `/Users/benjaminsong/Documents/Taptolk`에서 이어간다.
브랜치는 `codex/phase-1-foundation`이다.

먼저 이 순서로 읽어라:
1. `AGENTS.md`
2. `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
3. `docs/governance/OPERATING_MODEL.md`
4. `DESIGN_SYSTEM.md` §0
5. `docs/governance/DESIGN_CONSISTENCY_RULES.md`
6. `docs/design-canon/CONTRACTS.md`
7. `docs/development/PROJECT_COMPLETION_PLAN.md`
8. `docs/handoff-0726-1309.md`

역할:
- Claude/디자인이 아니라 Codex 구현자다.
- 앱 코드와 DB/API를 붙이되, 디자인 정본·토큰 값은 임의로 바꾸지 마라.
- `git add -A`/`.` 금지. 자기 레인 파일만 명시적으로 stage한다.
- 차주 전화번호는 브라우저로 절대 보내지 마라. 뒤 4자리도 금지.
- 기존 migration 수정 금지. migration은 새 파일만.
- 없는 값은 0/default/mock으로 채우지 말고 비워라.

현재 상태:
- 로컬 HEAD는 `931622a`.
- origin보다 로컬이 21커밋 앞서 있다.
- 미커밋 WIP가 있다:
  `apps/web/app/globals.css`,
  `apps/web/components/operations-dashboard-view.tsx`,
  `apps/web/content/operations-copy.ts`,
  `config/design-system-baseline.json`,
  `packages/qr-engine/src/render.ts`,
  `supabase/migrations/20260724000000_grant_qr_batch_samples_columns.sql`.

Exact next step:
1. `git status --branch --short`, `git diff --stat`으로 현재 WIP 확인.
2. WIP 6개 파일을 읽고 의도 확인.
3. 의도된 변경이면 `corepack pnpm validate:design-system`,
   `corepack pnpm validate:wcj`, `corepack pnpm --filter @taptolk/qr-engine build`,
   `corepack pnpm db:check`를 실행.
4. 통과하면 위 6개 파일만 명시적으로 stage하고 구현 레인 커밋으로 닫는다.
5. 그 다음 `PROJECT_COMPLETION_PLAN.md` Step A: 로컬 dev 500(owner/offline) 문제를 수정한다.
6. 이후 Step B: 활성화코드 회귀 sweep, 차주 재진입 2개, 스캔 판정, PWA 필드/히스토리/메시지 계약 audit을 진행한다.

각 slice마다 `validate:design-system`, `validate:wcj`.
완료 보고 전 `corepack pnpm verify`.
보고는 구현/자동검증/수동검증 대기/외부게이트/다음 한 단계로 나눠 한국어로 한다.
```

## 10. 보고 형식

새 세션은 완료 보고를 다음처럼 구분한다:

- 구현 완료: 파일/커밋 기준.
- 자동 검증: 명령과 결과.
- 수동 검증 대기: 실기기/눈대조/접근성 등 사람이 확인할 것.
- 외부 게이트: 운영자/Vercel/Supabase/provider/Production 등 Codex가 못 여는 것.
- 다음 한 단계: 하나만 구체적으로.

이 handoff 규약을 다음 handoff에도 그대로 이어간다.
