# Taptolk Customer Management Reference Alignment Handoff — 2026-07-22 10:51 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- HEAD: `0a824a3`
- Remote tracking state: `origin/codex/phase-1-foundation` is synchronized with HEAD.
- Working tree: the dashboard and customer-management reference-alignment slices are implemented
  but intentionally uncommitted, unpushed, and undeployed for sequential operator review.
- Production deployment: not changed.
- Production Cron: not changed; the deferred verifier passed.
- Decision: `CUSTOMER_MANAGEMENT_LIST_ALIGNMENT_IMPLEMENTED /
  AUTOMATED_AND_AUTHENTICATED_VISUAL_VALIDATION_COMPLETE / OPERATOR_ACCEPTANCE_PENDING`

## Completed scope

- Rechecked the Super Admin dashboard against
  `/Users/benjaminsong/Downloads/레퍼런스1.png` and accepted it as the implementation baseline for
  continuing to the next screen. Manual operator acceptance remains separate.
- Aligned `/{locale}/admin/platform/management-companies` to the same compact operating-console
  density:
  - compact heading and action placement;
  - four concise current-result/current-page metrics;
  - table-first management-company portfolio;
  - narrow operating-state review queue;
  - compact row action menu.
- Preserved the visible `관리회사 > 사이트` hierarchy.
  - Removed the internal contract-customer name from visible portfolio rows.
  - Kept the internal `tenant_id` contract in server actions, authorization, repositories, and RLS.
- Added a server-backed operating-state filter using the non-conflicting `state` query parameter.
  The existing `status` query parameter remains reserved for mutation result messages.
- Search, state filter, and pagination now preserve the same query state.
- The review queue uses only authoritative `ACTIVE`, `SUSPENDED`, and `CLOSED` organization states
  already present in the application contract. It does not create a new approval workflow.
- Added matching Korean and English typed copy and an application-service unit test for normalized
  search, paging, and status-filter propagation.
- Did not change central RBAC, PostgreSQL RLS, audit behavior, provider behavior, QR contracts,
  Production configuration, or the 1–100 per-Batch quantity policy.

## Files added or changed for this slice

- `apps/web/admin/supabase-management-company-catalog-repository.ts`
- `apps/web/app/[locale]/(admin)/admin/platform/management-companies/page.tsx`
- `apps/web/app/globals.css`
- `apps/web/components/management-company-catalog-view.tsx`
- `apps/web/content/admin-company-portfolio-copy.ts`
- `packages/application/src/management-company-catalog-service.ts`
- `packages/application/src/management-company-catalog-service.test.ts`

The earlier dashboard-alignment files recorded in `docs/handoff-0722-1034.md` remain modified and
uncommitted in the same working tree.

## Validation evidence

- `corepack pnpm validate:wcj`: PASS after each web implementation/refinement pass.
  - Total 100 / C100 / J100 / W100 over 124 files.
- Focused application test: PASS.
  - `management-company-catalog-service.test.ts`: 2 tests.
- `corepack pnpm typecheck`: PASS, 19/19 tasks.
- `corepack pnpm verify`: PASS.
  - lint: 382 files;
  - typecheck: 19/19 tasks;
  - unit: 61 files / 393 tests;
  - DB static contract: 65 migrations / 29 database tests;
  - secret scan: 638 text files;
  - immutable logo verification: PASS;
  - SERVICE fail-closed boundary: PASS;
  - deferred Production Cron manifest: PASS;
  - WCJ: PASS;
  - production build: PASS.
- Authenticated Chrome visual review: PASS at 1540 × 1174.
  - six authoritative management-company rows rendered;
  - table width 1002px and decision queue width 256px;
  - document-level horizontal overflow: none;
  - browser console errors: 0.
- Server-backed `state=SUSPENDED` review: PASS.
  - the localized filter showed `일시 중지` selected;
  - the authoritative result was zero rows and the localized empty state rendered.
- Responsive headline and overflow review: PASS at 320, 768, 1280, and 1920 CSS pixels.
  - document-level horizontal overflow: none at every reviewed width;
  - the Korean heading wrapped to two semantic lines at 320 and one line at the wider widths.
- After the final verify, the local development server was restarted and the authenticated customer
  management route rendered six rows with zero console errors.

## Local review

- Customer-management list: `http://localhost:3000/ko/admin/platform/management-companies`
- Dashboard baseline: `http://localhost:3000/ko/admin/platform`
- Administrator login: `http://localhost:3000/ko/admin/login`
- A local development server is listening on port 3000. If a later verification command removes
  `.next` while it is running, restart it with:
  `corepack pnpm --filter @taptolk/web dev`
- Do not place credentials, cookies, OTP values, phone numbers, messages, or tokens in logs,
  screenshots, or later handoffs.

## Unresolved work and risks

- Operator acceptance of the customer-management list remains pending even though implementation,
  automated validation, and authenticated visual review are complete.
- The review queue intentionally describes only the current filtered result page. It is not a
  global approval queue and must not be presented as one without a separately approved data model.
- Management-company detail, site list/detail, QR production, monitoring, reports, revenue, and
  account/permission variations have not received this final reference alignment.
- Real-device, keyboard, screen-reader, and computed-contrast acceptance remain manual gates.
- Staging fixture names remain authoritative and may be long. Do not replace them with invented
  application data.

## Protected untracked files

Do not delete, overwrite, rename, or commit these user-owned files without explicit approval:

- `apps/web/components/management-company-catalog-view 2.tsx`
- `docs/deployment/kakao-alimtalk-onboarding-guide.md`
- `docs/development/prd-public-surface-0721.md`
- `docs/development/prd-public-user-admin-portals-0721.md`
- `docs/development/workorder-kakao-alimtalk-live-provider.md`
- `docs/legal/`

The untracked `artifacts/` directory also remains outside this checkpoint and must be reviewed
before any future staging operation.

## Exact next development step

1. Read `AGENTS.md` and this handoff, then confirm branch `codex/phase-1-foundation`, HEAD
   `0a824a3`, and the uncommitted dashboard plus customer-management alignment changes.
2. Review `http://localhost:3000/ko/admin/platform/management-companies` against
   `/Users/benjaminsong/Downloads/레퍼런스1.png` without changing another page.
3. If the operator accepts the customer-management list, align only the existing management-company
   detail route `/{locale}/admin/platform/management-companies/[companyId]` next.
4. On that detail screen, preserve the compact shell and visible `관리회사 > 사이트` hierarchy;
   reuse authorized identity, site, QR, capacity, and lifecycle data already exposed by the existing
   workspace service. Do not invent new routes, approval states, contract rules, or customer data.
5. Do not begin the separate site list or site detail alignment until the management-company detail
   screen has been reviewed.
6. Run `corepack pnpm validate:wcj` after every web change and `corepack pnpm verify` before reporting
   that screen complete. Keep implementation, automated validation, authenticated visual review,
   and manual operator acceptance as separate statuses.

Do not deploy Production, connect a domain, activate Cron, configure a live Kakao/SMS/CAPTCHA
provider, select a Production Supabase project or region, change a Vercel plan, assign incident
owners, or start Task 3 access-grant/Test Lab/persona work without a separate explicit instruction.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next step,
and required user-owned external setup, without credentials or secret values.
