# Taptolk Admin Reference Alignment Handoff — 2026-07-22 10:34 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- HEAD: `0a824a3`
- Remote state: `origin/codex/phase-1-foundation` is synchronized with HEAD.
- Working tree: the reference-alignment slice is implemented but intentionally not committed,
  pushed, or deployed so the operator can review each screen sequentially.
- Base implementation commit: `6c96677`
- Previous release-evidence commit: `0a824a3`
- Production deployment: not changed in this session.
- Production Cron configuration: not changed; the deferred verifier passed.
- Decision: `DASHBOARD_REFERENCE_ALIGNMENT_IMPLEMENTED / AUTOMATED_VALIDATION_COMPLETE /
  OPERATOR_ACCEPTANCE_AND_NEXT_SCREEN_PENDING`

## Current uncommitted implementation

- Reworked the shared administrator shell using the operator-provided `레퍼런스1.png` as the
  visual source of truth:
  - compact 13rem fixed sidebar and low-height top bar;
  - smaller page heading, body copy, cards, table rows, radii, and whitespace;
  - top-right locale and signed-in account summary;
  - grouped icon navigation for service overview, customer operations, service operations, and
    administration.
- Changed the platform navigation label from `관리회사` to `고객관리` and shipped the matching
  English dictionary copy.
- Made the visible customer hierarchy `관리회사 > 사이트`.
  - Removed the internal `계약 고객/tenant` column from the platform dashboard presentation.
  - Kept tenant isolation in backend authorization and PostgreSQL RLS; this was a presentation
    change only.
- Replaced the oversized dashboard hero and duplicated role/scope cards with:
  - one compact semantic page heading;
  - six operating metrics;
  - a table-first customer portfolio;
  - a narrow decision queue;
  - lower operational flow, delivery-quality, and hierarchy panels.
- Added a real data-refresh link and retained authorized detail routes.
- Updated KO/EN typed dictionaries and `design-qa.md` together.
- Did not change authentication, central RBAC, PostgreSQL RLS, audit behavior, provider behavior,
  QR enums/action labels, `caller` terminology, or the 1–100 per-Batch contract.

## Modified tracked files

- `apps/web/app/[locale]/(admin)/admin/dashboard/page.tsx`
- `apps/web/app/[locale]/(admin)/admin/platform/page.tsx`
- `apps/web/app/globals.css`
- `apps/web/components/admin-dashboard-view.tsx`
- `apps/web/components/admin-page-header.tsx`
- `apps/web/content/admin-company-portfolio-copy.ts`
- `apps/web/content/admin-overview-copy.ts`
- `apps/web/content/messages.ts`
- `design-qa.md`

## Validation evidence

- `corepack pnpm validate:wcj`: PASS
  - Total 100 / C100 / J100 / W100 over 124 files.
- `corepack pnpm lint`: PASS over 381 files.
- `corepack pnpm verify`: PASS.
  - typecheck: 19/19 tasks
  - unit: 60 files / 391 tests
  - DB static contract: 65 migrations / 29 database tests
  - secret scan: 636 text files
  - immutable logo verification: PASS
  - SERVICE fail-closed boundary: PASS
  - deferred Production Cron manifest: PASS
  - WCJ: PASS
  - production build: PASS
- Authenticated desktop visual review before the final verify: PASS at 1487 × 1058.
  - Reference and implementation were compared together.
  - Browser console errors: 0.
  - No page-level horizontal overflow was observed at the reviewed desktop state.
- After the final verify, `http://localhost:3000/ko/admin/platform` returned HTTP 200 from the
  restarted local development server.

## Local review

- Super Admin dashboard: `http://localhost:3000/ko/admin/platform`
- Administrator login entry: `http://localhost:3000/ko/admin/login`
- A local development server was listening on port 3000 when this handoff was written. A new
  session must restart it if that process no longer exists:
  `corepack pnpm --filter @taptolk/web dev`
- Do not place credentials, cookies, OTP values, phone numbers, messages, or tokens in the next
  session's logs or handoff.

## Unresolved product and design work

- The next screens have not yet received the same final reference alignment:
  - customer-management portfolio/list;
  - management-company detail;
  - site list and site detail;
  - QR production, monitoring, reports, revenue, and account/permission page variations.
- Menu review identified possible future operator areas such as contract management, capacity and
  usage, alert center, and system settings. Do not add dead navigation links. Add a menu item only
  when its authorized route, data source, states, KO/EN copy, and tests are implemented together.
- Real mobile device, keyboard, screen-reader, and computed-contrast acceptance remain manual.
- Staging fixture company names are intentionally visible in local data and may be long; the UI now
  truncates them safely. Do not replace authoritative data with invented names in application code.

## Protected untracked files

Do not delete, overwrite, rename, or commit these user-owned files without explicit approval:

- `apps/web/components/management-company-catalog-view 2.tsx`
- `docs/deployment/kakao-alimtalk-onboarding-guide.md`
- `docs/development/prd-public-surface-0721.md`
- `docs/development/prd-public-user-admin-portals-0721.md`
- `docs/development/workorder-kakao-alimtalk-live-provider.md`
- `docs/legal/`

The untracked `artifacts/` directory also remains outside this checkpoint and must be reviewed
before any future staging operation.

## Exact next development step

1. Read `AGENTS.md` and this handoff, then confirm the branch is
   `codex/phase-1-foundation`, HEAD is `0a824a3`, and the tracked reference-alignment changes above
   are still present and uncommitted.
2. Restart the local web server only if port 3000 is not already serving the app.
3. Review `http://localhost:3000/ko/admin/platform` against
   `/Users/benjaminsong/Downloads/레퍼런스1.png` without changing other pages.
4. If the dashboard is accepted, continue sequentially with the `고객관리` portfolio/list page:
   apply the same compact shell, search/filter/table/decision-queue density, and preserve the visible
   `관리회사 > 사이트` hierarchy. Do not begin the management-company detail or site pages until
   the portfolio/list screen is reviewed.
5. After every web change run `corepack pnpm validate:wcj`; before reporting the screen complete run
   `corepack pnpm verify`. Keep implementation completion, automated validation, and manual operator
   acceptance as separate statuses.

Do not deploy Production, connect a domain, activate Cron, configure a live Kakao/SMS/CAPTCHA
provider, select a Production Supabase project or region, change a Vercel plan, assign incident
owners, or start Task 3 access-grant/Test Lab/persona work without a separate explicit instruction.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next step,
and required user-owned external setup, without credentials or secret values.
