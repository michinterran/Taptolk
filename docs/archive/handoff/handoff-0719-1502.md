# Taptolk handoff — 2026-07-19 15:02 KST

## Current branch and commit

- Branch: `codex/phase-1-foundation`
- Commit: `96b87b516874c7a339c3f54be269827aa0aa657f`
- Commit subject: `feat: version QR generation queue payload`
- GitHub push/deploy: not performed
- Worktree before this handoff file: clean

## Completed scope

- Continued directly from `docs/handoff-0719-1457.md`.
- Replaced the generic Worker Queue input with a strict QR generation v1 contract.
- Added explicit `schemaVersion`, `generationRevision`, `deliveryAttempt`, and Batch identity.
- Froze `jobType` to `QR_GENERATION` for the implemented consumer contract.
- Required non-null Tenant, Site, Batch, job, and trace UUIDs.
- Unknown versions/types, unknown keys, zero attempts/revisions, and missing Site scope fail before
  handler lookup.
- Strict parsing blocks token/code/contact data, reasons, storage paths, cookies, authorization
  headers, credentials, message bodies, and arbitrary provider payloads.
- Added 17 focused tests for parsing, compatibility rejection, allowlisting, handler dispatch, and
  missing handler behavior.
- No Queue provider call, polling loop, resident consumer, renderer, Asset/token issuance, or
  Production host configuration was added.

## Validation evidence

- `pnpm verify`: PASS
  - Biome: 179 files
  - Typecheck: 10 packages / 16 tasks
  - Vitest: 21 files / 134 tests
  - DB static check: 22 migrations / 12 database tests
  - Secret scan: 278 text files
  - Immutable logo SHA-256: PASS
  - WCJ: 100 total, C 100, J 100, W 100, 49 files
  - Worker and Web production builds: PASS
- Focused Worker Queue contract: 17 passed.
- The preceding dispatcher staging journey remains 11 passed; no database or staging state changed
  in this Queue schema unit.

## Current phase and open gates

- Official phase remains **Phase 1**. Phase 3 has an initial QR/dispatcher contract foundation.
- Docker is unavailable, so local Supabase reset and runtime pgTAP remain open. Phase 1 is not
  complete.
- The consumer payload is frozen, but a provider-neutral dispatch coordinator and
  publish-success/ack-failure injection are not implemented.
- Real Queue provider integration and Production Worker runtime remain intentionally blocked from
  this unit.
- Manual WCJ keyboard, screen-reader, computed contrast, responsive, and real-device gates remain.

## Exact next development step

1. Recheck Docker. If available, run local Supabase reset and all pgTAP tests first.
2. If Docker remains unavailable, add a provider-neutral Application dispatch coordinator using
   the existing dispatcher service plus a typed publisher port and fake-publisher tests.
3. Prove three paths without a real provider:
   - publish success records `QUEUED`;
   - publish failure records allowlisted `RETRY_WAIT`;
   - publish success followed by acknowledgement failure leaves the lease recoverable and does not
     falsely record provider failure.
4. Do not add Supabase Queue API calls, credentials, Cron/HTTP routes, polling loops, QR issuance,
   rendering, or Production deployment configuration.
5. Run focused tests and `pnpm verify`.

## Required user-owned external setup

- Install or start Docker Desktop only to close the local reset/runtime pgTAP gate.
- No Queue credentials or host selection are required for the provider-neutral exact next step.
- Never place database passwords, Supabase keys, Queue credentials, or other secrets in chat, Git,
  documentation, logs, or browser code.

## Session handoff protocol

Continue to follow the repository `AGENTS.md` Session handoff protocol described in
`docs/handoff-0719-1457.md`: create a fresh handoff after a major feature unit or repeating error,
record branch/commit/scope/evidence/risks/exact next/user setup, and never include secret values.
