# Taptolk Phase 3 10×100 Staging Acceptance Handoff — 2026-07-20 00:38 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Checkpoint commit: `0b984a4` (`test: prove 10x100 qr generation staging acceptance`)
- Authority: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Detailed report: `docs/04-report/qr-generation-staging-acceptance.report.md`
- Phase 2–4 audit: `docs/architecture/phase-2-4-gap-audit.md`

## Completed scope

- Preserved the 1–100 per-Batch contract and provisioned ten independently identified,
  approved 100-item Batches.
- Published all ten durable Jobs through the actual dispatcher into the staging
  `qr-generation` PGMQ Queue.
- Committed the first 50-item chunk, intentionally stopped the process, waited for the
  60-second visibility lease to expire, and resumed the same Queue message.
- Completed 1,000 actual QR issuance/render rows with duplicate ordinal, token, QR Asset,
  internal UUID, Human Code, and activation row counts all zero.
- Re-downloaded and independently decoded 1,000 Storage PNGs; all decoded token hashes matched
  the persisted public token hashes.
- Generated 10 sets of PDF, CSV, ZIP, and checksum manifest exports and matched all 40 Storage
  objects to DB byte-size/checksum ledgers.
- Proved visibility retry, 10 normal archives, one poison archive, poison active zero, and
  acceptance Queue history cleanup.
- Removed acceptance tenant, Auth actors, Queue messages, and exact generation/export Storage
  prefixes; a final orphan inventory returned zero.
- Corrected an ambiguous PL/pgSQL print-context revision variable and bounded Worker render,
  print-source load, and export-store concurrency.
- Kept PDF as its own export and removed only its redundant copy from ZIP. ZIP still contains
  CSV, manifest, 100 PNGs, and 100 SVGs.

## Validation evidence

- Actual staging 10×100 acceptance: PASS, 1 test in about 1.1 hours
- Generated: 1,000 / duplicate 0
- QR decode: 1,000/1,000
- Export checksum/byte ledger: 40/40
- Queue: retry observed, archive 11, poison active 0
- Residue: tenant/Auth/Queue/Storage 0
- Linked staging pgTAP: 16 files PASS
- Authenticated staging Playwright: 19 PASS, explicit acceptance 1 skipped by default
- `pnpm verify`: PASS
- Vitest: 43 files / 271 tests PASS
- DB static contract: 39 migrations / 16 tests PASS
- WCJ: 100 / C100 / J100 / W100, 54 files
- Secret scan: 376 text files PASS
- Immutable logo SHA-256: PASS
- Production build: PASS
- Staging migration history aligned through `20260719132000`

## Unresolved risks

1. Docker remains unavailable, so local reset has not run. The same migration chain passed the
   full linked staging pgTAP suite.
2. Actual 85mm printing, representative iOS/Android decode, keyboard, screen-reader, computed
   contrast, responsive, and real-device manual QA remain.
3. Supabase recommends TUS resumable upload above 6MB. The 100-item PDF/ZIP passed staging with
   Queue retry, but Phase 9 should evaluate TUS for production reliability.
4. Supabase leaked-password protection and existing advisor index/policy performance backlog
   remain Phase 9 hardening work.

## Exact next development step

Start Phase 5 Owner Activation with a separate Plan/Design before code:

1. define the Owner identity, verified-phone proof, OTP attempt/rate-limit, activation inspection,
   consent version, and PWA shell contracts;
2. add `owners`, `owner_devices`, `vehicle_owners`, OTP challenge/attempt, and activation proof
   schema with service-only secrets, RLS, tenant constraints, retention, and redacted audit;
3. implement activation inspect and complete through
   UI → Route → Application Service → Domain Policy → Repository transaction;
4. in one PostgreSQL transaction lock the QR Asset and activation code, verify OTP proof and
   consent, resolve/create Owner and Vehicle, reject Binding conflicts, create Owner relationship,
   activate the QR, consume the activation code, and write audit;
5. prove two concurrent completion requests yield exactly one success, enforce OTP expiry,
   resend/attempt/phone/IP/device limits, and prevent cross-owner vehicle/session reads;
6. add canonical `/ko/activate/[publicToken]`, `/en/activate/[publicToken]`, and locale Owner PWA
   shell routes with offline-safe static shell only;
7. run linked pgTAP, authenticated/owner staging E2E, WCJ, and `pnpm verify` before Phase 5
   completion.

Do not begin Phase 6 Public Contact until Phase 5 transaction, OTP limits, ACTIVE transition,
owner isolation, cleanup, and staging evidence pass.

## User-owned external setup

- Keep the linked Supabase staging project available for Phase 5 migrations and owner E2E.
- A real domestic SMS provider is not required for the first Phase 5 slice; use the bounded
  staging test inbox/adapter and never expose OTPs in logs or artifacts.
- Enable Supabase leaked-password protection when the project plan permits it.
- Provide access to an 85mm printer and representative iOS/Android devices for physical QA.
- Do not share database passwords, service-role credentials, encryption/HMAC keys, OTPs,
  activation codes, public QR tokens, cookies, or authorization headers.

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record
the current branch and commit, completed scope, validation evidence, unresolved risks, exact
next step, and required user-owned external setup, without credentials or secret values.
