# Taptolk Session Handoff — 2026-07-19 10:58 KST

## 1. Handoff rule

Create `docs/handoff-MMDD-HHmm.md` when a major feature unit is complete or when
the same error begins repeating and a fresh session would reduce context and
token waste.

Every handoff must record the current branch and commit, completed scope,
validation evidence, unresolved risks, exact next step, and any required
user-owned external setup. Never include credentials or secret values. Carry
this rule into every later handoff document, then notify the user that it is a
safe checkpoint and recommend opening a new Codex session with the handoff path.

## 2. Project authority and working state

- Highest specification: `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`
- Engineering contract: `AGENTS.md`
- Working branch: `codex/phase-1-foundation`
- Current commit: `4dee19c151e71dca5a8039d47e26b45a687d50f1`
- Remote branch: `origin/codex/phase-1-foundation`
- Supabase environment: staging only
- Supabase project ref: `evpwzjkhfppdjivkyokh`
- Latest migration: `20260719014506_phase_1_site_management.sql`
- Working tree at handoff creation: clean before this handoff-only change

## 3. Completed feature unit

Role-scoped Site CRUD and lifecycle management is implemented.

- RLS-backed Site catalog at `/{locale}/admin/sites`
- KO/EN Site UI and semantic headline line groups
- Super Admin direct Site creation
- Role-scoped operational profile update
- Super Admin contract vehicle limit update
- Super Admin suspend, reactivate, and close
- Platform Operator operational update, suspend, and reactivate
- Management Admin management-company-scoped operational update
- Site Admin exact-Site-scoped operational update
- Site Operator and Read Only scoped read behavior
- Immutable Tenant and Management Company relationship after creation
- Optimistic version locking
- Active Contract closure guard
- Atomic, redacted Site audit events
- Browser table mutation revoked; reviewed RPC command boundary applied
- Request-only lifecycle roles do not perform direct mutations while the
  maker-checker request queue remains unimplemented

Primary artifacts:

- `docs/01-plan/features/site-management.plan.md`
- `docs/02-design/features/site-management.design.md`
- `packages/application/src/site-service.ts`
- `packages/application/src/site-catalog-service.ts`
- `apps/web/app/[locale]/admin/sites/page.tsx`
- `supabase/migrations/20260719014506_phase_1_site_management.sql`
- `supabase/tests/database/phase_1_site_management.sql`

## 4. Validation evidence

- `pnpm verify`: passed using Node `v24.18.0` and Corepack `pnpm 10.34.5`
- Unit tests: 16 files, 71 tests passed
- WCJ: 100 total, W 100, C 100, J 100, 47 files
- DB static contract: 13 migrations and 6 database tests
- Secret scan: passed
- Immutable logo SHA-256 check: passed
- Next.js production build: passed
- Playwright Desktop/Mobile smoke: 26 tests passed
- Staging rollback runtime verification:
  - Super Admin, Platform Operator, Management Admin, and Site Admin boundaries
  - AAL/MFA rejection
  - stale version rejection
  - invalid timezone rejection
  - active Contract closure rejection
  - cross-tenant and cross-Site isolation
  - audit payload address redaction
  - rollback residue: 0 rows

Use the project-pinned package manager in a fresh session:

```sh
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" corepack pnpm verify
PATH="$HOME/.nvm/versions/node/v24.18.0/bin:$PATH" corepack pnpm e2e:smoke
```

Do not use the Codex bundled `pnpm 11`; the repository requires pnpm 10.

## 5. Remaining risks and external settings

- Do not claim Phase 1 complete until migration reset, local pgTAP,
  authenticated Site CRUD browser E2E, and tenant-isolation E2E pass.
- Docker was unavailable, so local migration reset and local pgTAP remain open.
- The staging SQL rollback tests prove database-level authenticated scope
  behavior, but a real browser-session Site CRUD journey is still required.
- Supabase Advisor reports authenticated `SECURITY DEFINER` RPC warnings. These
  RPCs are intentional command endpoints and independently enforce Auth user,
  AAL, active membership, role, scope, parent state, version, and audit rules.
- Supabase leaked-password protection is still disabled and should be enabled
  before production readiness.
- Management Admin and Site Admin lifecycle request/approval records are
  intentionally deferred until a maker-checker request entity is designed.

## 6. Exact next development step

Implement the authenticated Site CRUD and Tenant Isolation browser E2E gate.

1. Build a safe staging test-session fixture without storing credentials.
2. Exercise Super Admin Site create, operational update, contract update,
   suspend, reactivate, and close through the real KO/EN UI.
3. Exercise Management Admin and Site Admin scoped visibility and mutation.
4. Prove cross-tenant and cross-Site data never renders and cannot mutate.
5. Verify audit events and rollback or clean up all test fixtures.
6. Run WCJ, unit, DB, build, and Desktop/Mobile E2E again.
7. Create the next handoff when this feature unit finishes or if authentication
   setup errors begin repeating.

After this gate, the next product slice should be the Site lifecycle
request/approval queue or the QR inventory/batch lifecycle, according to the
master specification and approved sequencing.
