# Taptolk handoff — QR-only issuance recovery

## State

- Repository: `/Users/benjaminsong/Documents/Taptolk`
- Branch: `codex/phase-1-foundation`
- HEAD at checkpoint: `dd4c2d2`
- The worktree contains broad pre-existing user WIP. Do not reset, clean, stash,
  revert, or broadly stage it.

## Completed in this checkpoint

- Restored `/admin/qr-inventory` as the QR operations entry point; it now routes
  to the v2 QR-only operations surface instead of the legacy design wizard.
- Added QR-only generation request service, repository, server action, and
  migration `20260826090000_qr_only_generation_approval.sql`.
- QR-only requests create internal immutable QR-only compatibility data,
  automated quality evidence, and `FINAL_APPROVAL_PENDING` batches; no Queue/job
  is created before an independent final approval.
- Replaced the new-flow approval page with a QR-only approval queue. Legacy
  design/sample screens remain only for historical compatibility and are no
  longer reachable from the canonical issuance route.
- Converted the new request and approval forms to client-side pending/duplicate
  submission lockout while keeping server-side authorization and RPC mutation.
- Fixed approval wake ordering: the generation pipeline is woken only after the
  final approval RPC succeeds.
- Removed unused application/web direct-generation dead-path files. Existing
  applied legacy migrations remain untouched.

## Validation evidence

- `pnpm verify` PASS
- 95 test files / 577 tests PASS
- `pnpm validate:design-system` PASS
- `pnpm validate:wcj` PASS — 100/100/100/100
- Production build PASS

## External gates still required

- Apply the new Supabase migration in the intended staging project; local
  static checks do not prove the remote function, trigger, grants, or RLS state.
- Deploy a Preview from the exact reviewed commit after selective staging.
- Manual authenticated test with two Super Admin accounts: request QR-only
  issuance, confirm requester cannot self-approve, approve as the other account,
  verify Queue/progress, SVG bundle, site receiving, and QR scan/owner journey.
- Do not claim SOLAPI, Google OAuth, physical print/scan, or production readiness
  from `pnpm verify` alone.

## Exact next development step

1. Review the selective diff for only the QR-only files and stage a narrow
   commit; do not include unrelated dirty WIP.
2. Apply `20260826090000_qr_only_generation_approval.sql` to staging and inspect
   migration/function errors.
3. Deploy that narrow commit to a Preview and manually test the two-account
   approval → Queue → SVG → site operations journey.
