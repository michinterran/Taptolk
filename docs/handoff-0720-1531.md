# Taptolk Clean Local Supabase Handoff — 2026-07-20 15:31 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Clean-reset implementation commit: `37b20de`
- Public landing/Production commit: `6a78fd9`
- Prior handoff commit: `6dd05f3`
- Prior implementation/Production commit: `0e2a15e`
- Production URL: `https://taptolk.vercel.app`
- Active Production Cron definitions: `0`
- Pilot checklist: `docs/deployment/pilot-readiness-checklist.md`
- PDCA reports:
  - `docs/04-report/phase-0-9-development-closeout.report.md`
  - `docs/04-report/public-landing-onboarding.report.md`
- Decision: `AUTOMATED_DEVELOPMENT_READY / EXTERNAL_PILOT_GATES_PENDING`

## Completed scope

- Installed and started only the operator-approved Docker Desktop runtime and verified its
  Docker-compatible daemon.
- Completed two clean local Supabase resets.
- Repaired two older migrations so clean replay is deterministic whether legacy constraints or
  the already-redacted cleanup function are present.
- Added a repeatable `qr-generation` Queue baseline with bounded migration timeouts, server-only
  wrappers, browser-role denial, service-role access, and RLS.
- Added 12 Queue baseline pgTAP assertions.
- Applied the new Queue baseline migration to linked staging.
- Updated authenticated staging fixtures for the new `/{locale}/admin` role-selection landing.
- Bounded the 100-item QR decode concurrency to avoid local resource thrashing.
- Moved secret-bearing internal acceptance requests out of Playwright artifacts.
- Preserved the `1..100` per-Batch contract and the existing ten-Batch 1,000-item acceptance.
- Kept Production Cron deferred with zero active definitions. No Vercel plan change was made.

## Validation evidence

- Docker-compatible daemon: PASS.
- Clean local Supabase reset: PASS twice.
- Local pgTAP: all 23 database test files and 587 tests PASS.
- Queue baseline pgTAP: 12/12 PASS.
- Linked staging migration apply: PASS.
- Linked pgTAP: all 23 database test files PASS through `pnpm db:test:linked`.
- Authenticated staging focused suites:
  - Phase 9 bounded hardening: 2/2 PASS.
  - Public contact, notification, and privacy cleanup: 5/5 PASS.
  - QR inventory plus Site CRUD/tenant isolation: 19/19 PASS.
  - Owner activation: 2/2 PASS.
  - Aggregate: 28 PASS; the separate approved 10x100 acceptance remains an intentional opt-in
    skip.
- Lint: 324 files PASS.
- Typecheck: 19/19 tasks PASS.
- Unit: 53 files and 326 tests PASS.
- DB static contract: 56 migrations and 23 database tests PASS.
- Secret scan: 530 text files PASS.
- Immutable logo: required SHA-256 PASS.
- WCJ: 100 / C100 / J100 / W100 over 91 files.
- Production build: PASS. Turbo cache was set read-only for the terminal build confirmation
  because the default local cache writer stalled after the successful build.
- Deferred Production Cron verifier: PASS; active definitions remain zero.

The repository's linked database test entry point is `pnpm db:test:linked`. A raw
`supabase test db --linked` invocation does not reproduce the repository-configured pgTAP path and
must not replace the scripted gate.

## Unresolved risks and gates

1. A separate approved Production Supabase project and data region remain undecided.
2. Production SMS and CAPTCHA providers and their adapters remain undecided and unimplemented.
3. Production secret configuration remains intentionally absent.
4. Representative iOS/Android, VoiceOver/TalkBack, computed contrast, keyboard, and physical
   85mm print checks remain manual.
5. Production monitoring, alerting, rollback, incident ownership, domain, and pilot window remain
   undecided.
6. Actual-service Cron activation, same-hour replay, duplicate-zero proof, monitoring, secret
   rotation, and rollback rehearsal remain intentionally deferred. Hobby remains at active Cron
   zero.
7. The local Turbo cache writer stalled after a successful Production build in one `pnpm verify`
   run. The same build exits successfully with read-only cache; keep the final verification
   evidence explicit until the cache behavior is investigated.

## Exact next development step

Stop for the next user-owned decision: name or approve a separate Production Supabase project and
its data region, and grant access through the provider's access control. Do not create or select a
Production project or region without that approval.

After that exact approval only:

1. verify the approved Production target without printing credentials;
2. dry-run the complete migration set;
3. apply migrations through `20260720135421`;
4. run the repository-configured linked pgTAP gate against the approved target;
5. record aggregate-only validation evidence;
6. stop again for the separate SMS and CAPTCHA provider decisions.

Do not select an SMS/CAPTCHA provider, configure Production secrets, assign monitoring or incident
owners, change the Vercel plan, or activate Cron. Request Pro or Enterprise approval only at actual
hourly Cron activation time.

If a quantity above 100 per Batch is requested, stop and obtain separate quantity-policy design
approval before changing schema, UI, Worker, Queue, or export contracts.

## User-owned external setup

- Name or approve the separate Production Supabase project and data region, then grant access.
- Later, name the approved Production SMS and CAPTCHA providers before adapter implementation.
- Provide representative devices, assistive-technology operators, and the target 85mm printer.
- Name the accountable Production monitoring, rollback, incident, domain, and pilot-window
  owners.
- At actual service launch only, approve the Vercel plan change and active Cron release.
- Never provide database credentials, provider tokens, Cron bearers, phone numbers, messages,
  OTPs, QR/activation/response tokens, cookies, authorization headers, or secret values in Git,
  documentation, logs, screenshots, or chat.

## Continuation prompt

`docs/handoff-0720-1531.md를 읽고 Exact next development step부터 계속 진행해.`

## Session handoff protocol

Continue to follow the `AGENTS.md` Session handoff protocol. Every later handoff must record the
current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and required user-owned external setup, without credentials or secret values.
