# Taptolk Session Handoff — 2026-07-26 14:19 KST (통합본)

이 문서 하나만 읽고 이어받는다. **이 문서는 세션 상태 기록이다** — 설계 근거는
`DESIGN_SYSTEM.md` + `docs/design-canon/`, 범위 근거는 `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`와
승인된 `docs/development/` 문서다. handoff 산문을 화면·범위 근거로 인용하지 않는다(`AGENTS.md`).

> 이 통합본은 `handoff-0725-1314.md`(디자인 레인)와 `handoff-0726-1309.md`(Codex)를 **대체**한다.
> 두 문서는 `docs/archive/handoff/`로 이동했다.

## 0. 새 세션이 먼저 읽을 순서
1. `AGENTS.md` — 엔지니어링 계약(2026-07-25 권위 체인에 거버넌스 추가 반영. **대체가 아니라 추가**)
2. `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md` — 최상위 제품 범위
3. `docs/governance/OPERATING_MODEL.md` — Claude/Codex/Antigravity 레인 경계
4. `DESIGN_SYSTEM.md` §0 — 화면 완료 7조건
5. `docs/governance/DESIGN_CONSISTENCY_RULES.md` — 디자인 시스템 강제 규칙
6. `docs/design-canon/CONTRACTS.md` — 화면별 서버 DTO 계약
7. `docs/development/PROJECT_COMPLETION_PLAN.md` — 남은 작업 순서(스텝 A~F)
8. 이 문서

## 1. 체크포인트
- 경로 `/Users/benjaminsong/Documents/Taptolk` · 브랜치 `codex/phase-1-foundation`
- **HEAD·앞선 커밋 수는 하드코딩하지 않는다** — 새 세션이 직접 확인:
  ```bash
  git rev-parse --short HEAD
  git rev-list --count origin/codex/phase-1-foundation..HEAD   # 원격보다 앞선 수
  git status --short
  ```
  (작성 시점: 로컬이 원격보다 20+커밋 앞섬, **원격 push 안 함**. 미커밋 WIP 있음 — §4)
- 마지막으로 원격/데모에 반영된 구현 기준: `9887dc3 Complete owner PWA reply flow design`.
  이후 디자인·거버넌스 로컬 커밋과 현재 WIP는 아직 원격/데모 미반영.

## 1.5 환경 준비 (부트스트랩) — 정본 `docs/development/local-setup.md`
새 세션은 코드 착수 전에 환경부터 세운다. 막히면 여기부터 확인한다.
- **요구사항:** Node.js `24.18.0`(`.node-version`) · Corepack + pnpm `10.34.5`(`package.json` packageManager) ·
  Supabase Local 쓸 때 Docker Desktop.
- **설치:**
  ```bash
  corepack enable
  corepack pnpm install --frozen-lockfile
  cp .env.example .env.local     # 이미 있으면 건너뜀. .env.local은 커밋 금지
  ```
- **실행:** 웹만 `corepack pnpm dev:web` / Supabase Local+전체 `corepack pnpm dev:local`
  (후자는 Docker 필요). `supabase status`의 로컬 값만 `.env.local`에 쓴다.
- **주의:** `typecheck`가 `.next`를 지운다. 이후 dev가 500이면
  `rm -rf apps/web/.next` → `corepack pnpm build:packages` → dev 재기동.
  로컬 dev 500(owner/offline)은 Step A에서 고칠 대상이다.

## 2. 운영 모델 (binding) — `docs/governance/OPERATING_MODEL.md`
**세 레인, 한 규칙: 앱 코드를 메인에 쓰는 에이전트는 Codex 하나.**
- **Claude = 디자인 전용** — `tokens.css`·`design-canon/`·`DESIGN_SYSTEM.md`·거버넌스·워크오더/스펙. globals.css 안 건드림
- **Codex = 유일한 구현자** — `apps/**`(globals.css 포함)·`packages/**`(tokens.css 제외)·`supabase/**`·baseline
- **Antigravity = 리뷰·제안** — 메인라인에 쓰지 않음
- **절대 규칙:** 커밋 전 `git status`, 자기 레인 파일만 `git add <경로>`, **`git add -A`/`.` 금지**,
  한 커밋에 레인 섞지 않기, 다른 레인 미커밋 변경 stage 금지

## 3. 이 세션까지 준비된 것 (디자인 레인, 전부 커밋됨)
| 산출물 | 파일 |
|---|---|
| 운영모델·디자인규정·완료계획 | `docs/governance/OPERATING_MODEL.md`, `DESIGN_CONSISTENCY_RULES.md`, `docs/development/PROJECT_COMPLETION_PLAN.md` |
| 화면 전수 인벤토리(32개+상태) | `docs/development/design-page-inventory-0725.md` |
| 화면별 데이터 계약(PWA·사이트·콘솔·플랫폼·인증) | `docs/design-canon/CONTRACTS.md` |
| 인증(로그인/가입/MFA) 상태 폴리시 | `docs/development/workorder-codex-auth-states-0725.md` |
| 토큰 이관 워크오더 + `--tt-font-size-2xs` | `docs/development/workorder-codex-token-migration-0725.md`, `packages/ui/src/styles/tokens.css` |
| 콘솔·플랫폼 상태 정본 | `docs/design-canon/console-states.md` |
| 콘솔 눈대조 체크리스트(스텝 D용) | `docs/design-canon/console-compare-checklist.md` |
| 디자인 갤러리(아티팩트, 저장소 밖) | claude.ai/code/artifact/16c41222-0ddf-4d39-b352-edeb9ecef960 |

**확정 결정 4건**(CONTRACTS 반영): ①호출자 차량번호 뒤4자리 유지 ②평균시간 실측 전 숨김
③"연락 부탁드립니다" 멘트 **추가**(Codex: reason code + KO/EN) ④승인 큐 §11 해제 전 보류.

## 4. 현재 미커밋 WIP — 보존 필요 (Codex 레인)
`git status`로 재확인. 작성 시점 6개:
```text
M  apps/web/app/globals.css                     # 공개/관리자 caption·eyebrow·lead → --tt-pub-* 토큰 치환 중
M  apps/web/components/operations-dashboard-view.tsx  # 응답시간 단위 하드코딩 "s" → locale copy
M  apps/web/content/operations-copy.ts          # secondsUnit KO "초" / EN "s" 추가
M  config/design-system-baseline.json           # 위 토큰 치환으로 사라진 font-size 항목 제거
M  packages/qr-engine/src/render.ts             # QR decode 전 sharp flatten(#FFFFFF) — 투명 배경 판독 안정화
?? supabase/migrations/20260724000000_grant_qr_batch_samples_columns.sql  # qr_batch_samples SELECT grant(새 migration)
```
- 이 WIP는 **아직 verify 안 됨.** 버리거나 되돌리지 말 것. 의도 불명확하면 운영자에 질문.
- 전부 Codex 레인이다. `tokens.css`는 Claude 소유 — Codex가 임의 수정 금지.

## 5. Exact Next Step

### Step 0 — WIP 안정화 (첫 작업, 새 기능 아님)
1. `git status --branch --short`, `git diff --stat`으로 WIP 확인.
2. §4의 6개 파일을 읽고 의도 확인.
3. 검증:
   ```bash
   corepack pnpm validate:design-system
   corepack pnpm validate:wcj
   corepack pnpm --filter @taptolk/qr-engine build
   corepack pnpm db:check
   ```
4. 통과하면 **6개 파일만 명시 경로로 stage**(`git add -A` 금지). 성격이 다르므로 논리 단위로
   나눠 커밋 권장(예: ① 토큰 치환 globals.css+baseline, ② operations 카피, ③ qr-engine render, ④ grant migration).
5. 그 다음에만 Step A로.

### Step A — 기반 복구 (`PROJECT_COMPLETION_PLAN.md` §3.1·§4)
- **로컬 dev 500 수정**: 대상 `apps/web/app/[locale]/(public)/owner/offline`(JSON 처리 버그).
  `corepack pnpm dev`로 공개·콘솔 화면 200 확인.
- `.next` 로컬 잔재가 grep/검증 오염 시 삭제 후 재검증(소스 삭제와 구분).

### Step B — 미결 배선 (`CONTRACTS.md` + 계획서 §4)
1. 🔴 활성화 완결 응답에서 `activationCode` 회귀 sweep.
2. 🔴 차주 재진입 엔드포인트 2개(`/api/owner/session/reclaim/*`) 최신 계약 대조.
3. 스캔 판정 RPC/route가 스티커 소지 기반으로 일관되는지.
4. PWA/owner 메시지·히스토리·세션 필드가 CONTRACTS와 일치하는지 audit → 빠진 필드만 추가.
5. "연락 부탁드립니다" reason code + KO/EN copy 추가(결정 3).
> 2026-07-24에 PWA 답장 흐름은 상당 부분 구현됨 → 새로 만들기보다 **현재 코드 ↔ 최신 CONTRACTS audit** 우선.

### 이후 — 🟣 토큰 이관 / 상태 구현
- `workorder-codex-token-migration-0725.md`: 스테일 baseline 먼저 `--update-baseline` → 클러스터별
  (qr→operations→admin-catalog…) 치환 → 정본 대조 → `--update-baseline`.
- `console-states.md` 신규 2개(스켈레톤·잠금) + `workorder-codex-auth-states-0725.md`.

## 6. 검증 기준
- 작은 slice마다: `corepack pnpm validate:design-system`, `corepack pnpm validate:wcj`.
- DB/API·공유 도메인 변경 시: `corepack pnpm db:check`, `--filter @taptolk/application build`, `--filter @taptolk/web typecheck`.
- **slice/phase 완료 보고 전: `corepack pnpm verify`.**
- 자동 검증 ≠ 정본 일치. 화면 변경은 320/768/1280/1920·실기기·키보드·스크린리더·대비 수동 검토.
  콘솔 실화면 눈대조는 스텝 D(스테이징 로그인) 이후.

## 7. 착수 금지 (운영자 승인 없이)
Production 배포·Production Supabase·live domain·Production Cron·live Kakao/SMS·Vercel 설정 변경 ·
QR `1..100` 초과 · 승인 워크플로 신설 · S7/S9 임계 임의결정 · 로고 crop/recolor/redraw/filter/stretch ·
**차주 전화번호를 브라우저 응답/화면/로그/문서에 노출(뒤 4자리도 금지)** ·
**없는 값을 0/default/mock으로 내려보내기** · **기존 migration 수정(새 migration만)**.

## 8. 외부/운영자 게이트 (Codex·Claude가 못 여는 것)
- **스텝 D — 스테이징 로그인 복구**: Vercel Supabase env 설정+Redeploy
  (`docs/deployment/staging-login-env-checklist.md`, 값 제외) → 이후 콘솔 실화면 눈대조 가능.
- **스텝 E — 파일럿 외부 게이트**: Production Supabase 프로젝트/리전 · SMS/CAPTCHA/Kakao provider·요금제 ·
  실기기/a11y/85mm 인쇄 검수 · 모니터링·롤백·인시던트·파일럿 담당 · Cron 플랜/활성.
- **스텝 F — BM 확정**: 과금 가격 · 인쇄 규격·재질 · 개인정보 보유기간 · 카카오 알림톡 계약 · 파일럿 고객.

## 9. 새 세션에 붙여넣을 명령 프롬프트
```text
Taptolk 저장소 `/Users/benjaminsong/Documents/Taptolk`에서 이어간다. 브랜치 `codex/phase-1-foundation`.
너는 Claude/디자인이 아니라 Codex 구현자다.

먼저 이 순서로 읽어라:
1. AGENTS.md
2. TAPTOLK_MASTER_DEVELOPMENT_SPEC.md
3. docs/governance/OPERATING_MODEL.md
4. DESIGN_SYSTEM.md §0
5. docs/governance/DESIGN_CONSISTENCY_RULES.md
6. docs/design-canon/CONTRACTS.md
7. docs/development/PROJECT_COMPLETION_PLAN.md
8. docs/handoff-0726-1419.md   ← 이 통합 handoff

규칙:
- 앱 코드·DB/API를 붙이되 디자인 정본·토큰 값은 임의로 바꾸지 마라(tokens.css는 Claude 소유).
- `git add -A`/`.` 금지. 자기 레인 파일만 명시 경로로 stage. 한 커밋에 레인 섞지 마라.
- 차주 전화번호는 브라우저로 절대 보내지 마라(뒤 4자리도 금지).
- 기존 migration 수정 금지(새 파일만). 없는 값은 0/default/mock으로 채우지 말고 비워라.
- 화면은 handoff 산문이 아니라 정본·CONTRACTS를 보고 구현. 답이 없으면 추측 말고 되돌려라.

환경 준비(handoff §1.5, docs/development/local-setup.md):
  corepack enable
  corepack pnpm install --frozen-lockfile
  cp .env.example .env.local     # 없을 때만
  # Node 24.18.0 · pnpm 10.34.5 · Supabase Local 쓰면 Docker 필요

먼저 현재 상태를 직접 확인하라(하드코딩된 커밋 번호를 믿지 말 것):
  git rev-parse --short HEAD
  git rev-list --count origin/codex/phase-1-foundation..HEAD
  git status --short

Exact next step:
1. 미커밋 WIP 6개 파일을 읽고 의도 확인(handoff §4).
2. corepack pnpm validate:design-system / validate:wcj /
   --filter @taptolk/qr-engine build / db:check 실행.
3. 통과하면 WIP 6개만 명시 경로로 stage하고 논리 단위로 분리 커밋.
4. PROJECT_COMPLETION_PLAN Step A: 로컬 dev 500(owner/offline) 수정.
5. Step B: activationCode 회귀 sweep, 재진입 2개, 스캔 판정, PWA 필드/히스토리/메시지 CONTRACTS audit,
   "연락 부탁드립니다" reason code + KO/EN.

각 slice마다 validate:design-system, validate:wcj. 완료 보고 전 corepack pnpm verify.
보고는 구현 완료 / 자동검증(명령·결과) / 수동검증 대기 / 외부게이트 / 다음 한 단계로 나눠 한국어로.
```

## 10. 보고 형식
완료 보고는 **구현 완료(파일·커밋) / 자동 검증(명령·결과) / 수동 검증 대기 / 외부 게이트 /
다음 한 단계(하나만)** 로 구분한다. credential·전화번호·OTP·QR token·secret은 Git·문서·로그·
스크린샷에 남기지 않는다. 이 handoff 규약을 다음 handoff에도 그대로 이어간다.
