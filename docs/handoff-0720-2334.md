# Taptolk Server-only Stage Policy Handoff — 2026-07-20 23:34 KST

## Checkpoint

- Branch: `codex/phase-1-foundation`
- Task 1 implementation commit: `9c768b5`
- Prior safe-checkpoint commit: `133382e`
- Production-deployed source commit: `f022ca0`
- Production deployment: `dpl_DTvj4RjQ68MnVYAXQKR6KexiEEM7` READY
- Stable URL: `https://taptolk.vercel.app`
- Active Production Cron definitions: `0`
- Current stage: `DEVELOPMENT_TEST`
- Decision: `TASK_1_AUTOMATED_COMPLETE / EXTERNAL_PILOT_GATES_PENDING`

## Completed scope

- Added typed `DEVELOPMENT_TEST` and `SERVICE` policy through
  `@taptolk/config/stage/server`.
- Made missing, blank, and invalid stage values fail closed to `SERVICE`.
- Ensured browser variables, query values, cookies, and headers cannot select the stage.
- Added the central `requireDevelopmentTestStage` denial boundary.
- Added a Service test-surface registry and static CI guard.
- Included `verify:service-stage` in `pnpm verify`.
- Proved an actual Client Component import fails the Next production build through `server-only`.
- Added no Test Lab, access grant, persona, mock provider, role, or Production configuration.

## Validation evidence

- Focused config typecheck: PASS.
- Stage policy unit tests: 7/7 PASS.
- Service leak guard: PASS with zero registered test-only product surfaces.
- Actual Client Component import proof: expected Next build failure; proof file removed.
- Normal `pnpm verify`: lint 329, typecheck 19/19, unit 54 files/334 tests, DB 57/23,
  secret scan 544, logo PASS, Service guard PASS, Cron deferred PASS, WCJ 100/92,
  build 11/11 PASS.
- Linked pgTAP after Task 1: 23 files / 590 tests PASS.
- Authenticated Staging E2E after Task 1: 28 PASS; one separate 10x100 opt-in skip.
- Production remains on deployment `dpl_DTvj4RjQ68MnVYAXQKR6KexiEEM7`; Task 1 was not
  separately deployed.

## Unresolved risks and gates

1. Task 2 single-login server role routing is not implemented.
2. Task 3 staging test access grants and later Test Lab/persona work are not implemented.
3. Production Supabase project and region remain undecided.
4. SMS/CAPTCHA providers and Production secrets remain undecided.
5. Monitoring, rollback, incident ownership, real-device, assistive-technology, contrast, and
   physical print checks remain open.
6. Actual-service Cron activation and Vercel plan change remain deferred.

## Exact next development step

Review and implement workorder Task 2 only:

1. keep one visible administrator login entry;
2. perform post-login routing from the server-loaded approved profile, membership, scope, and MFA;
3. retain distinct customer and platform screens and authorization boundaries;
4. add KO/EN typed states and WCJ coverage;
5. prove role routing in authenticated Staging E2E and run `pnpm verify`.

Do not begin Task 3 grants or Test Lab until Task 2 passes. Do not derive authorization from
browser metadata or select any external Production setting.

## User-owned external setup

- No new external setup is required for Task 2 implementation.
- Production project/region, providers, secrets, operations owners, plan, and actual Cron
  activation remain user-owned decisions.

## Session handoff protocol

Create `docs/handoff-MMDD-HHmm.md` when a major feature unit is complete or when the same error
begins repeating and a fresh session would reduce context and token waste. Every handoff records
the current branch and commit, completed scope, validation evidence, unresolved risks, exact next
step, and any required user-owned external setup. Never include credentials or secret values.
After writing the handoff, notify the user that this is a safe checkpoint and recommend opening a
new Codex session with the handoff path.
