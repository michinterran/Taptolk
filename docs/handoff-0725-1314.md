# Taptolk Session Handoff — 2026-07-25 13:14 KST

이 문서만 읽고 이어받는다. **세션 상태 기록일 뿐 설계·범위 근거가 아니다**(`AGENTS.md`).
설계 근거는 `DESIGN_SYSTEM.md` + `docs/design-canon/`, 범위는 `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md`.

## 1. Checkpoint
- 브랜치 `codex/phase-1-foundation` · HEAD **`3d13aab`** · 원격 push 안 함
- 이번 세션은 **디자인 레인 준비 작업**(문서·토큰만). 앱 코드 변경 없음
- **⚠️ 워킹트리에 Codex의 미커밋 변경이 남아 있다**(이번 세션에서 건드리지 않음):
  `apps/web/app/globals.css`, `apps/web/components/operations-dashboard-view.tsx`,
  `apps/web/content/operations-copy.ts`, `config/design-system-baseline.json`,
  `packages/qr-engine/src/render.ts`, `supabase/migrations/20260724000000_grant_qr_batch_samples_columns.sql`
  → **Codex가 자기 레인으로 먼저 커밋**해 잃지 않게 한다(`OPERATING_MODEL.md` §6)

## 2. 이 세션에서 확정된 운영 모델 (되돌리려면 운영자에게)
**세 레인, 한 규칙: 앱 코드를 메인에 쓰는 에이전트는 Codex 하나.** 정본 `docs/governance/OPERATING_MODEL.md`.
- **Claude = 디자인 전용** — `tokens.css`·`design-canon/`·`DESIGN_SYSTEM.md`·거버넌스·스펙만. globals.css 안 건드림
- **Codex = 유일한 구현자** — `apps/**`(globals.css 포함)·`packages/**`(tokens.css 제외)·`supabase/**`
- **Antigravity = 리뷰·제안** — 메인라인에 쓰지 않음
- 커밋 전 `git status`, 자기 레인만 `git add <path>`, `git add -A`/`.` 금지

## 3. 이 세션에서 준비된 것 (디자인 레인, 전부 커밋됨)
| 산출물 | 파일 | 커밋 |
|---|---|---|
| 운영모델·디자인규정·완료계획 | `docs/governance/*`, `docs/development/PROJECT_COMPLETION_PLAN.md` | f2e21b7 |
| 화면 전수 인벤토리 | `docs/development/design-page-inventory-0725.md` | ae1e623 |
| 콘솔·플랫폼 데이터 계약 | `docs/design-canon/CONTRACTS.md` | 53fefa5 |
| 인증(로그인/가입/MFA) 상태 폴리시 스펙 | `docs/development/workorder-codex-auth-states-0725.md` | a8fb895 |
| 레퍼런스↔시스템 결정 4건 | CONTRACTS.md | f88518c |
| 토큰 이관 워크오더 + `--tt-font-size-2xs` 추가 | `docs/development/workorder-codex-token-migration-0725.md`, `tokens.css` | a66071e |
| 콘솔·플랫폼 상태 정본 | `docs/design-canon/console-states.md` | 3d13aab |
| 콘솔 눈대조 체크리스트(스텝 D용) | `docs/design-canon/console-compare-checklist.md` | (이 커밋) |
| 디자인 갤러리(아티팩트, 미커밋·저장소 밖) | claude.ai/code/artifact/16c41222-0ddf-4d39-b352-edeb9ecef960 | — |

**이번 세션 확정 결정 4건**(CONTRACTS 반영): ①호출자 차량번호 뒤4자리 유지 ②평균시간 실측 전 숨김
③"연락 부탁드립니다" 멘트 **추가**(Codex: reason code + KO/EN) ④승인 큐 §11 해제 전 보류.

## 4. Exact next step — Codex (토큰 리셋 후 착수)
순서 정본 `docs/development/PROJECT_COMPLETION_PLAN.md` §6, 화면 상태 `design-page-inventory-0725.md`.

1. **스텝 A — 기반 복구**: 워킹트리 레인별 분리 커밋 → **로컬 dev 500(`owner/offline` JSON) 수정**.
   이게 있어야 어떤 화면도 실기 검증 가능.
2. **스텝 B — 🔴 차주 여정 배선**(막힌 것): `complete`에서 `activationCode` 제거 → 재진입
   엔드포인트 2개(`/api/owner/session/reclaim/*`) → 스캔 판정 RPC. 계약은 `CONTRACTS.md` [A]·[R]·스캔.
3. **🟡 PWA 배선**: 세션 필드(`plateLast4`·`callerMessage`·`elapsedSeconds`…), owner 목록/이력/채널/중지·해지.
4. **사이트 입고 엔드포인트 + 6단계 위자드**: 워크오더 `workorder-codex-site-operations-0724.md`·`-qr-wizard-6step-0724.md`.
5. **"연락 부탁드립니다" 멘트**: `CONTACT_REASON_CODES` + KO/EN 카피(결정 3).
6. **🟣 토큰 이관**: `workorder-codex-token-migration-0725.md` — 스테일 43건 `--update-baseline` 먼저 →
   클러스터별(qr→operations→admin-catalog…) 치환 → 정본 대조 → `--update-baseline`.
7. **상태 구현**: `console-states.md` 신규 2개(스켈레톤·잠금) + 인증 상태(`workorder-codex-auth-states-0725.md`).

각 단계: `validate:design-system`·`validate:wcj`·`verify` 실행, 스펙 §31 형식 보고, PR→Antigravity 리뷰.

## 5. 운영자 몫 (Codex·Claude가 못 여는 것)
- **스텝 D — 스테이징 로그인 복구**: Vercel에 Supabase 환경변수 설정+Redeploy
  (`docs/deployment/staging-login-env-checklist.md`, 값 제외). **이후에야 콘솔 실화면 눈대조 가능**.
- **스텝 E — 파일럿 외부 게이트**: Production Supabase·SMS/CAPTCHA provider·실기기/a11y/85mm 인쇄
  검수·모니터링/인시던트 담당·Cron 활성(closeout §5).
- **스텝 F — BM 확정**: 과금 가격·인쇄 규격·개인정보 보유기간·카카오 알림톡 계약·파일럿 고객.

## 6. 착수 금지 (별도 승인 없이)
Production 배포·도메인·Cron 활성·live Kakao/SMS·Vercel 설정·Production Supabase·**승인 워크플로 신설**·
**QR `1..100` 초과**·S7/S9 임계 임의결정·로고 크롭/재색·**화면을 handoff 산문 근거로 변경**.

## 7. 검증 상태
- 이번 세션은 문서·토큰만 변경 → 게이트 재실행 안 함. **Codex가 코드 착수 시 `verify` 실행**.
- 직전 코드 기준(closeout 2026-07-20/21): Phase 0–9 자동/스테이징 경계 완료(pgTAP 587·unit 326·
  E2E 28–30·WCJ 100·build PASS). 남은 🔴/🟡는 최근 디자인 결정에 따른 재배선.

## 8. 보고·보안
구현/자동검증/수동검증 대기 구분. PASS엔 명령·범위·수. credential·전화번호·OTP·secret은
Git·문서·로그·스크린샷에 남기지 않는다. 이 핸드오프 규약을 다음 핸드오프에 그대로 이어간다(`AGENTS.md`).
