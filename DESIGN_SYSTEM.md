# Taptolk Design System

**이 문서가 화면 작업의 유일한 정본이다.** handoff 문서·워크오더·PRD의 서술은 설계 근거가
아니다. 그것들은 세션 상태와 요구사항을 기록할 뿐이고, **화면이 어떻게 생겨야 하는지는 여기와
`docs/design-canon/`만이 정한다.**

이 문서가 생긴 이유: 여러 세션에 걸쳐 handoff의 산문 설명("3구역으로 분리한다" 같은)이 설계
근거로 쓰였고, 그때마다 이미 컴펌된 시안과 다른 화면이 만들어졌다. 정본이 코드베이스 밖(git
히스토리, 다운로드 폴더, 사라진 scratchpad)에 있었던 것이 원인이다.

---

## 0. 작업 순서 (건너뛰지 말 것)

화면을 만들거나 고치기 전에 **반드시** 이 순서를 따른다.

> 2026-08-20 운영자 결정: 랜딩·로그인·가입은 기존 공개/인증 표면으로 유지하고,
> 슈퍼어드민·관리회사·사이트 어드민의 **전체 백오피스 콘솔**은
> `docs/design-canon/CONSOLE_DESIGN_SYSTEM_V3.md`를 새 기준으로 삼는다. 첨부 레퍼런스처럼
> 검정 운영실, 큰 한국어 제목, 카드형 사이드바, hairline frame, 업무 queue 중심 UX를 기본으로
> 한다. QR 운영은 생성 화면이 아니라 제작·배송·입고·차주 등록·예외까지 이어지는 lifecycle
> command center로 다룬다. 기존 v2 문서는 이전 기준선과 마이그레이션 기록으로만 참조한다.

1. **정본을 띄운다.** `docs/design-canon/`을 정적 서버로 열고 대상 화면의 시안을 화면에 띄운다.
   ```bash
   cd docs/design-canon && python3 -m http.server 8907
   ```
   `reference-1-admin-console.png`은 이미지 뷰어로 연다.
2. **패턴을 §3에서 고른다.** 만들려는 화면이 어떤 패턴 조합인지 먼저 정한다.
   목록 화면인가, 위자드인가, 상세인가.
3. **토큰으로 작업한다.** 값이 틀리면 `packages/ui/src/styles/tokens.css`를 고친다.
   화면별 CSS 블록에 수치를 다시 쓰지 않는다(§2).
4. **게이트를 통과시킨다.**
   ```bash
   corepack pnpm validate:design-system
   corepack pnpm validate:wcj
   ```
5. **눈으로 대조한다.** 구현 화면과 정본 시안을 같은 뷰포트 폭(1487px)에서 나란히 두고 비교한다.
   다른 점이 있으면 고치거나, 고칠 수 없으면 사용자에게 **구체적으로** 묻는다.

**금지:** 정본을 열어보지 않고 화면을 만드는 것. 이것이 반복된 실패의 단일 원인이었다.

### 화면 1장이 끝났다는 것 (7개 모두)

디자인은 Claude가, 백엔드·기능은 Codex가 붙인다(운영자 확정 2026-07-24). 아래 7개를
만족해야 **Codex가 붙일 수 있는 상태**다. 완료 기준이 화면마다 달랐던 것이 화면을
반복해서 다시 만든 원인이었다.

1. **정본 근거** — 어느 시안의 어디를 봤는지. 시안이 없으면 §3 패턴 조합을 명시
2. **토큰** — 새 값은 `packages/ui/src/styles/tokens.css`에. 화면 CSS에 수치 없음
3. **프리미티브** — `packages/ui`에서 조합 가능. 화면이 마크업을 다시 쓰지 않음
4. **KO/EN 카피 계약** — 타입 있는 카탈로그. 컴포넌트에 문구 없음
5. **상태 전부** — 빈·로딩·오류·권한없음·한도초과. **나중에 몰아서 붙이지 않는다** —
   상태를 뒤에 넣으면 레이아웃이 흔들려 정본과 다시 대조해야 한다
6. **데이터 계약** — `docs/design-canon/CONTRACTS.md`에 "서버가 줘야 하는 것" 한 행.
   **디자인은 스키마를 기다리지 않는다.** 서버가 아직 안 주는 값은 계약으로 선언하고
   화면은 그 자리를 비운다(§4)
7. **눈으로 대조** — 같은 뷰포트에서 정본과 나란히 두고 확인

순서와 진행 상태는 `docs/development/plan-design-completion-0724.md`.

---

## 1. 정본 (`docs/design-canon/`)

| 파일 | 무엇의 정본인가 |
|---|---|
| `CONSOLE_DESIGN_SYSTEM_V3.md` | **슈퍼어드민·관리회사·사이트 어드민 전체 운영 콘솔 v3.** 첨부 레퍼런스 기반의 검정 운영실, 큰 제목, 카드형 사이드바, 업무 queue 중심 UX |
| `CONSOLE_DESIGN_SYSTEM_V2.md` | 이전 운영 콘솔 기준선. v3 전환 중 기존 기능·데이터 계약과 마이그레이션 기록을 확인할 때만 참조 |
| `reference-1-admin-console.png` | **관리자 콘솔 전체의 시각 언어.** 셸·목록 화면·우측 레일·테이블·지표 스트립 |
| `console-qr-wizard.html` | QR 제작 위자드 (스테퍼 + 패널 + 미리보기 레일) |
| `console-shell.html` | 셸·사이드바·상단바 |
| `console-pages.html` | 목록/관리 화면들 |
| `console-detail.html` | 상세 화면 |
| `console-site-operations.html` | **사이트 운영** — 입고·재고, 디자인 보관. 슈퍼어드민이 배송한 배치를 사이트가 입고 |
| `console-states.md` | **콘솔·플랫폼 상태 정본** — 빈·로딩·오류·권한없음·한도·알수없음. 완료조건 5번 |
| `sticker-previews/*.png` | 템플릿 선택 카드에 쓰는 실제 렌더 결과 |
| `pwa/01-caller-compose.png` | **PWA** — 스캔 직후 호출자가 상황을 고르고 메시지를 보내는 화면 |
| `pwa/02-caller-sent-waiting.png` | **PWA** — 전달 완료, 차주 확인 대기 |
| `pwa/03-owner-reply.png` | **PWA** — 차주가 받은 메시지에 응답 |
| `pwa/04-caller-response-received.png` | **PWA** — 차주 응답 도착, 호출 종료 |
| `pwa/sticker-physical.png` | 실물 스티커 |
| `pwa/README.md` | **시안이 없는 모바일 화면의 정본** — 활성화 3단계, 4개 탭, 오류 상태. Claude 설계, 승인 대기 |

**PWA 공통**: 상단 taptolk 로고, 흰 카드 스택, 선택지는 아이콘 + 한 줄, 가장 많이 쓰는
선택지에 브랜드 틴트, 하단 primary 버튼 1개(로고 마크 + 라벨),
하단 탭 `MESSAGES · ALERT · HISTORY · SETTINGS`. **차량번호는 브랜드색 대형 수치.**

정본은 **배포되지 않는 경로**에 둔다. `apps/web/public/`에 프로토타입을 두지 않는다.

### 정본에 없는 화면을 만들 때

임의로 만들지 말고 **§3의 패턴을 조합**한다. 새 패턴이 필요하면 사용자 승인을 받고 §3에 추가한다.

---

## 2. 단일 정의 규칙 (CSS로 강제됨)

**globals.css는 7,000줄이 넘고, 한때 68개 규칙이 각자 카드를 정의하고 있었다.** 그 상태에서는
한 화면을 고쳐도 다음 화면이 그대로다. 그래서 아래 세 가지는 규칙이다.

1. **새 콘솔 표면 → `apps/web/app/globals.css` 최하단 "콘솔 표면 규칙" 블록에 셀렉터만 추가한다.**
   `background`/`border`/`border-radius`/`padding` 세트를 또 쓰지 않는다.
2. **값을 바꿔야 하면 토큰을 바꾼다.** 콘솔 전체가 함께 움직인다.
3. **카드 안에 카드 금지.** 구분이 필요하면 헤어라인 하나다.

화면별 CSS 블록이 가져도 되는 것: **컬럼 `min-width`, `text-overflow` 처리, 컬럼별 폭 제한,
그리드 열 수.** 그 외 수치(padding·radius·font-size·font-weight·height·gap)는 토큰에서 온다.

**모바일(`.tt-m-*`)은 부채 없이 시작한다.** "모바일 표면 규칙" 블록이 유일한 정의이고
게이트가 **첫 줄부터** 검사한다. 콘솔처럼 baseline에 기록된 예외가 없다 —
`.tt-m-` 규칙이 토큰 대신 수치를 쓰면 즉시 실패한다.

이 규칙은 `corepack pnpm validate:design-system`이 검사한다.

---

## 3. 패턴 (레퍼런스1 실측)

측정은 레퍼런스1을 자체 폭 1487 CSS px에서 픽셀 단위로 읽은 값이다.

### 3.1 셸

```
┌──────────┬────────────────────────────────────────────────┐
│ 사이드바  │ 상단바  [범위 선택]        [KO|ENG] [시각] [계정] │
│ 206px    ├────────────────────────────────────────────────┤
│          │ 페이지 본문 (좌우 여백 --tt-console-gutter)      │
```

- 사이드바 `--tt-sidebar-width` `12.875rem` (206px), 흰 배경, 우측 헤어라인
- 항목 높이 `2.5rem`, pitch 50px, radius `0.625rem`
- 활성 항목: 브랜드 8% 틴트 알약 + 브랜드 강조색 글자. **왼쪽 inset 바는 없다**
- 그룹 라벨: `0.62rem` · 굵게 · 대문자 · muted
- 본문은 사이드바 옆 공간을 **채운다**(`--tt-console-max-width: none`)

### 3.2 페이지 헤더

```
서비스 운영                        ← eyebrow, 브랜드색, --tt-label-size
QR 스티커 발주                     ← h1, --tt-heading-1-size (26px)
현장·디자인·수량을 정해…            ← 설명, muted, --tt-body-size
                                      [우측 정렬 주 액션 버튼]
```

h1과 같은 줄 오른쪽에 주 액션 하나. 그 이상은 두지 않는다.

### 3.3 지표 스트립

**한 장의 카드 안에 셀들.** 셀마다 카드를 그리지 않는다(박스-인-박스의 근원이었다).

- 스트립 = 프레임만, `padding: 0`
- 셀 사이는 세로 헤어라인
- 셀 = 아이콘 · 라벨(`--tt-label-size`, muted) · 값(`--tt-stat-value-size`, tabular) · 보조줄
- **값이 없으면 `—`.** 분모 0을 1로 바꿔 100%를 만들지 않는다

### 3.4 목록 화면 (레퍼런스1의 본체)

```
┌─────────────────────────────────┐ ┌──────────────┐
│ [검색] [필터▾] [필터▾] [내보내기] │ │ 승인 대기 (5) │
│ ─────────────────────────────── │ │ ┌──────────┐ │
│  테이블 (헤더 밴드 + 48px 행)     │ │ │ 배지      │ │
│ ─────────────────────────────── │ │ │ 제목      │ │
│  총 28개  ‹ 1 2 3 ›  [10개▾]    │ │ │ [거절][승인]│ │
└─────────────────────────────────┘ └──────────────┘
```

- **본문 카드 + 우측 레일** 2단. 레일은 고정 폭, 본문은 나머지를 채운다
- 필터바·테이블·페이지네이션은 **같은 카드 안**에 헤어라인으로 나뉜다
- 우측 레일 항목: 종류 배지 · 제목 · 소속 · 시각 · `거절`/`승인` 인라인 버튼
- 레일 하단에 전체 관리 버튼 1개

### 3.5 테이블

| 항목 | 토큰 | 값 |
|---|---|---|
| 행 최소 높이 | `--tt-table-row-min-height` | `4rem` (64px) |
| 헤더 밴드 높이 | `--tt-table-header-min-height` | `2.75rem` (44px) |
| 셀 padding | `--tt-table-cell-padding-block/-inline` | `0.75rem` / `1rem` |
| 본문 크기 | `--tt-table-font-size` | `0.875rem` |
| 헤더 크기 | `--tt-table-header-font-size` | `0.75rem` |
| 헤더 굵기 | `--tt-table-header-font-weight` | `500` |
| 헤더 변환 | `--tt-table-header-transform` | `none` — **대문자 없음** |
| 헤더 배경 | `--tt-table-header-surface` | `#f7f8fa` |
| 행 구분선 | `--tt-table-divider-color` | `#f0f1f4` — **카드 테두리보다 옅다** |
| 셀 보조줄 | `--tt-table-meta-font-size` | `0.75rem` |

**헤더는 대문자 캡션이 아니다.** muted·medium이고 옅은 밴드 위에 놓인다.
한국어는 대문자가 없으므로 `text-transform: uppercase`와 넓은 `letter-spacing`은 자간만 망가뜨린다.

셀 안에서 쓰는 것: 상태 알약, 가로 미터 바(값 + 바), 2줄 셀(제목 + 보조줄), 배지, 고스트 버튼.

### 3.6 위자드 (QR 제작)

**흐름은 6단계, 한 페이지다. QR 제작은 슈퍼어드민 전용이다**(운영자 확정 2026-07-24).

```
① 관리회사 → ② 사이트 → ③ 디자인 → ④ 수량 → ⑤ 검토 → ⑥ 제작·발주·배송
   납품처를 정한다      템플릿+상단브랜드  100 이하 분할  샘플·디코드   인쇄 발주·배송
```

**슈퍼어드민만 QR을 만들고, 인쇄 발주와 배송까지 한 뒤 관리회사·사이트에 납품한다.**
그래서:

- **권한별 잠금 단계가 없다.** 예전 정본은 ④단계 승인 버튼을 다른 역할에게 잠갔지만,
  이제 슈퍼어드민만 위자드에 들어오므로 잠금·읽기전용 분기 자체가 사라졌다
- **관리회사 선택이 첫 단계로 돌아왔다.** 슈퍼어드민은 모든 관리회사·모든 사이트를
  보므로 사이트를 평면 목록으로 한 번에 보여줄 수 없다. 회사를 먼저 고르고(①) 그 회사의
  사이트를 고른다(②). 납품은 물리적 배송이라 목적지를 틀리면 스티커가 엉뚱한 곳으로 간다
- **디자인은 한 단계다.** 템플릿과 상단 브랜드는 같은 미리보기를 보며 정하므로 통합했다
- **검토(⑤)는 승인 큐가 아니라 샘플·디코드 게이트다.** 생성자와 승인자가 같아졌으므로,
  이 단계는 100장을 찍기 전에 샘플이 실제로 스캔되는지 확인하는 자리다
- **입고는 위자드에 없다.** 제작·발주·배송(⑥)까지가 슈퍼어드민의 일이고, 입고(수령)는
  받는 사이트가 하는 downstream이다. 사이트 운영 화면 또는 라이프사이클 상태에서 다룬다

**`?section=order|approvals|tracking` 같은 구역 분리 URL을 만들지 않는다.** 3구역
분리(발주/승인 큐/제작 추적)는 폐지됐고, 검토와 제작·발주·배송은 위자드 단계로 남는다.


```
┌───────────────────────────────────────────────────┐
│ ✓ 관리회사  ✓ 사이트  ③ 디자인  ④ 수량  ⑤ 검토 …  │  ← 스테퍼 = 카드 1장
└───────────────────────────────────────────────────┘
┌──────────────────────────────────┐ ┌─────────────┐
│ 단계 제목                          │ │ 실시간 미리보기│
│ 설명                              │ │  [스티커 렌더] │
│ …단계 본문…                        │ │  캡션         │
│ ────────────────────────────────  │ │  템플릿  블루…│
│ [이전]                 [다음 · 수량]│ │  규격   85mm…│
└──────────────────────────────────┘ └─────────────┘
```

- **스테퍼는 카드 한 장**, 셀마다 카드를 그리지 않는다
- 셀 = 원형 번호 + 제목 + 보조줄. 완료 = 초록 체크, 현재 = 브랜드 틴트로 셀 전체를 채움
- **본문 패널 1장 + 우측 미리보기 레일 고정.** 미리보기는 **실제 생성 엔진 렌더**이고
  선택을 바꾸면 즉시 반영된다
- 패널 하단 `이전` / `다음 · <다음 단계 이름>` — 왼쪽 ghost, 오른쪽 primary
- 미리보기 레일 하단 kv 목록: 라벨 muted 좌측 / 값 굵게 우측 정렬

**선택 카드**(템플릿 선택): 렌더 이미지 위, 굵은 제목, muted 보조줄. 선택 시 브랜드 테두리 + 틴트.

**선택 행**(상단 브랜드): 전체 폭 테두리 행, 라디오 + 제목 + 보조줄, 우측에 상태 배지.
선택 시 브랜드 테두리 + 틴트.

**고지 노트**: 성공(연녹) `✓`, 경고(연황) `⚠`, 위험(연적). 본문 좌측에 색 바.

### 3.7 타이포

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--tt-heading-1-size` | `1.5rem` | 페이지 제목 |
| `--tt-heading-2-size` | `1.125rem` | 카드·레일 제목 |
| `--tt-heading-3-size` | `1rem` | 패널 안 소제목 |
| `--tt-body-size` | `0.875rem` | 본문 |
| `--tt-label-size` | `0.75rem` | 라벨·eyebrow·보조 |
| `--tt-stat-value-size` | `2.125rem` | 지표 값 |

**모든 수치는 `font-variant-numeric: tabular-nums`.** 열이 어긋나거나 카운터가 흔들리지 않게 한다.

### 3.8 버튼

`--tt-button-*` 9개 토큰이 유일한 정의다(§5.1 계약). 높이 `2.5rem`, 좌우 `1.15rem`,
글자 `0.85rem`/`600`, 모서리 pill. 변형은 `primary`·`secondary`·`ghost`·`danger`,
크기는 `default`·`compact`. **그 외 변형을 만들지 않는다.**

### 3.9 색

| 용도 | 값 |
|---|---|
| 페이지 배경 | `#f9fafc` |
| 카드·테이블 표면 | `#ffffff` |
| 카드 테두리 | `--tt-color-border` |
| 테이블 행 구분선 | `#f0f1f4` |
| 브랜드 | `#8066ff` / 강조 `#5c42da` |

상태색은 `--tt-color-{success,warning,danger,info}` 와 각 `-surface`/`-fg` 쌍만 쓴다.

### 3.10 모바일 (차주·호출자)

**콘솔과 다른 시스템이다.** `docs/design-canon/pwa/` 시안 4장에서 실측했고,
값은 `--tt-m-*` 토큰이 전부 소유한다. 화면은 `packages/ui`의 모바일 프리미티브를
조합한다 — 카드를 다시 정의하지 않는다.

```
        taptolk            ← 로고만. 뒤로가기·타이틀바 없음
┌────────────────────────┐
│      120라7098          │ ← 흰 카드. 차량번호는 브랜드색 대형 수치
│   차주님께 전달했습니다     │
└────────────────────────┘
┌────────────────────────┐
│ [칩]  선택지 한 줄        │ ← 선택 = 브랜드 테두리 + 틴트. 두 번째 카드가 아니다
└────────────────────────┘
  [🔷 주 액션]              ← 화면당 primary 1개, 전체 폭
     보조 링크
─────────────────────────
 MESSAGES ALERT HISTORY SETTINGS  ← 활성 탭만 브랜드색
```

| 항목 | 토큰 | 값 |
|---|---|---|
| 열 최대 폭 | `--tt-m-max-width` | `30rem` |
| 좌우 여백 | `--tt-m-gutter` | `1.25rem` |
| 카드 간격 | `--tt-m-stack-gap` | `1.25rem` |
| 카드 모서리·패딩 | `--tt-m-card-radius` / `-padding` | `1rem` / `1.25rem` |
| 선택지 행 높이 | `--tt-m-choice-height` | `4rem` |
| 주 버튼 높이 | `--tt-m-button-height` | `2.875rem` |
| 입력 높이 | `--tt-m-field-height` | `3.25rem` |
| 차량번호 | `--tt-m-plate-size` | `2rem`, 브랜드색, tabular |
| 화면 질문 | `--tt-m-heading-size` | `1.3125rem` |
| 본문·선택지 | `--tt-m-title-size` | `1rem` |
| 로고 폭 | `--tt-m-brand-width` | `8.75rem` |
| 하단 탭 | `--tt-m-tabbar-height` | `4.25rem` + safe-area |
| 브랜드 | `--tt-m-accent` | `#8a6eff`(로고 톤), 틴트 12% |

- **모바일 배경은 흰색에 가깝다**(`--tt-m-background`). 그래서 흰 카드는 테두리 없이
  분리된다. 테두리를 두르는 카드는 차주 응답 카드(`--accent`) 하나뿐이다
- **화면당 primary 1개.** `이전`/`다음`만 예외이고 `이전`은 secondary다
- 보내는 동작에는 로고 마크, 종료 확인에는 없다.
  **⚠️ 마크 에셋이 아직 없다.** 시안의 3점 마크는 로고의 일부이고 AGENTS.md가
  로고 크롭·추출을 금지한다. 별도 마크 파일을 받기 전까지 `mark`를 비워 둔다
- 시안 4장에는 모든 프레임에 하단 탭이 그려져 있지만 **호출자는 무회원이다.**
  `apps/web/policies/route-policy.ts`가 `publicCaller`에 `bottomNavigation: "hidden"`을
  못박고 있고 WCJ가 검사한다. 탭은 차주 화면의 것이다
- 시안의 `평균 응답 예상 시간 : 3분`·`확인 평균시간 2분`·`★가장 많이 사용된 멘트`는
  **실측이 쌓이기 전에는 그리지 않는다**(§4)

실측 주석: 시안 4장이 같은 뷰포트에서 렌더되지 않았다(여백이 01에서 18.3px,
03에서 22.3px). 카드 폭 기준으로 정규화한 뒤 가까운 정수값을 택했다. 높이와 색은
4장이 모두 일치했다.

**글자 크기는 글자 폭(advance)에 맞췄다.** 시안의 한글 폰트는 글리프가 em 대비
Pretendard보다 크다(높이 기준으로 맞추면 1.4배). 높이에 맞추면 시안에서 한 줄인
선택지가 두 줄로 넘어가므로, **줄이 시안대로 떨어지도록 폭 기준**으로 정했다.
비율은 시안 그대로다 — 본문 1 : 질문 1.32 : 차량번호 2.01.

---

## 4. 정직성 규칙 (디자인 규칙과 동급)

레퍼런스는 가짜 데이터로 채운 목업이다. **목업을 닮게 만들려고 없는 데이터를 지어내지 않는다.**

- 분모가 0이면 `—`. 1로 바꿔 100%를 만들지 않는다
- 한도가 0이면 `—`. `0%`를 그리지 않는다
- 사용자가 확정하지 않은 임계(S7 응답 품질, S9 리스크 등급)로 등급·색을 칠하지 않는다
- 권한이 없어 볼 수 없는 값은 `0`이 아니라 `—`
- 데이터가 없으면 **빈 상태**를 보여준다. 자리를 채우지 않는다

---

## 5. 게이트

| 명령 | 무엇을 막는가 |
|---|---|
| `corepack pnpm validate:design-system` | 화면별 CSS가 토큰 값을 다시 쓰는 것, 콘솔 표면 규칙 미등록, 카드-인-카드 |
| `corepack pnpm validate:wcj` | KO/EN 누락, `<html lang>`, SemanticHeading, 접근성, 여정 |

**둘 다 통과해야 화면 작업이 끝난 것이다.** 두 게이트는 서로 다른 것을 본다. WCJ는 레이아웃을
검사하지 않는다 — WCJ PASS가 "레퍼런스와 같다"는 뜻이 아니다.

자동 검증은 **눈으로 하는 대조를 대체하지 않는다.** 관리자 화면은 로그인 뒤라 사람이 캡처해서
정본과 대조한다.

---

## 6. 문서 권위 순서

1. `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md` — 제품 범위
2. `AGENTS.md` — 엔지니어링 계약
3. **`DESIGN_SYSTEM.md`(이 문서) + `docs/design-canon/` — 화면이 어떻게 생기는가**
4. `docs/architecture/` — 승인된 아키텍처
5. `docs/development/` 워크오더·PRD — 무엇을 만드는가
6. `docs/handoff-*.md` — **세션 상태 기록일 뿐. 설계 근거로 인용 금지**

충돌하면 위가 이긴다.
