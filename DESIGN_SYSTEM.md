# Taptolk Design System

**이 문서가 화면 작업의 유일한 정본이다.** handoff 문서·워크오더·PRD의 서술은 설계 근거가
아니다. 그것들은 세션 상태와 요구사항을 기록할 뿐이고, **화면이 어떻게 생겨야 하는지는 여기와
`docs/design-canon/`만이 정한다.**

이 문서가 생긴 이유: 여러 세션에 걸쳐 handoff의 산문 설명("3구역으로 분리한다" 같은)이 설계
근거로 쓰였고, 그때마다 이미 컴펌된 시안과 다른 화면이 만들어졌다. 정본이 코드베이스 밖(git
히스토리, 다운로드 폴더, 사라진 scratchpad)에 있었던 것이 원인이다.

---

## 0. 작업 순서 (건너뛰지 말 것)

화면을 만들거나 고치기 전에 **반드시** 이 순서를 따른다.

1. **정본을 띄운다.** `docs/design-canon/`을 정적 서버로 열고 대상 화면의 시안을 화면에 띄운다.
   ```bash
   cd docs/design-canon && python3 -m http.server 8907
   ```
   `reference-1-admin-console.png`은 이미지 뷰어로 연다.
2. **패턴을 §3에서 고른다.** 만들려는 화면이 어떤 패턴 조합인지 먼저 정한다.
   목록 화면인가, 위자드인가, 상세인가.
3. **토큰으로 작업한다.** 값이 틀리면 `packages/ui/src/styles/tokens.css`를 고친다.
   화면별 CSS 블록에 수치를 다시 쓰지 않는다(§2).
4. **게이트를 통과시킨다.**
   ```bash
   corepack pnpm validate:design-system
   corepack pnpm validate:wcj
   ```
5. **눈으로 대조한다.** 구현 화면과 정본 시안을 같은 뷰포트 폭(1487px)에서 나란히 두고 비교한다.
   다른 점이 있으면 고치거나, 고칠 수 없으면 사용자에게 **구체적으로** 묻는다.

**금지:** 정본을 열어보지 않고 화면을 만드는 것. 이것이 반복된 실패의 단일 원인이었다.

---

## 1. 정본 (`docs/design-canon/`)

| 파일 | 무엇의 정본인가 |
|---|---|
| `reference-1-admin-console.png` | **관리자 콘솔 전체의 시각 언어.** 셸·목록 화면·우측 레일·테이블·지표 스트립 |
| `console-qr-wizard.html` | QR 제작 위자드 (스테퍼 + 패널 + 미리보기 레일) |
| `console-shell.html` | 셸·사이드바·상단바 |
| `console-pages.html` | 목록/관리 화면들 |
| `console-detail.html` | 상세 화면 |
| `sticker-previews/*.png` | 템플릿 선택 카드에 쓰는 실제 렌더 결과 |
| `pwa/01-caller-compose.png` | **PWA** — 스캔 직후 호출자가 상황을 고르고 메시지를 보내는 화면 |
| `pwa/02-caller-sent-waiting.png` | **PWA** — 전달 완료, 차주 확인 대기 |
| `pwa/03-owner-reply.png` | **PWA** — 차주가 받은 메시지에 응답 |
| `pwa/04-caller-response-received.png` | **PWA** — 차주 응답 도착, 호출 종료 |
| `pwa/sticker-physical.png` | 실물 스티커 |

**PWA 공통**: 상단 taptolk 로고, 흰 카드 스택, 선택지는 아이콘 + 한 줄, 가장 많이 쓰는
선택지에 브랜드 틴트, 하단 primary 버튼 1개(로고 마크 + 라벨),
하단 탭 `MESSAGES · ALERT · HISTORY · SETTINGS`. **차량번호는 브랜드색 대형 수치.**

정본은 **배포되지 않는 경로**에 둔다. `apps/web/public/`에 프로토타입을 두지 않는다.

### 정본에 없는 화면을 만들 때

임의로 만들지 말고 **§3의 패턴을 조합**한다. 새 패턴이 필요하면 사용자 승인을 받고 §3에 추가한다.

---

## 2. 단일 정의 규칙 (CSS로 강제됨)

**globals.css는 7,000줄이 넘고, 한때 68개 규칙이 각자 카드를 정의하고 있었다.** 그 상태에서는
한 화면을 고쳐도 다음 화면이 그대로다. 그래서 아래 세 가지는 규칙이다.

1. **새 콘솔 표면 → `apps/web/app/globals.css` 최하단 "콘솔 표면 규칙" 블록에 셀렉터만 추가한다.**
   `background`/`border`/`border-radius`/`padding` 세트를 또 쓰지 않는다.
2. **값을 바꿔야 하면 토큰을 바꾼다.** 콘솔 전체가 함께 움직인다.
3. **카드 안에 카드 금지.** 구분이 필요하면 헤어라인 하나다.

화면별 CSS 블록이 가져도 되는 것: **컬럼 `min-width`, `text-overflow` 처리, 컬럼별 폭 제한,
그리드 열 수.** 그 외 수치(padding·radius·font-size·font-weight·height·gap)는 토큰에서 온다.

이 규칙은 `corepack pnpm validate:design-system`이 검사한다.

---

## 3. 패턴 (레퍼런스1 실측)

측정은 레퍼런스1을 자체 폭 1487 CSS px에서 픽셀 단위로 읽은 값이다.

### 3.1 셸

```
┌──────────┬────────────────────────────────────────────────┐
│ 사이드바  │ 상단바  [범위 선택]        [KO|ENG] [시각] [계정] │
│ 206px    ├────────────────────────────────────────────────┤
│          │ 페이지 본문 (좌우 여백 --tt-console-gutter)      │
```

- 사이드바 `--tt-sidebar-width` `12.875rem` (206px), 흰 배경, 우측 헤어라인
- 항목 높이 `2.5rem`, pitch 50px, radius `0.625rem`
- 활성 항목: 브랜드 8% 틴트 알약 + 브랜드 강조색 글자. **왼쪽 inset 바는 없다**
- 그룹 라벨: `0.62rem` · 굵게 · 대문자 · muted
- 본문은 사이드바 옆 공간을 **채운다**(`--tt-console-max-width: none`)

### 3.2 페이지 헤더

```
서비스 운영                        ← eyebrow, 브랜드색, --tt-label-size
QR 스티커 발주                     ← h1, --tt-heading-1-size (26px)
현장·디자인·수량을 정해…            ← 설명, muted, --tt-body-size
                                      [우측 정렬 주 액션 버튼]
```

h1과 같은 줄 오른쪽에 주 액션 하나. 그 이상은 두지 않는다.

### 3.3 지표 스트립

**한 장의 카드 안에 셀들.** 셀마다 카드를 그리지 않는다(박스-인-박스의 근원이었다).

- 스트립 = 프레임만, `padding: 0`
- 셀 사이는 세로 헤어라인
- 셀 = 아이콘 · 라벨(`--tt-label-size`, muted) · 값(`--tt-stat-value-size`, tabular) · 보조줄
- **값이 없으면 `—`.** 분모 0을 1로 바꿔 100%를 만들지 않는다

### 3.4 목록 화면 (레퍼런스1의 본체)

```
┌─────────────────────────────────┐ ┌──────────────┐
│ [검색] [필터▾] [필터▾] [내보내기] │ │ 승인 대기 (5) │
│ ─────────────────────────────── │ │ ┌──────────┐ │
│  테이블 (헤더 밴드 + 48px 행)     │ │ │ 배지      │ │
│ ─────────────────────────────── │ │ │ 제목      │ │
│  총 28개  ‹ 1 2 3 ›  [10개▾]    │ │ │ [거절][승인]│ │
└─────────────────────────────────┘ └──────────────┘
```

- **본문 카드 + 우측 레일** 2단. 레일은 고정 폭, 본문은 나머지를 채운다
- 필터바·테이블·페이지네이션은 **같은 카드 안**에 헤어라인으로 나뉜다
- 우측 레일 항목: 종류 배지 · 제목 · 소속 · 시각 · `거절`/`승인` 인라인 버튼
- 레일 하단에 전체 관리 버튼 1개

### 3.5 테이블

| 항목 | 토큰 | 값 |
|---|---|---|
| 행 최소 높이 | `--tt-table-row-min-height` | `3rem` (48px) |
| 헤더 밴드 높이 | `--tt-table-header-min-height` | `2.375rem` (38px) |
| 셀 padding | `--tt-table-cell-padding-block/-inline` | `0.7rem` / `1rem` |
| 본문 크기 | `--tt-table-font-size` | `0.8125rem` |
| 헤더 크기 | `--tt-table-header-font-size` | `0.8125rem` — **본문과 같다** |
| 헤더 굵기 | `--tt-table-header-font-weight` | `600` |
| 헤더 변환 | `--tt-table-header-transform` | `none` — **대문자 없음** |
| 헤더 배경 | `--tt-table-header-surface` | `#f7f8fa` |
| 행 구분선 | `--tt-table-divider-color` | `#f0f1f4` — **카드 테두리보다 옅다** |
| 셀 보조줄 | `--tt-table-meta-font-size` | `0.6875rem` |

**헤더는 축소된 대문자 캡션이 아니다.** 본문 크기·muted·semibold이고 옅은 밴드 위에 놓인다.
한국어는 대문자가 없으므로 `text-transform: uppercase`와 넓은 `letter-spacing`은 자간만 망가뜨린다.

셀 안에서 쓰는 것: 상태 알약, 가로 미터 바(값 + 바), 2줄 셀(제목 + 보조줄), 배지, 고스트 버튼.

### 3.6 위자드 (QR 제작)

**흐름은 5단계, 한 페이지다.**

```
① 현장 선택 → ② 디자인 → ③ 수량 → ④ 검토·승인 → ⑤ 제작·입고
   관리회사·현장   템플릿+상단 브랜드  100 이하 분할   샘플·디코드 확인   인쇄·배송·재고
```

이 5단계가 나온 근거:

- 컴펌된 `console-qr-wizard.html`은 6단계였다
- 이후 시안에서 **템플릿(2)과 상단 브랜드(3)를 "디자인" 한 단계로 통합**했다.
  같은 미리보기를 보며 내리는 결정이라 단계를 왕복할 이유가 없다 → 6 → 5
- 같은 시안이 제안한 **3구역 분리(발주/승인 큐/제작 추적)는 폐지됐다.**
  검토·승인과 제작·입고는 위자드 단계로 남는다

**`?section=order|approvals|tracking` 같은 구역 분리 URL을 만들지 않는다.**

권한이 없는 단계는 구역으로 떼어내지 않고 **잠금·읽기 전용으로 표시**한다.
`qr-batch:generation-approve`는 SUPER_ADMIN 전용이므로, 다른 역할에게 ④단계는
현황과 대기 사유를 보여주되 승인 버튼을 열지 않는다. 구조를 권한에 맞춰 쪼개지 않는다.


```
┌───────────────────────────────────────────────────┐
│ ✓ 현장 선택   ② 디자인   ③ 수량   ④ 검토·승인 …    │  ← 스테퍼 = 카드 1장
└───────────────────────────────────────────────────┘
┌──────────────────────────────────┐ ┌─────────────┐
│ 단계 제목                          │ │ 실시간 미리보기│
│ 설명                              │ │  [스티커 렌더] │
│ …단계 본문…                        │ │  캡션         │
│ ────────────────────────────────  │ │  템플릿  블루…│
│ [이전]                 [다음 · 수량]│ │  규격   85mm…│
└──────────────────────────────────┘ └─────────────┘
```

- **스테퍼는 카드 한 장**, 셀마다 카드를 그리지 않는다
- 셀 = 원형 번호 + 제목 + 보조줄. 완료 = 초록 체크, 현재 = 브랜드 틴트로 셀 전체를 채움
- **본문 패널 1장 + 우측 미리보기 레일 고정.** 미리보기는 **실제 생성 엔진 렌더**이고
  선택을 바꾸면 즉시 반영된다
- 패널 하단 `이전` / `다음 · <다음 단계 이름>` — 왼쪽 ghost, 오른쪽 primary
- 미리보기 레일 하단 kv 목록: 라벨 muted 좌측 / 값 굵게 우측 정렬

**선택 카드**(템플릿 선택): 렌더 이미지 위, 굵은 제목, muted 보조줄. 선택 시 브랜드 테두리 + 틴트.

**선택 행**(상단 브랜드): 전체 폭 테두리 행, 라디오 + 제목 + 보조줄, 우측에 상태 배지.
선택 시 브랜드 테두리 + 틴트.

**고지 노트**: 성공(연녹) `✓`, 경고(연황) `⚠`, 위험(연적). 본문 좌측에 색 바.

### 3.7 타이포

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--tt-heading-1-size` | `1.625rem` | 페이지 제목 |
| `--tt-heading-2-size` | `1.0625rem` | 카드·레일 제목 |
| `--tt-heading-3-size` | `0.9375rem` | 패널 안 소제목 |
| `--tt-body-size` | `0.875rem` | 본문 |
| `--tt-label-size` | `0.75rem` | 라벨·eyebrow·보조 |
| `--tt-stat-value-size` | `2.125rem` | 지표 값 |

**모든 수치는 `font-variant-numeric: tabular-nums`.** 열이 어긋나거나 카운터가 흔들리지 않게 한다.

### 3.8 버튼

`--tt-button-*` 9개 토큰이 유일한 정의다(§5.1 계약). 높이 `2.5rem`, 좌우 `1.15rem`,
글자 `0.85rem`/`600`, 모서리 pill. 변형은 `primary`·`secondary`·`ghost`·`danger`,
크기는 `default`·`compact`. **그 외 변형을 만들지 않는다.**

### 3.9 색

| 용도 | 값 |
|---|---|
| 페이지 배경 | `#f9fafc` |
| 카드·테이블 표면 | `#ffffff` |
| 카드 테두리 | `--tt-color-border` |
| 테이블 행 구분선 | `#f0f1f4` |
| 브랜드 | `#8066ff` / 강조 `#5c42da` |

상태색은 `--tt-color-{success,warning,danger,info}` 와 각 `-surface`/`-fg` 쌍만 쓴다.

---

## 4. 정직성 규칙 (디자인 규칙과 동급)

레퍼런스는 가짜 데이터로 채운 목업이다. **목업을 닮게 만들려고 없는 데이터를 지어내지 않는다.**

- 분모가 0이면 `—`. 1로 바꿔 100%를 만들지 않는다
- 한도가 0이면 `—`. `0%`를 그리지 않는다
- 사용자가 확정하지 않은 임계(S7 응답 품질, S9 리스크 등급)로 등급·색을 칠하지 않는다
- 권한이 없어 볼 수 없는 값은 `0`이 아니라 `—`
- 데이터가 없으면 **빈 상태**를 보여준다. 자리를 채우지 않는다

---

## 5. 게이트

| 명령 | 무엇을 막는가 |
|---|---|
| `corepack pnpm validate:design-system` | 화면별 CSS가 토큰 값을 다시 쓰는 것, 콘솔 표면 규칙 미등록, 카드-인-카드 |
| `corepack pnpm validate:wcj` | KO/EN 누락, `<html lang>`, SemanticHeading, 접근성, 여정 |

**둘 다 통과해야 화면 작업이 끝난 것이다.** 두 게이트는 서로 다른 것을 본다. WCJ는 레이아웃을
검사하지 않는다 — WCJ PASS가 "레퍼런스와 같다"는 뜻이 아니다.

자동 검증은 **눈으로 하는 대조를 대체하지 않는다.** 관리자 화면은 로그인 뒤라 사람이 캡처해서
정본과 대조한다.

---

## 6. 문서 권위 순서

1. `TAPTOLK_MASTER_DEVELOPMENT_SPEC.md` — 제품 범위
2. `AGENTS.md` — 엔지니어링 계약
3. **`DESIGN_SYSTEM.md`(이 문서) + `docs/design-canon/` — 화면이 어떻게 생기는가**
4. `docs/architecture/` — 승인된 아키텍처
5. `docs/development/` 워크오더·PRD — 무엇을 만드는가
6. `docs/handoff-*.md` — **세션 상태 기록일 뿐. 설계 근거로 인용 금지**

충돌하면 위가 이긴다.
